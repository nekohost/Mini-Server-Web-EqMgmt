# 미구현 로드맵(Unimplemented Roadmap) 관리 문서

이 문서는 `ROADMAP.md`에서 현재까지 개발이 완료되지 않은 기능 항목만을 별도로 분리하여 우선순위별 작업 대기열(To-Do)로 관리합니다.
개발이 완료된 항목은 `Rule.md` 제7조에 의거하여 이 문서에서 제거되고 `ROADMAP.md` 및 `FEATURES.md`에서 영구 관리됩니다.

---

## 📌 [최상] 미구현 목록 (우선순위: 최상)

1. **[제안-022] & [제안-033] 장비 상태(Status) 및 라이프사이클(이력) 관리 통합 기능**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 자산 관리의 핵심인 라이프사이클(정상, 대여중, 수리중, 폐기 등)을 시스템적으로 추적하기 위해 필수적인 공통 요소입니다.
   - **구현 우선순위:** [최상] 
   - **구현 가능성:** [높음] `equipment` 테이블에 `Status` 컬럼을 확장하고, 상태 변경 이력을 기록하는 `equipment_history` 테이블을 신설하여 구현 가능합니다.
   - **상세 내용:** 장비의 현재 상태 추적 필드 추가 및 상태 이력 추적 구현. **구현 시 대시보드 통계(`app.py` `api_dashboard_stats`)에 하드코딩된 `'정상' as Status` 임시 조치문을 실제 `Status` 컬럼 참조 쿼리로 반드시 복구할 것.**

2. **[제안-013] 웹 기반 DB 데이터 백업 및 복원 (Administrator Backup & Restore)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 물리적인 미니서버(특히 SD카드 기반) 손상 시 모든 자산 데이터가 증발하는 치명적인 사태를 방지하는 가장 훌륭하고 단순한 재난 복구 수단입니다.
   - **구현 우선순위:** [최상] 시스템 데이터의 영속성 및 생존과 직결된 핵심 기능입니다.
   - **구현 가능성:** [매우 높음] SQLite DB가 단일 파일(`equipment.db`)이라는 점을 십분 활용하여, 관리자가 해당 파일을 다운로드(send_file)하거나 새 파일로 덮어쓰기(업로드)하는 단순한 API만으로 백업/복원 기능을 완성할 수 있습니다.
   - **상세 내용:** 관리자 전용 DB 덤프/다운로드(ZIP/DB 파일) 기능 및 백업 파일 업로드를 통한 DB 롤백/복원 기능.

---

## 📌 [상] 미구현 목록 (우선순위: 상)

1. **[제안-023] 상세 검색 및 필터링 기능 (API 단)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 장비 데이터가 누적됨에 따라 원하는 장비를 빠르게 찾기 위한 필수 기능이므로, 상태 관리 도입 이후 최우선적으로 구현합니다.
   - **구현 우선순위:** [상] 데이터 활용도를 위해 필수적입니다.
   - **구현 가능성:** [높음] 백엔드 API에서 쿼리 파라미터를 받아 동적 쿼리를 생성하고, 프론트엔드 UI(체크박스/셀렉트박스 등)와 연동하여 무난히 구현 가능합니다.
   - **상세 내용:** 카테고리, 제조사, 상태, 구입일자 등 다양한 조건으로 장비를 다중 검색하고 필터링하는 기능.

2. **[제안-002] 입력값 유효성 검증 강화 (프론트엔드 + 백엔드)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 사용자의 악의적, 혹은 실수로 인한 잘못된 데이터 입력을 서버단에서 원천 차단하는 것은 DB 정합성과 시스템 안정성을 위해 필수적입니다.
   - **구현 우선순위:** [상] DB 스키마 무결성 유지와 직결됩니다.
   - **구현 가능성:** [매우 높음] Python 백엔드의 API 엔드포인트(`/register`, `/api/equipment`)에 간단한 정규식(Regex)과 조건문을 추가하는 것만으로 신속히 방어 로직을 구성할 수 있습니다.
   - **상세 내용:** 비밀번호 복잡도(영문/숫자/특수문자 8자 조합), 아이디 제한(특수문자/공백 차단), 장비 등록 시 필수 필드 서버단 검증.

3. **[제안-007] API 요청 속도 제한 (Rate Limiting)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 브루트포스(Brute-force) 공격으로 인한 관리자 계정 탈취를 막기 위해 로그인 API 등에 대한 최소한의 호출 속도 제한은 보안상 필수적입니다.
   - **구현 우선순위:** [상] 보안 강화 차원입니다.
   - **구현 가능성:** [중~높음] 외부 라이브러리(`flask-limiter`)를 도입하거나, 미니서버의 성능을 고려해 Python 내장 딕셔너리와 시간을 이용한 in-memory Rate Limiting을 가볍게 구현할 수 있습니다.
   - **상세 내용:** 동일 계정/IP 기준 N회(예: 5회) 로그인 연속 실패 시 일정 시간(예: 15분) 계정 잠금.

4. **[제안-010] 장비 이미지 및 문서 첨부 기능 (영수증, 사진, 매뉴얼)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 영수증(증빙자료)과 매뉴얼(사용법) 보관은 자산 관리의 필수 요소입니다. 텍스트만으로는 상세 정보나 회로도 등을 보관하기 어렵습니다.
   - **구현 우선순위:** [상] 사용자 편의성과 자산 관리의 완결성을 위해 우선순위가 높습니다.
   - **구현 가능성:** [중] Flask의 파일 업로드 처리(`secure_filename`), 미니서버의 디스크 보호를 위한 용량 및 확장자 제한 로직, 그리고 `equipment_files` 테이블 추가 작업이 필요합니다.
   - **상세 내용:** 장비 등록/수정 모달에 파일 첨부 UI 추가 및 업로드된 영수증/이미지 preview 기능 제공.


6. **[제안-017] 감사 로그 고도화 (다중 필터링, 컬럼 커스텀, 페이징 및 영속화)**
   - **상태:** [일부 완료 / 고도화 대기중]
   - **결정 사유:** 대용량 감사 로그 탐색 효율성과 보안 추적 성능을 대폭 향상시키는 엔터프라이즈급 모니터링 필수 기능입니다.
   - **구현 우선순위:** [상] 보안 모니터링 고도화 차원입니다.
   - **구현 가능성:** [매우 높음] RESTful 비동기 API(`/api/audit_logs`), 10,000건 대량 조회 및 `user_settings` 연동 기반 완성.
   - **상세 내용:**
     - [완료] RESTful 비동기 API, 전역 페이징, per_page 설정(`user_settings` 영속화), max 10,000 대량 조회.
     - [대기중] 복합 조건 다중 필터링 (행위자, Action 유형, 시작/종료 일시 동시 적용) 및 표 표시 열 선택(컬럼 커스터마이징) 기능 추가.


---

## 📌 [중] 미구현 목록 (우선순위: 중)

1. **[제안-008] 장비 보증기간(A/S) 및 교체 주기 추적 (D-Day 카운트다운)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 자산 관리의 핵심 중 하나인 "유지보수 및 교체 기간"을 추적할 수 있어 시스템의 실용성을 크게 높여줍니다.
   - **구현 우선순위:** [중] 장비 데이터의 품질과 관리 편의성 향상을 위해 유용합니다.
   - **구현 가능성:** [높음] `equipment` 테이블에 `WarrantyDate` 컬럼을 추가하고, 프론트엔드에서 현재 날짜와 비교하는 JavaScript(D-Day 계산) 로직만 추가하면 됩니다.
   - **상세 내용:** 장비 카드/목록에서 잔여 D-Day 계산 및 뱃지(Badge) 표시. **(추가 확장) 매일 자정, 보증기간 만료 임박(예: 7일) 장비 리스트를 담당자 이메일로 자동 리포팅하는 백그라운드 스케줄러 기능 포함.**

2. **[제안-019] 사용자 맞춤형 레이아웃 스킨(Skin) 시스템 (Edge vs Standard)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 서로 다른 기기(모바일/데스크탑) 환경에서 최적화된 UI를 사용자 입맛에 맞게 선택하여 활용할 수 있어 UX 만족도가 극대화됨.
   - **구현 우선순위:** [중] UI 파편화를 해결하면서도 사용자 편의성을 챙길 수 있습니다.
   - **구현 가능성:** [매우 높음] 이미 구현된 `user_settings` 인프라를 재활용하여 `layout_skin` 값을 저장/불러오고, Python 서버 렌더링 시 조건부로 HTML을 분기하면 됨.
   - **상세 내용:** 데스크탑 통일성 유지용 Standard 스킨과 모바일 공간 활용 극대화용 Edge 스킨을 분리 제작하고, 원클릭 스킨 전환 버튼을 통한 UI 설정 영속화.


---

## 📌 [하] 미구현 목록 (우선순위: 하)

1. **[제안-026] 엑셀(CSV) 데이터 일괄 등록 및 다운로드**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 대규모 장비 관리에 유용하지만, 개인 목적의 소규모 운영 환경에서는 당장 실사용 빈도가 높지 않으므로 도입을 잠시 미룹니다.
   - **구현 우선순위:** [하]
   - **구현 가능성:** [중] `pandas` 또는 Python 내장 `csv` 모듈을 통한 파일 I/O 파싱 로직 및 DB 벌크 인서트 트랜잭션 등 백엔드 작업이 소요됩니다.
   - **상세 내용:** 엑셀 파일을 통해 다수의 장비를 한 번에 업로드(Import)하거나 현재 등록된 장비 목록을 파일로 다운로드(Export)하는 기능.

---

## 📌 [최하] 미구현 목록 (우선순위: 최하)
*(현재 최하 우선순위로 등록된 기능은 없습니다. 추후 보류된 제안 중 일부가 재논의를 거쳐 이곳에 추가될 수 있습니다.)*

---
*참고: 위 기능들은 프로젝트 학습 목적(REST API 기본기)과 미니서버 구동 환경 최적화(경량화)를 고려하여 순차적으로 구현할 예정입니다. 각 항목의 구현 완료 시 `UNIMPLEMENTED_ROADMAP.md`에서 제거되고 `ROADMAP.md` 및 `FEATURES.md`로 일치화됩니다.*


