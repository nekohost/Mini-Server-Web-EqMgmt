.env
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/

# 채팅 기록 로컬에만 보관 (Github 푸시 방지)
Chat/
scratch/
Temp/

# 제안-013: 운영 DB와 백업·복원 런타임 작업 사본은 Git에 올리지 않음
instance/
*.db
*.sqlite
*.sqlite3

# SQLite sidecar에도 실제 DB 페이지가 들어가므로 원본과 함께 제외한다.
*.db-wal
*.db-shm
*.db-journal
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
*.sqlite3-wal
*.sqlite3-shm
*.sqlite3-journal
*.db.before-*.bak
*.db.notification.lock

# 감사 snapshot은 선행 dirty 파일의 원문을 포함하므로 로컬에만 보존한다.
.agent-governance/comment-sync/audit-snapshot*.json
