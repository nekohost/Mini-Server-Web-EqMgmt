# Mini-Server-Web-EqMgmt (장비 및 자산 통합 관리 시스템)

이 프로젝트는 개인 보유 장비(노트북, 보조배터리, 모니터, 이어폰 등)를 등록하고 관리하기 위해 Windows PC에서 개발하여 GitHub를 거쳐 Linux Lite 미니서버에 배포되는 웹 애플리케이션입니다.

---

1. 프로젝트 배경 및 목적
	1-1. 학습 목적: Python을 배우는 사용자가 Flask 백엔드와 RESTful API의 기본 구조를 공부하기 위한 프로젝트입니다.
	1-2. 개발 환경: Windows PC (개발/Git Push) ➡️ GitHub ➡️ Linux Lite 미니서버 (배포/Git Pull).
	1-3. 사용자 특성: 개발 초보자이므로 모든 코드에는 상세한 설명 주석이 반드시 포함되어야 하며, 변경 발생 시 의존성 여파를 반드시 명확하게 안내해야 합니다.

2. 기술 스택 및 네트워크 정보
	2-1. Language & Backend: Python 3 + Flask (되도록 가벼운 REST API 서버)
	2-2. Database: SQLite3 (`equipment.db` 파일 형태 - 미니서버 리소스 사용 되도록 최소화)
	2-3. Database GUI (PC용): DBeaver (SSMS 스타일의 DB 관리 환경 제공)
	2-4. Frontend: HTML5 + Vanilla JavaScript + Tailwind CSS (CDN)
	2-5. 외부 의존성 리소스는 필요 시 사용한다. 단 사용하지 않고 구현가능한 방법에 대해서도 동시에 제안한다.
	2-6. 미니서버 IP: `192.168.0.166` (포트: `5000`)
	2-7. 실제 접속 URL (모바일/PC 공통): `http://192.168.0.166:5000`

3. 핵심 요구사항 및 화면 최적화 원칙
	3-1. 장비 데이터 구조: 
		3-1-1. 기본 필드: 고유ID(`id`), 장비별명(`name`), 카테고리(`category`), 제조사(`manufacturer`), 모델명(`model_name`), 구입일(`purchase_date`), 시리얼넘버(`serial_number`), 메모(`memo`).
		3-1-2. CRUD(생성, 조회, 수정, 삭제) 기능 완벽 제공.
	3-2. 실시간 반응형 및 모바일/폴더블 최적화:
		3-2-1. 접은 스마트폰/폴더블(세로): 1열 카드 레이아웃 (`grid-cols-1`)
		3-2-2. 펼친 폴더블/태블릿: 2열 카드 레이아웃 (`sm:grid-cols-2`)
		3-2-3. PC 및 대형 화면: 3~4열 카드 레이아웃 (`lg:grid-cols-3 xl:grid-cols-4`)
		3-2-4. 기기 리소스 최적화를 위해 무거운 JS 라이브러리를 사용하지 않고 초경량 Tailwind CSS 활용.

4. 소프트웨어 설계 및 의존성 지침 (AI Assistant 필수 준수 수칙)
AI(Codex, ChatGpt, Gemini, Claude 등등...)가 이 프로젝트의 코드를 수정하거나 기능을 추가할 때는 아래 원칙을 엄격히 준수해야 합니다.
	4-1. 컬럼(필드) 확장성 보장
		4-1-1. DB 테이블에 새로운 컬럼(예: 가격 `price`, 보증기간 `warranty_date` 등)이 추가되더라도 기존 코드가 절대로 파괴되지 않아야 합니다. 백엔드의 DB 조회 로직은 `sqlite3.Row` 기반으로 작성되어 컬럼 추가 시 파이썬 코드 수정을 최소화합니다.
	4-2. 컬럼 추가 시 동시 수정 체크리스트 (의존성 체인)
	새로운 컬럼을 추가할 경우, 반드시 아래 3가지 지점을 동시에 수정해야 하며 사용자에게 이를 안내해야 합니다:
		4-2-1. `app.py` -> `init_db()` : `CREATE TABLE` 구문에 새 컬럼 정의 추가
		4-2-2. `app.py` -> `add_equipment()` : `INSERT INTO` 구문 및 `data.get()`에 새 컬럼 추가
		4-2-3. `templates/index.html` : 
			4-2-3-1. `<form>` 태그 내 입력받을 `<input>` 추가
			4-2-3-2. JS `payload` 객체에 새 필드 추가
			4-2-3-3. `fetchEquipment()` 내부의 HTML 카드 렌더링 부분에 `${item.새컬럼명}` 출력 추가
	4-3. 코드 주석 보존 수칙
	모든 함수와 API 라우트 상단에는 반드시 아래 항목이 명시된 주석을 유지해야 합니다:
		4-3-1. `[역할]`: 해당 함수의 기능
		4-3-2. `[의존성 관계]`: 의존하는 함수/파일 및 이 함수에 의존하는 프론트엔드 요소
		4-3-3. `[변경 시 영향도]`: 이 코드를 수정할 때 함께 영향을 받는 다른 위치 안내
	4-4. 데이터 보존 원칙 (Data Integrity & Migration)
	모든 DB 스키마 조작 및 변경 사항은 기존 데이터의 보존을 최우선 원칙으로 삼습니다.
		4-4-1. 스키마가 변경된다고 해서 절대로 기존 테이블을 함부로 `DROP`하거나 전체를 초기화해서는 안 됩니다.
		4-4-2. DB 구조 변경이나 데이터 보정이 필요한 경우, `ALTER TABLE`을 활용하거나 사용자에게 데이터를 안전하게 이전(Migration) 또는 직접 쿼리로 처리할 수 있는 정확한 방법과 절차를 사전에 반드시 안내하여야 합니다.
	4-5. 보안 및 환경 설정 수칙 (Security Guidelines)
	시스템이 외부(인터넷)에 공개되는 것을 전제로, 모든 코드와 설정은 안전하게 보호되어야 합니다.
		4-5-1. 민감 정보 하드코딩 금지: `app.secret_key`, DB 접속 계정, API 토큰 등은 절대로 소스코드(`app.py` 등) 내에 평문으로 작성하여 깃허브에 푸시해서는 안 됩니다. 반드시 `.env` 파일 등 별도의 환경 변수 파일로 분리하여 관리하고 `.gitignore`로 제외합니다.
		4-5-2. 환경변수를 통한 유연한 Debug 제어: 미니서버에서 테스트 시 에러를 편하게 확인하기 위해 무조건 `debug=False`로 강제하지 않습니다. 대신 소스 코드에 `debug=True`를 하드코딩하지 않고 환경변수(`.env`)의 `FLASK_DEBUG` 값을 통해 손쉽게 끄고 켤 수 있도록 제어 구조를 만들어야 합니다.
		4-5-3. 세션 및 권한 보호: 세션 탈취 및 CSRF 공격을 방어하기 위해 쿠키 정책(`SESSION_COOKIE_HTTPONLY`, `SESSION_COOKIE_SAMESITE`)을 엄격하게 적용합니다. 외부에서 전달된 입력(파라미터)은 무조건 불신하며, 권한 체크는 항상 서버에 기록된 세션 정보를 기준으로 철저히 수행합니다.

5. 실행 및 배포 방법
	5-1. 미니서버 (Linux Lite 환경)
		5-1-1. 현재 개발환경인 Windows PC에서는 소스코드 작성 및 Git Push 역할만 수행하며, 로컬 테스트를 이 환경에서 절대로 진행하지 않는다.
		5-1-2. 모든 테스트 및 실제 구동은 미니서버(192.168.0.166)에 SSH로 접속하여 진행한다. 
		5-1-3. Staging은 직접 구동을 위해서 분리해둔 것이 아니라 테스트를 위한 안전한 베타의 복사본이다. 즉 그 자체만으로 코드를 판단하고 검토하고 보고서를 만들어내기 위한 것이다.
		5-1-4. 실행 스크립트 예시:
```bash
# 최신 소스코드 동기화
git pull origin main

# 백엔드 서버 구동 (이전 프로세스가 있다면 kill 후 재시작)
python3 app.py

# 접속 URL (내부 네트워크): http://192.168.0.166:5000
```

6. 대화 기록 관리 규칙
AI(Codex, ChatGpt, Gemini, Claude 등등...)와 사용자가 나눈 대화는 날짜별 Markdown 파일로 기록한다.
	6-1. 핵심 무결성 원칙 (최상)
		6-1-1. 사용자 메시지와 AI(Codex, ChatGpt, Gemini, Claude 등등...)의 최종 응답은 요약·의역·문장 수정 없이 Markdown 구조, 코드 블록 및 링크를 포함한 원문 그대로 기록한다.
		6-1-2. 대화 기록에는 설명뿐 아니라 AI(Codex, ChatGpt, Gemini, Claude 등등...)가 제안한 코드, 설정 예시 및 실행 명령도 함께 남긴다. 이 때 기록은 반드시 100% Conversation 에서 출력한 내용과 동일하여야 한다. 토씨 하나라도 틀려서는 안된다.
		6-1-3. AI의 발언은 이전 대화 맥락에 남아있는 다른 AI의 이름을 무비판적으로 복사하지 말고, 반드시 현재 작업을 수행 중인 자기 자신의 실제 모델명을 명확히 자각하여, 예를 들어 Codex인 경우 `## Codex YYYY-MM-DD HH:mm:ss.000` Claude인 경우  `## Claude YYYY-MM-DD HH:mm:ss.000` Gemini인 경우  `## Gemini YYYY-MM-DD HH:mm:ss.000` 등의 형식으로 작성한다. 시각은 `Asia/Seoul` 기준으로 기록한다.
		6-1-4. 비밀번호, API 키, 인증서 개인키, 토큰 및 개인정보는 기록하지 않으며, `<비밀번호>`와 같은 자리표시자로 대체한다.
		6-1-5. [중요] 파일 작성 시 오직 파일 끝에 이어쓰기(Append)만 원칙으로 하며, 기존 대화 내용을 수정하거나 제거하는 행위는 기본적으로 금지된다. 기존 대화 내용을 수정하는것은 누락되거나 잘못 기재된 경우에 사용자가 요청하도록 되어있으나, AI의 검사결과 누락이나 잘못 기재된 내용이 있다면 수정이나 제거를 사용자에게 제안해야한다.
		6-1-6. [중요] 에디터 API(replace_file_content 등)를 활용한 대화 저장은 누락 오류가 빈번하므로, 대화(Chat) 파일 기록 및 저장 작업에 한해서만 오직 터미널 명령어(PowerShell 등)를 사용하여 완전하게 저장한다. (단, 소스코드 및 기타 리소스 편집에는 절대 터미널 쓰기를 사용하지 않는다.)
		6-1-7. [중요] 타임스탬프 파싱 및 시간대 검증 원칙: 대화 로그에서 시간을 추출할 때, 시스템 텍스트에 `Z`(UTC 기호)가 붙어 있더라도 실제로는 이미 로컬 타임(KST)일 수 있다. 기계적으로 9시간을 더하기 전에 반드시 현재 KST 시각과 비교 검증하여 과거 로그가 미래 시간으로 밀려나는 현상을 방지해야 한다.
		6-1-8. [중요] 터미널 인코딩 파괴 방지 원칙: Windows PowerShell을 통해 대화 로그를 덧붙일 때(Append), 스크립트 내부에 한글을 하드코딩하면 ANSI 변환으로 문자가 깨진다(Mojibake). 대화 기록을 덧붙일 때는 반드시 API를 사용해 'UTF-8 중간 텍스트 파일(Intermediate file)'을 먼저 생성한 뒤, 터미널에서 `Get-Content -Encoding UTF8`로 읽어와 덧붙이는 우회 방식을 사용하여 인코딩 파괴를 원천 봉쇄한다.
		6-1-9. [중요] PowerShell 확장 문자열(Here-String) 이스케이프 파괴 방지 원칙: PowerShell을 사용하여 마크다운 텍스트를 파일에 쓸 때, 큰따옴표 기반의 확장 문자열(`@"..."@`)을 사용하면 텍스트 내의 백틱(`` ` ``) 문자가 PowerShell의 이스케이프 문자로 인식되어 원본 텍스트가 파괴되고 제어 문자가 삽입되는 치명적 오류가 발생한다. 따라서 터미널(PowerShell)로 마크다운 문서를 덧붙이거나 쓸 때는 반드시 변수 확장이 일어나지 않는 작은따옴표 기반의 리터럴 문자열(`@'...'@`)만을 사용해야 한다.
	6-2. 자동화 기록 파이프라인 (상)
		6-2-1. [중요] 대화 기록을 파일에 추가하는 작업은 별도의 사용자 승인(플랜 리뷰 등) 절차 없이 즉시 백그라운드에서 진행한다.
		6-2-2. 작업 과정의 중간 안내또한 반드시 대화 원문에 포함하며, 사용자에게 전달한 것과 완벽히 동일하게 기록한다.
		6-2-3. 기록 위치: `Chat/연도/월/일자.md`
		6-2-4. 각 AI(Codex, ChatGpt, Gemini, Claude 등등...) 최종 응답을 생성하기 전에 현재 날짜를 확인한다.
		6-2-5. 해당 날짜의 `Chat/YYYY/MM/DD.md` 파일이 있으면 원문 대화를 파일 끝에 이어 기록한다.
		6-2-6. 해당 날짜 파일이 없으면 연도·월 폴더를 생성하고 새 일자 파일을 만든다.
		6-2-7. 날짜가 바뀐 경우에는 이전 날짜 파일에 이어 쓰지 않고, 새 날짜 파일에 기록한다.
		6-2-8. 같은 날짜의 대화는 해당 일자 파일에 이어서 기록한다.
	6-3. 대화 기록 포맷팅 및 가독성 (중)
		6-3-1. 각 대화 항목의 제목은 사용자에 대해서는 `## 사용자 YYYY-MM-DD HH:mm:ss.000` 형태로 작성한다
		6-3-2. 실제 프로젝트나 서버에 적용한 변경과, 사용자가 검토하거나 실행해야 하는 제안은 명확히 구분한다.
		6-3-3. 제안 단계의 코드에는 실행 환경, 적용 대상 파일 또는 서버, 필요한 선행 조건을 함께 기록한다.
		6-3-4. 제안한 내용은 Markdown 코드 블록으로 원문을 보존하고, 언어 또는 형식을 지정한다. 예: `bash`, `python`, `caddyfile`
	6-4. 단순 서식 규칙 및 예외 조항 (하)
		6-4-1. 연도와 월은 각각 하위 폴더로 구분한다. 월 폴더명은 두 자리 숫자를 사용한다. 예: `07`
		6-4-2. 일자별 대화는 두 자리 숫자의 파일명으로 저장한다. 예: `23.md`
		6-4-3. 단, 사용자가 대화의 삭제나 덮어쓰기를 명시적으로 요청한 경우에는 예외로 하되, 이 경우 즉시 실행하지 말고 반드시 한 번 더 사용자에게 실행 여부(재확인)를 물어 최종 승인을 득한 후 처리해야 한다.
		6-4-4. 실제 대화 시각 정보를 보유하지 않은 기존 항목은 임의 시각을 만들지 않고, 확인 기준 시각을 포함해 `## 사용자 YYYY-MM-DD HH:mm:ss.000 이전에 기록됨` 또는 `## Codex YYYY-MM-DD HH:mm:ss.000 이전에 기록됨` 형식으로 작성했다. 그 이유는 위 규칙이 완전하게 정해지기 이전에 기록된 대화에 한정하여 적용한 구문이기 때문이다. 새로운 대화부터는 정확한 시각 정보를 포함해야 한다.

7. 문서 생명주기 및 개발 파이프라인 원칙 (Documentation & Development Pipeline)
AI(Codex, ChatGpt, Gemini, Claude 등등...)는 기능 추가 및 코드 수정 시 아래의 파이프라인 순서와 폴더/문서별 역할을 엄격히 준수해야 한다.
	7-1. [기획 및 대기] (PROPOSALS ➡️ ROADMAP)
		7-1-1. 신규 기능 제안이나 아이디어는 원본 보존 문서인 `PROPOSALS.md`에 기록함과 동시에, 현재 미구현 상태임을 추적하기 위해 `UNIMPLEMENTED_PROPOSALS.md` 에도 병기하여 적치한다.
		7-1-2. 개발이 채택/승인된 항목은 원본 보존 문서인 `ROADMAP.md`에 등록하여 히스토리를 남기고, 실제 작업 대기열 관리를 위해 `UNIMPLEMENTED_ROADMAP.md` 에도 등록하여 관리한다.
	7-2. [영구 보존 기획] (Plans 디렉토리)
		7-2-1. 기능 추가, 버그 수정, 아키텍처 개편 등의 작업 전에는 반드시 기획서(Plan)를 작성한다.
		7-2-2. 모든 기획 문서는 히스토리 보존을 위해 반드시 `Plans/YYYY-MM-DD_XXX_Plan.md` 파일 형태로 `Plans/` 디렉토리 하위에 영구 기록 및 조율한다.
	7-3. [임시 검증 및 병합] (Staging 디렉토리)
		7-3-1. 실제 운영 코드(`templates/`, `app.py` 등)를 수정하기 전 안전한 테스트(Safety Net)를 위해 `Staging/` 디렉토리를 활용한다.
		7-3-2. `Staging/` 폴더 내에 모의 소스코드(`Staging_*.html`) 등을 작성하여 검증을 진행한다.
		7-3-3. 사용자가 테스트 결과를 승인하여 실제 운영(Production) 코드에 병합(Merge)이 완료되면, `Staging/` 디렉토리 내부의 임시 파일들은 모두 삭제하여(Clear) 항상 빈 폴더 상태로 깨끗하게 유지한다.
		7-3-4. 단, [중요] 스테이징 검증을 위해 작성한 기획 문서인 `Staging_PLAN.md`는 바로 삭제하는것이 아니고, `Plans/YYYY-MM-DD_작업내용_Plan.md` 형태의 파일명으로 변경하여 `Plans/` 디렉토리로 이관 및 영구 보존(아카이빙)한다. 보존 이후에 `Staging_PLAN.md`를 삭제한다.
	7-4. [완료 및 히스토리 보존] (FEATURES, ROADMAP, PROPOSALS)
		7-4-1. 개발이 완료되더라도 `PROPOSALS.md`와 `ROADMAP.md`에서 해당 항목을 절대 삭제하지 않는다.
		7-4-2. 대신 상태(Status) 값을 `[개발 완료 (FEATURES.md 이관)]` 등으로 갱신하여 프로젝트의 기획 및 개발 히스토리를 영구 보존한다. 단, 개발이 완료된 항목은 미구현 목록 문서인 `UNIMPLEMENTED_PROPOSALS.md` 및 `UNIMPLEMENTED_ROADMAP.md` 에서 완전히 삭제(제거)하여 추적 목록을 최신화한다.
		7-4-3. 최종 완성된 상세 기능 명세만 `FEATURES.md`에 추가로 기록한다.
	7-5. 다중 AI(Codex, ChatGpt, Gemini, Claude 등등...) 간 컨텍스트 유지 및 자아 식별 원칙
		7-5-1. 사용자는 상황에 따라 기획/검토에 특화된 모델과 단순 코딩/실행에 특화된 모델 등 다양한 플랫폼의 AI(Codex, ChatGpt, Gemini, Claude 등등...)를 수시로 교차 투입할 수 있다.
		7-5-2. 7-5-1의 상황에서 AI는 자신이 기획이나 검토를 수행 중인건지 코딩이나 실행을 수행 중인건지 명확하게 인지해야한다. 만약 자신이 어떤 작업을 수행해야하는지 인지가 어렵다면 사용자에게 사용자에게 문의하여 이를 바로잡는다.
		7-5-3. 새롭게 호출된 AI(Codex, ChatGpt, Gemini, Claude 등등...)는 직전 AI가 수립하고 사용자가 승인한 기획서(`Plans/`, `Staging_PLAN.md`)와 문서들(`ROADMAP.md` 및 'UNIMPLEMENTED_ROADMAP.md' 등...)의 방향성을 완벽히 숙지하고 존중하여 맥락의 단절 없이 작업을 이어가야 한다.
		7-5-4. [중요] 대화 기록 작성 시(6-3-10 규칙 참조), 교차 투입된 AI는 이전 대화 및 작업의 기록에 남아있는 다른 AI의 이름(예: Gemini)을 무비판적으로 복사하여 사용하지 말고, 반드시 현재 대화를 수행중인 AI 자신의 실제 모델명(Codex, ChatGpt, Gemini, Claude 등등...)을 정확히 자각하여 기록 헤더(`## 모델명 YYYY-MM-DD...`)에 명시해야 한다.

8. AI Assistant 시스템 도구(API) 운용 및 제어 원칙
AI(Codex, ChatGpt, Gemini, Claude 등)가 이 프로젝트의 파일 시스템 및 터미널에 접근할 때는 시스템의 안전성과 사용자의 통제권을 보장하기 위해 다음 도구 사용 원칙을 엄격히 준수한다.
	8-0. [우선 적용 예외] 본 8조의 모든 조항은 '대화 저장(Chat 디렉토리)'에는 적용되지 않는다. 대화 저장은 제6조의 터미널 사용 규칙을 최우선으로 따른다. 본 8조는 대화 외의 나머지 모든 소스코드 및 리소스에 대한 절대 원칙이다.
	8-1. 터미널(run_command) 읽기 전용 원칙
		8-1-1. 터미널(PowerShell, Bash 등)은 파일 내용 조회, 디렉토리 탐색, 서버 구동, 상태 모니터링 등 상태를 '읽거나(Read)' '실행(Execute)'하는 목적으로만 제한하여 사용한다.
		8-1-2. 터미널 명령어(예: `Add-Content`, `echo`, `cat >` 등)를 사용하여 파일(소스, 리소스 등)을 생성하거나 수정, 덮어쓰는 행위는 절대 금지한다. (단, 8-0 조항에 의거하여 Chat 기록 예외)
	8-2. 파일 편집 API(replace_file_content 등) 쓰기 전용 원칙
		8-2-1. 소스 코드 수정, 신규 문서 생성 등 대화를 제외한 모든 쓰기(Write/Append) 작업에는 반드시 IDE 시스템에 내장된 '파일 편집 API'를 사용한다.
		8-2-2. 이를 통해 파일이 변경될 경우, 사용자가 IDE 환경에서 즉시 Diff(변경 사항)를 확인하고 필요시 쉽게 '실행 취소(Undo)'하여 이전 상태로 되돌릴 수 있는 구조를 보장해야 한다.
