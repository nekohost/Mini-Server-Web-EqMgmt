# 제안(Proposal) 관리 문서

이 문서는 AI가 프로젝트에 대해 공식적으로 제시한 설계/구현 제안들을 관리합니다.
대화 기록(`Chat/`)과는 별도로, 제안의 개요와 상세를 구조화하여 기록합니다.

# 제안은 Gemini 3.1 Pro (High) 가 제안한 아래의 추가 권장 항목 및 포맷 예시의 틀을 따라서 작성해야 합니다. 아래 예시는 참고용입니다.

### [PROPOSALS.md 추가 권장 항목]
1. **영향도 및 의존성 (Impact & Dependencies)**
   - `Rule.md`에서 가장 강조하신 부분입니다. 이 제안을 적용할 때 "기존 기능 중 어디가 영향을 받는지", "DB 스키마 변경이 있는지" 등을 명확히 기재해야 안전한 검토가 가능합니다.
2. **채택/기각 사유 (Reason for Decision)**
   - 회원님께서 이 제안을 검토하신 후 채택하셨거나 혹은 기각(보류)하셨을 때, **'왜 그런 결정을 내렸는지'**를 기록해 두는 항목입니다. 나중에 비슷한 문제가 발생했을 때 훌륭한 나침반이 됩니다.
3. **추정 난이도 / 소요 작업량 (Complexity / Effort)**
   - 당장 도입할지 나중에 도입할지 우선순위를 결정하시기 좋도록, AI가 판단한 간단한 난이도(상/중/하)를 기재해 두면 유용합니다.

**[완성된 PROPOSALS.md 포맷 예시]**
```markdown
## [제안-001] 회원탈퇴 시 데이터 Soft-Delete 처리 방안
  - **제안 일시:** 2026-08-03 16:50:00
  - **제안 모델:** Gemini 3.1 Pro (High)
  - **상태:** [대기중 / 채택 / 기각]
  - **결정 사유:** (채택/기각 시 사용자가 직접 혹은 AI를 통해 기록)
  - **관련 파일:** `app.py`, `templates/index.html`
  - **영향도 및 의존성:** DB `users` 테이블 스키마 변경 필요, 기존 로그인 로직에 탈퇴 유예 검증 로직 추가 필요.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 사용자가 회원탈퇴를 실수로 했을 때 복구 유예기간을 두기 위함.
  - **상세 내용(코드 포함):** ...
    ```

---

## [제안-001] 세션 만료(자동 로그아웃) 및 동시 로그인 제어
  - **제안 일시:** 2026-08-03 20:19:30
  - **제안 모델:** Claude Opus 4.6 (Thinking)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 인터넷에 노출되는 시스템 특성상 계정 탈취를 방어하기 위한 가장 기초적이고 필수적인 보안 장치이므로 반드시 구현해야 합니다.
  - **구현 우선순위:** [최상] 보안과 직결되므로 다른 부가 기능보다 먼저 구현되어야 합니다.
  - **구현 가능성:** [매우 높음] Flask 기본 `PERMANENT_SESSION_LIFETIME` 설정만으로 자동 로그아웃은 즉시 구현 가능합니다. 동시 로그인 제어의 경우 SQLite에 `session_tokens` 테이블을 만들어 로그인 시마다 발급/검증하면 무리 없이 구현 가능합니다.
  - **관련 파일:** `app.py`
  - **영향도 및 의존성:** Flask 세션 설정(`PERMANENT_SESSION_LIFETIME`) 추가 필요. 동시 로그인 제어 시 세션 저장소를 서버 사이드(DB 또는 Redis)로 전환하거나, `users` 테이블에 세션 토큰 컬럼 추가가 필요할 수 있음.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 현재 세션에 만료 시간 설정이 없어 한번 로그인하면 브라우저를 닫지 않는 한 사실상 영원히 유지됨. `Rule.md`에 외부(인터넷) 공개를 전제한다고 명시되어 있어, 기기 분실이나 공용 환경에서 로그아웃을 잊었을 때 계정이 무방비로 노출되는 보안 위험이 존재함.
  - **상세 내용:**
    - `app.config['PERMANENT_SESSION_LIFETIME']`을 설정하여 일정 시간(예: 30분~1시간) 후 자동 로그아웃 처리
    - 동시 로그인 차단이 필요할 경우, 로그인 시 세션 토큰을 DB에 기록하고 다른 기기에서의 기존 세션을 무효화하는 로직 추가

---

## [제안-002] 입력값 유효성 검증 강화 (프론트엔드 + 백엔드)
  - **제안 일시:** 2026-08-03 20:19:30
  - **제안 모델:** Claude Opus 4.6 (Thinking)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 완료 - 개발 예정]
  - **결정 사유:** 사용자의 악의적, 혹은 실수로 인한 잘못된 데이터 입력을 서버단에서 원천 차단하는 것은 DB 정합성과 시스템 안정성을 위해 필수적입니다.
  - **구현 우선순위:** [상] DB 스키마 무결성 유지와 직결됩니다.
  - **구현 가능성:** [매우 높음] Python 백엔드의 API 엔드포인트(`/register`, `/api/equipment`)에 간단한 정규식(Regex)과 조건문을 추가하는 것만으로 신속히 방어 로직을 구성할 수 있습니다.
  - **관련 파일:** `app.py`, `templates/register.html`, `templates/index.html`
  - **영향도 및 의존성:** 회원가입 API(`/register`), 장비 등록/수정 API(`/api/equipment`)의 서버단 로직에 유효성 검증 코드 추가 필요. 프론트엔드 폼에도 실시간 검증 피드백 UI 추가.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 회원가입 시 비밀번호 길이·특수문자 조합 등의 복잡도 검증이 없고, 아이디도 공백이나 특수문자를 포함한 어떤 문자열이든 등록 가능함. 프론트엔드의 `required` 속성만으로는 API를 직접 호출하는 공격을 막을 수 없으므로, 서버단(백엔드)에서의 유효성 검증이 최소한의 방어선으로 반드시 필요함.
  - **상세 내용:**
    - 비밀번호: 최소 길이(예: 8자), 영문+숫자+특수문자 조합 규칙 적용
    - 아이디(LoginId): 영문+숫자만 허용, 길이 제한, 공백/특수문자 차단
    - 장비 등록: 필수 필드 서버단 검증, 문자열 길이 제한
    - 프론트엔드: 입력 시 실시간 유효성 피드백 표시

---

## [제안-003] 감사 로그(Audit Log) 조회 화면
  - **제안 일시:** 2026-08-03 20:19:30
  - **제안 모델:** Claude Opus 4.6 (Thinking)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 현재 훌륭하게 적재되고 있는 `audit_logs` 데이터를 관리자가 웹 UI를 통해 쉽게 모니터링하고 추적할 수 있도록 시각화해야 로그의 가치가 발휘됩니다.
  - **구현 우선순위:** [중] 시스템 운영 및 보안 모니터링을 위해 필요합니다.
  - **구현 가능성:** [높음] 이미 데이터가 적재되고 있으므로, 새로운 HTML 템플릿(`audit_logs.html`)과 페이징(Pagination)이 적용된 단순 SELECT 쿼리 API만 작성하면 해결됩니다.
  - **관련 파일:** `app.py`, `templates/` (신규 `audit_logs.html` 생성 필요)
  - **영향도 및 의존성:** `audit_logs` 테이블 조회 API 신규 추가 필요. `menus` 테이블에 감사 로그 메뉴 등록 및 `role_menu_permissions`에 관리자 전용 권한 설정 필요. 데이터량이 많아질 수 있으므로 페이징(Paging) 처리 필수.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** `audit_logs` 테이블에 로그인 성공/실패, 장비 CRUD, 권한 변경 등 모든 기록이 훌륭하게 쌓이고 있지만, 이를 열람할 수 있는 화면(UI)이 아예 없음. 현재 DBeaver 등 외부 도구로만 확인 가능. 감사 로그를 쌓는 목적 자체가 "누가, 언제, 무엇을 했는지" 추적하기 위함인데, 관리자가 웹 UI에서 이를 조회할 수 없다면 로그 시스템의 실질적 가치가 반감됨.
  - **상세 내용:**
    - 관리자 전용 감사 로그 조회 화면 구현
    - 필터링: 날짜 범위, 행위 유형(Action), 사용자(ActorLoginId)별 검색
    - 페이징: 한 페이지당 표시 건수 제한 (예: 50건)
    - OldValue/NewValue JSON 데이터를 읽기 쉽게 포맷팅하여 표시

---

## [제안-004] 관리자용 사용자 관리 화면 (회원 목록 조회, 역할 변경)
  - **제안 일시:** 2026-08-03 20:19:30
  - **제안 모델:** Claude Opus 4.6 (Thinking)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 관리자가 사용자의 역할을 변경하거나 계정을 관리하는 기능은 다중 사용자 시스템의 가장 기초적인 관리 도구입니다.
  - **구현 우선순위:** [중] 사용자가 늘어나기 전에 관리 도구 확보 차원에서 유용합니다.
  - **구현 가능성:** [높음] `users` 테이블에 대한 간단한 CRUD 및 권한 검증 미들웨어를 조합하여 구현할 수 있어 아키텍처 변경 없이 즉각적인 구현이 가능합니다.
  - **관련 파일:** `app.py`, `templates/` (신규 `users.html` 생성 필요)
  - **영향도 및 의존성:** `users` 테이블 조회/수정 API 신규 추가 필요. `menus` 테이블에 사용자 관리 메뉴 등록 및 `role_menu_permissions`에 관리자 전용 권한 설정 필요. 역할(Role) 변경 시 해당 사용자의 메뉴 접근 권한이 즉시 변경되므로 `role_menu_permissions` 체계와 연동됨.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 관리자가 사용자를 관리할 수 있는 별도 화면이 없음. 장비 등록/수정 시 "소유자 검색" 모달을 통해 사용자를 검색할 수는 있지만, 사용자 목록을 전체적으로 열람하거나 특정 사용자의 역할(Role)을 변경하는 관리 기능이 부재. 현재 역할은 회원가입 시 자동으로 부여될 뿐, 이후 변경하려면 DB를 직접 수정해야 함.
  - **상세 내용:**
    - 전체 사용자 목록 조회 (LoginId, Name, NickName, Role, 가입일 표시)
    - 역할(Role) 변경 기능 (admin ↔ user 전환, Audit Log 기록)
    - 향후 회원탈퇴(Soft Delete) 기능과 연계하여 계정 잠금/해제 기능 확장 가능

---

## [제안-005] 대시보드 (통계/현황 요약)
  - **제안 일시:** 2026-08-03 20:19:30
  - **제안 모델:** Claude Opus 4.6 (Thinking)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 시스템 포털 화면에 통계를 표시함으로써 사용자와 관리자 모두에게 직관적이고 유용한 인사이트(현재 관리 중인 자산 규모 등)를 제공합니다.
  - **구현 우선순위:** [하] UI/UX 개선 측면이므로 필수 기능(보안/권한) 이후에 진행해도 무방합니다.
  - **구현 가능성:** [높음] SQLite의 `COUNT`, `GROUP BY` 쿼리를 활용해 집계 API를 만들고 프론트엔드에 간단한 CSS 카드 형태로 렌더링하면 됩니다. 챠트 라이브러리(Chart.js)를 추가해도 무겁지 않습니다.
  - **관련 파일:** `app.py`, `templates/portal.html`
  - **영향도 및 의존성:** 통계 데이터 집계 API 신규 추가 필요. `equipment`, `users`, `audit_logs` 테이블에 대한 집계 쿼리(COUNT, GROUP BY 등) 추가. 포털 화면(`portal.html`)의 레이아웃 변경.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 포털 화면(`portal.html`)은 단순한 메뉴 바로가기 링크만 나열하고 있어, 시스템 전반의 현황을 한눈에 파악할 수 있는 정보가 없음. 요약 정보가 표시된다면 관리자와 사용자 모두에게 시스템의 활용도와 상태를 직관적으로 제공할 수 있음.
  - **상세 내용:**
    - 전체 등록 장비 수 / 나의 장비 수
    - 오늘 등록된 장비 건수
    - 카테고리별 장비 분포 (간단한 막대 또는 원형 차트)
    - 최근 로그인 기록 (관리자용)
    - 전체 등록 사용자 수 (관리자용)

---

## [제안-006] 알림 및 공지사항 시스템
  - **제안 일시:** 2026-08-03 20:19:30
  - **제안 모델:** Claude Opus 4.6 (Thinking)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [보류 권장]
  - **결정 사유:** 미니서버(라즈베리파이 등) 기반의 개인/소규모 팀 자산 관리 용도에서는 시스템 내장 공지사항보다 직접적인 커뮤니케이션(메신저 등)이 더 빠르고 효율적일 수 있습니다. 시스템이 불필요하게 무거워질 우려가 있습니다.
  - **관련 파일:** `app.py`, `templates/portal.html`, `templates/` (신규 `notices.html` 생성 필요)
  - **영향도 및 의존성:** DB에 `notices` 테이블 신규 생성 필요 (`init_db()` 수정). 공지사항 CRUD API 추가. 포털 화면 상단에 공지 배너 영역 추가. `menus` 테이블에 공지사항 관리 메뉴 등록.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 관리자가 사용자들에게 시스템 점검 안내, 업데이트 내역, 중요 공지 등을 전달할 수 있는 수단이 전혀 없음. 서비스 운영의 기본적인 커뮤니케이션 채널로서, 간단한 공지사항 CRUD와 포털 상단 배너 노출 정도만으로도 운영 편의성이 크게 올라감.
  - **상세 내용:**
    - `notices` 테이블: NoticeId, Title, Content, AuthorId, IsActive, CreatedAt, UpdatedAt
    - 관리자: 공지사항 작성/수정/삭제 (CRUD)
    - 사용자: 포털 진입 시 활성화된 공지사항 배너 자동 표시
    - 공지 확인(읽음) 처리 여부는 선택적으로 구현

---

## [제안-007] API 요청 속도 제한 (Rate Limiting) 및 브루트포스 방어
  - **제안 일시:** 2026-08-03 20:19:30 (수정: 2026-08-04 보안 로그 검토 반영)
  - **제안 모델:** Claude Opus 4.6 (Thinking)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 완료 - 우선순위 격상 및 개발 예정]
  - **결정 사유:** 2026년 8월 4일 실제 서버 액세스 로그에서 해외 IP(봇넷)들의 무작위 취약점 스캔(xmlrpc, onvif 등) 및 로그인 페이지 접근(`GET /login 200 OK`)이 관측되었습니다. 이는 곧 봇(Bot)들에 의한 아이디/비밀번호 무작위 대입(Brute-force) 공격으로 이어질 가능성이 매우 높으므로, 방어 로직 구현이 시급해졌습니다.
  - **구현 우선순위:** [최상] 실제 공격 징후(정찰)가 포착되었으므로 기능 개발보다 우선되어야 합니다.
  - **구현 가능성:** [중~높음] Python 메모리 딕셔너리 기반 자체 Rate Limiter 구현 또는 `flask-limiter` 도입.
  - **관련 파일:** `app.py`
  - **영향도 및 의존성:** 로그인 API(`/login`)에 속도 제한 미들웨어 추가. 실패 횟수에 따른 429 Too Many Requests 응답 반환 로직.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 외부(인터넷) 공개 환경에서 봇들의 자동화된 공격으로부터 관리자 계정을 보호하기 위한 1차 방어선.
  - **상세 내용:**
    - 1차 방어 (애플리케이션 계층): 동일 IP에서 1분 내 로그인 5회 실패 시 15분간 해당 IP의 로그인 시도 차단 (In-memory 기반).
    - 2차 방어 (향후 인프라 계층 조치 - 기억용): `fail2ban` 설정이나 Nginx 방화벽(WAF) 룰을 통해 지속적으로 404/403을 유발하는 악성 봇넷 IP를 OS 커널(iptables) 단에서 원천 차단하는 방안 도입 검토.

---

## [제안-008] 장비 보증기간(A/S) 및 교체 주기 추적 (D-Day 카운트다운)
  - **제안 일시:** 2026-08-03 22:21:30
  - **제안 모델:** Gemini 3.6 Flash (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 완료 - 개발 예정]
  - **결정 사유:** 자산 관리의 핵심 중 하나인 "유지보수 및 교체 기간"을 추적할 수 있어 시스템의 실용성을 크게 높여줍니다.
  - **구현 우선순위:** [중] 장비 데이터의 품질과 관리 편의성 향상을 위해 유용합니다.
  - **구현 가능성:** [높음] `equipment` 테이블에 `WarrantyDate` 컬럼을 추가하고, 프론트엔드에서 현재 날짜와 비교하는 JavaScript(D-Day 계산) 로직만 추가하면 됩니다.
  - **관련 파일:** `app.py`, `templates/index.html`, `templates/portal.html`
  - **영향도 및 의존성:** DB `equipment` 테이블에 `WarrantyDate` 컬럼 추가 필요 (`init_db()` 및 마이그레이션 확장). 프론트엔드 장비 카드 및 포털 화면에 D-Day 계산 JS 및 태그 UI 연동 필요.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 구입일자만 기록하고 있어 보증기간이나 교체 주기 관리가 어려움. 무상 A/S 기간 내 점검/교체 기회를 놓치지 않도록 관리 편의성을 향상함.
  - **상세 내용:**
    - `equipment` 테이블에 `WarrantyDate TEXT` 컬럼 추가
    - 장비 목록/카드에서 `PurchaseDate` 및 `WarrantyDate` 기반 남은 D-Day 계산 및 뱃지(Badge) 표시 (예: A/S 만료 D-30)
    - **(추가 확장) 매일 자정, 보증기간 만료 임박(예: 7일) 장비 리스트를 담당자 이메일로 자동 리포팅하는 백그라운드 스케줄러 기능 포함.**

---

## [제안-009] 장비 QR코드 / 바코드 생성 및 모바일 스캔 기능
  - **제안 일시:** 2026-08-03 22:21:30
  - **제안 모델:** Gemini 3.6 Flash (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [보류 권장]
  - **결정 사유:** 실물 관리 측면에서 매우 매력적이나, 브라우저에서 카메라를 제어하여 바코드를 스캔하려면 모바일 UI 최적화와 HTTPS 보안 설정(카메라 접근 필수)이 강제되며 라벨 프린터와의 연동 등 외부 허들이 많아 현 단계에서는 오버스펙으로 판단됩니다.
  - **관련 파일:** `app.py`, `templates/index.html`
  - **영향도 및 의존성:** QR코드 생성 라이브러리(jsqrcode 또는 qrcode.js CDN) 도입 필요. 모바일 카메라 스캔용 라이브러리(html5-qrcode CDN) 연동 필요.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 장비 검색 시 시리얼 넘버나 이름을 직접 입력하는 검색 단계를 단축하고, 실물 장비에 부착된 스티커 스캔으로 빠른 상세 조회를 지원하기 위함.
  - **상세 내용:**
    - 장비별 unique URL 또는 ID가 포함된 QR코드 이미지 생성 및 모달 출력 (인쇄 기능 지원)
    - 스마트폰 카메라를 이용해 QR코드를 스캔하면 해당 장비의 상세 페이지로 즉시 이동

---

## [제안-010] 장비 이미지 및 문서 첨부 기능 (영수증, 사진, 매뉴얼)
  - **제안 일시:** 2026-08-03 22:21:30
  - **제안 모델:** Gemini 3.6 Flash (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 완료 - 개발 예정]
  - **결정 사유:** 영수증(증빙자료)과 매뉴얼(사용법) 보관은 자산 관리의 필수 요소입니다. 텍스트만으로는 상세 정보나 회로도 등을 보관하기 어렵습니다.
  - **구현 우선순위:** [상] 사용자 편의성과 자산 관리의 완결성을 위해 우선순위가 높습니다.
  - **구현 가능성:** [중] Flask의 파일 업로드 처리(`secure_filename`), 미니서버의 디스크 보호를 위한 용량 및 확장자 제한 로직, 그리고 `equipment_files`라는 새로운 연결 테이블 작성이 필요하지만 프레임워크 내에서 표준적으로 지원하는 기능입니다.
  - **관련 파일:** `app.py`, `templates/index.html`, `static/uploads/` (신규 디렉토리)
  - **영향도 및 의존성:** `equipment_files` 테이블 신규 생성 필요 (`init_db()`). 파일 업로드 API 구현 및 파일 저장 디렉토리 관리. 미니서버 용량 보호를 위해 파일 크기(예: 5MB) 및 확장자 제한 적용.
  - **추정 난이도:** 상 (High)
  - **제안 배경 (개요):** 구입 영수증(PDF/이미지), 제품 실물 사진, 보증서, 사용 설명서 링크 등을 장비 데이터와 함께 보관하여 완전한 통합 자산 보관소 역할을 수행하기 위함.
  - **상세 내용:**
    - 장비 등록/수정 모달에 파일 첨부 UI 추가
    - 영수증 이미지 preview 및 클릭 시 확대/다운로드 기능 제공

---

## [제안-011-고도화] 동적 마스터 데이터 관계형 매핑 및 다국어 지원 (고도화)
  - **제안 일시:** 2026-08-11 10:20:00 (기존 제안 수정)
  - **제안 모델:** Gemini 3.1 Pro (High)
  - **검토 모델:** 사용자 직접 승인 완료
  - **상태:** [채택 / 완료]
  - **결정 사유:** 기존 텍스트 기반 저장 방식의 한계를 넘어, 고유 ID(Key) 기반의 관계형 구조(RDBMS)로 전환하여 데이터 정합성을 극대화합니다. 또한 카테고리/제조사명에 다국어(한국어, 영어) 컬럼을 추가하여 확장성을 확보하고, 중복 항목 통폐합(Merge) 시 시스템 부하를 최소화합니다.
  - **구현 우선순위:** [최상] 관계형 구조로의 전환은 전체 장비 데이터 쿼리에 영향을 주므로 가장 먼저 선행되어야 합니다.
  - **구현 가능성:** [보통] 기존 `equipment` 테이블에 `CategoryId`, `ManufacturerId` 컬럼을 신설하고 데이터 마이그레이션을 수행해야 합니다. UI/UX도 텍스트가 아닌 ID 기반으로 모두 수정되어야 하므로 작업 범위가 넓습니다.
  - **관련 파일:** `app.py`, `templates/index.html`, `templates/master_management.html` (신규)
  - **영향도 및 의존성:** `init_db()` 스키마 변경, `equipment` 마이그레이션, `GET /api/equipment` 조인 쿼리 변경 등.
  - **추정 난이도:** 상 (High)
  - **제안 배경 (개요):** 단순 텍스트 표기 오류 방지를 넘어 완전한 관계형 매핑과 다국어 지원, 그리고 중복 카테고리의 원클릭 통합(Merge) 기능을 지원하기 위함.
  - **상세 내용:**
    1. `categories`, `manufacturers` 테이블에 고유키(Key)를 유지하고, `NameKo`, `NameEn` 다국어 컬럼 추가 (비워둘 수 있음).
    2. 기존 `equipment` 테이블의 카테고리/제조사를 1에서 생성한 테이블의 고유 Key(ID)로 외래키 매핑(Migration).
    3. 프론트엔드 UI 및 API에서 장비 리스트 출력 및 수정 시, 해당 Key를 바탕으로 연결된 이름을 조회하여 출력.
    4. 관리자 전용 화면(`master_management.html`) 신설: 카테고리/제조사 이름 수정, 두 개의 항목을 하나로 통합(Merge)하여 장비의 Key를 일괄 업데이트하는 기능.

---

## [제안-012] 다중 사용자 지정 태그 (Custom Tagging) 시스템
  - **제안 일시:** 2026-08-03 22:21:30
  - **제안 모델:** Gemini 3.6 Flash (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [보류 권장]
  - **결정 사유:** 다중 태그 시스템은 다대다(N:M) 매핑 테이블이 필요하며, 검색 로직과 UI(태그 추가/삭제 칩) 구현이 복잡해집니다. [제안-011]의 카테고리 마스터화 만으로도 소규모 환경에서는 충분히 대응 가능할 것으로 판단됩니다.
  - **관련 파일:** `app.py`, `templates/index.html`
  - **영향도 및 의존성:** `tags`, `equipment_tags` 테이블 신규 생성 필요 (`init_db()`). 태그 검색/필터링 API 구현.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 단일 카테고리 분류의 한계를 넘어 `#업무용`, `#휴대용`, `#개발용`, `#고가자산` 등 유연한 다중 태그 기반의 자산 관리를 지원하기 위함.
  - **상세 내용:**
    - 장비 등록/수정 시 해시태그 형태의 다중 태그 추가/삭제 UI 구현
    - 장비 목록 및 카드에서 태그 클릭 시 해당 태그가 달린 장비 목록만 필터링 출력

---

## [제안-013] 웹 기반 DB 데이터 백업 및 복원 (Administrator Backup & Restore)
  - **제안 일시:** 2026-08-03 22:21:30
  - **제안 모델:** Gemini 3.6 Flash (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 완료 - 개발 예정]
  - **결정 사유:** 물리적인 미니서버(특히 SD카드 기반) 손상 시 모든 자산 데이터가 증발하는 치명적인 사태를 방지하는 가장 훌륭하고 단순한 재난 복구 수단입니다.
  - **구현 우선순위:** [최상] 시스템 데이터의 영속성 및 생존과 직결된 핵심 기능입니다.
  - **구현 가능성:** [매우 높음] SQLite DB가 단일 파일(`equipment.db`)이라는 점을 십분 활용하여, 관리자가 해당 파일을 다운로드(send_file)하거나 새 파일로 덮어쓰기(업로드)하는 매우 단순한 API만으로 백업/복원 기능을 완성할 수 있습니다.
  - **관련 파일:** `app.py`, `templates/portal.html`
  - **영향도 및 의존성:** 백업 API(`/api/admin/backup`) 및 복원 API(`/api/admin/restore`) 추가. 관리자 전용 권한 검증 필수. 복원 시 DB 커넥션 재설정 및 복구 로직 필요.
  - **추정 난이도:** 상 (High)
  - **제안 배경 (개요):** 미니서버의 SD카드/디스크 장애나 실수로 인한 데이터 손실에 대비하여, 관리자가 클릭 한 번으로 DB 백업본을 수신받고 복원할 수 있는 재난 복구(DR) 수단을 제공하기 위함.
  - **상세 내용:**
    - 관리자 전용 DB 덤프/다운로드 기능 (ZIP/DB 파일 다운로드)
    - 백업 파일 업로드를 통한 DB 롤백/복원 기능 (복원 전 자동 백업 생성 안전장치 포함)

---

## [제안-014] 다크 모드(Dark Mode) 지원 및 사용자 UI 테마 설정
  - **제안 일시:** 2026-08-03 22:21:30
  - **제안 모델:** Gemini 3.6 Flash (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 최근 웹 환경의 표준 편의 기능이며, 다크 모드를 선호하는 사용자들의 눈 피로도를 낮추고 시스템의 미적 만족도(Premium UX)를 높여줍니다.
  - **구현 우선순위:** [하] 핵심 비즈니스 로직이 아니므로 여유가 있을 때 진행합니다.
  - **구현 가능성:** [높음] Tailwind CSS를 이미 사용 중이므로, HTML 최상단 태그에 `dark` 클래스를 토글하고 브라우저 `localStorage`에 상태를 저장하는 자바스크립트 코드 몇 줄로 손쉽게 구현할 수 있습니다.
  - **관련 파일:** `templates/` 전 화면 HTML
  - **영향도 및 의존성:** Tailwind CSS 다크 모드 클래스(`dark:`) 전면 적용. 사용자 테마 설정(라이트/다크/시스템) 저장 로직(localStorage 또는 session) 구현.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 야간이나 어두운 조명 환경에서의 눈 피로도를 낮추고 사용자 선호에 맞는 테마를 제공하기 위함.
  - **상세 내용:**
    - 상단 내비게이션 바에 라이트/다크 모드 토글 스위치 제공
    - 브라우저/OS의 `prefers-color-scheme` 연동 및 선택된 테마 설정 유지

---

## [제안-015] 동적 메뉴 렌더링(Dynamic Menu Routing) 아키텍처
  - **제안 일시:** 2026-08-03 23:52:27
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 메뉴가 추가될 때마다 프론트엔드 HTML(`portal.html`)을 일일이 수정해야 하는 수동적인 구조를 탈피하고, 관리 유지보수의 효율을 극대화하기 위한 완벽한 통찰입니다. 시스템 확장을 위해 반드시 갖추어야 할 아키텍처입니다.
  - **구현 우선순위:** [최상] (다행히도 현재 설계에 이미 선반영되어 동작 중입니다!)
  - **구현 가능성:** [매우 높음] 백엔드에서 사용자 역할(Role) 기반으로 허용된 메뉴 목록만 API로 내려주고, 프론트엔드에서는 JS를 통해 해당 목록을 반복문으로 렌더링하는 방식으로 구현할 수 있습니다.
  - **관련 파일:** `app.py`, `templates/portal.html`
  - **영향도 및 의존성:** 프론트엔드의 메인 포털은 완벽히 API(`/api/portal/menus`)에 의존성을 가집니다. 신규 메뉴(예: 감사 로그) 추가 시 DB에 Insert만 수행하면 화면에 즉시 나타납니다.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 메뉴 확장성 확보 및 코드 수정 없는 자동화된 메뉴 리스트 연동 체계 구축.
  - **상세 내용:**
    - 하드코딩된 메뉴를 제거하고, 메뉴 데이터를 DB(`menus`, `role_menu_permissions` 테이블)에서 동적으로 불러와 렌더링.
    - 메뉴 신설 또는 권한 변경 시 재배포(HTML 수정) 없이 시스템 즉시 반영.

---

## [제안-016] 사용자 맞춤 설정(User Preferences) DB 테이블 분리 및 UI 상태 영속화
  - **제안 일시:** 2026-08-04 00:18:51
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 회원의 핵심 보안 정보(ID, 비밀번호 등)가 담긴 `users` 테이블과 단순 편의성 UI 설정(체크박스 상태, 테마 등)을 분리하는 것은 데이터베이스 아키텍처 정규화 측면에서 **가장 완벽한 모범 설계(Best Practice)**입니다. 사용자 편의성과 시스템 확장성을 모두 잡은 훌륭한 제안입니다.
  - **구현 우선순위:** [중] (다크 모드 및 장비 목록 고도화 기능과 함께 묶어서 개발)
  - **구현 가능성:** [매우 높음] `users` 테이블과 1:1 관계를 가지는 `user_settings` 테이블을 신설하고, 설정 데이터를 JSON 형태로 유연하게 저장(`{"show_my_equip_first": true, "theme": "dark"}`)하도록 API를 구성하면 깔끔하게 구현됩니다.
  - **관련 파일:** `app.py` (API 및 DB 스키마), `templates/index.html` (체크박스 UI)
  - **영향도 및 의존성:** 기존 `users` 테이블을 건드리지 않고 독립적으로 확장되므로 무중단 배포 및 확장에 매우 안전합니다. [제안-014] 다크 모드 기능과 강력하게 연계됩니다.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** "내 장비 최상단 보기" 등 사용자 개인의 UI 선택값을 브라우저를 껐다 켜거나 다른 기기에서 접속해도 동일하게 유지하기 위함.
  - **상세 내용:**
    - `user_settings` 별도 테이블 신설 (컬럼: UserId, PreferencesJSON, UpdatedAt)
    - `index.html`의 "내 장비 최상단 보기" 체크박스 변경 시 비동기로 API를 호출하여 상태 자동 저장.
    - 로그인 성공 직후 해당 설정값을 불러와 UI(체크박스 및 목록 정렬)를 자동 복원.
    - "다크 모드" 설정 등 추후 생겨날 모든 개인화 옵션을 이 테이블 한 곳에서 통합 관리.

---

## [제안-017] 감사 로그 컬럼별 조건 검색, 페이징 및 페이지 당 보기 개수 영속화
  - **제안 일시:** 2026-08-04 10:26:46
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.6 Flash (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 대용량 보안 로그 탐색 효율성과 보안 추적 성능을 대폭 향상시키는 엔터프라이즈급 모니터링 필수 제안. 긍정적/부정적 요소를 종합 검토한 결과 기술적 구현 타당성이 매우 높음.
  - **구현 우선순위:** [중상]
  - **구현 가능성:** [매우 높음] 백엔드 `/api/audit_logs` REST API 신설(Whitelist 안전 파라미터화 쿼리) 및 `user_settings` JSON 연동으로 완벽히 구현 가능.
  - **관련 파일:** `app.py`, `templates/audit_logs.html`, `static/js/session_timer.js`
  - **영향도 및 의존성:** `audit_logs.html` 동적 JS 렌더링 구조로 전환, `user_settings` 스키마에 `audit_log_page_size` 키 추가.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 고정 200개 최신 로그만 표출되던 제한을 해제하고, 컬럼별 조건 검색 및 페이징, 사용자 맞춤 개수 보기를 영속화하기 위함.
  - **상세 내용:**
    - 컬럼별 필터링 (행위자, 일시, IP, 대상 등) 및 Match Mode (완전 일치 / LIKE 부분 검색) 조건 제공.
    - 전역 페이징(Page Navigation) 도입.
    - 한 페이지 당 표출 개수 선택 (50, 100, 150, 200, 300, 500 및 기타/사용자 직접 입력 옵션).
    - 해당 표출 개수 설정값의 `user_settings` DB 저장 및 자동 복원.
## [제안-018] 세션 강제 만료 3단계 세분화 (본인 제외/본인 포함/선택 유저)
  - **제안 일시:** 2026-08-04 12:13:00
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 비상 상황(보안 패치, 해킹 의심 등) 발생 시 관리자가 즉각적으로 특정 사용자 또는 전체 사용자의 세션을 강제 종료시킬 수 있는 강력한 통제 수단이 필요함.
  - **구현 우선순위:** [상] 보안 통제권 확보 차원에서 핵심적인 기능입니다.
  - **구현 가능성:** [매우 높음] `/api/system/force_logout` API를 신설하여 `users` 테이블의 `SessionToken`을 새로운 난수로 갱신하는 방식으로 즉시 차단 가능.
  - **관련 파일:** `app.py`, `templates/users_management.html`
  - **영향도 및 의존성:** 사용자 관리 화면에 다중 선택 체크박스 및 일괄 제어 UI 추가 필요.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 서버가 재시작되어도 세션이 유지되는 아키텍처 특성상, 능동적으로 세션을 파기할 비상 스위치(Panic Button)가 부재함.
  - **상세 내용:**
    - 전체 만료 (본인 제외): 현재 접속 중인 관리자 세션만 살리고 나머지 토큰 일괄 갱신.
    - 전체 만료 (본인 포함): 모든 유저 토큰 일괄 갱신.
    - 선택 유저 만료: 사용자 관리 화면에서 체크박스로 선택한 유저들의 토큰만 갱신.

---

## [제안-019] 사용자 맞춤형 레이아웃 스킨(Skin) 시스템 (Edge vs Standard)
  - **제안 일시:** 2026-08-04 12:13:00
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 완료 - 개발 예정]
  - **결정 사유:** 서로 다른 기기(모바일/데스크탑) 환경에서 최적화된 UI를 사용자 입맛에 맞게 선택하여 활용할 수 있어 UX 만족도가 극대화됨.
  - **구현 우선순위:** [중] UI 파편화를 해결하면서도 사용자 편의성을 챙길 수 있습니다.
  - **구현 가능성:** [매우 높음] 이미 구현된 `user_settings` 인프라를 재활용하여 `layout_skin` 값을 저장/불러오고, Python 서버 렌더링 시 조건부로 HTML을 분기하면 됨.
  - **관련 파일:** `app.py`, `templates/audit_logs_standard.html`, `templates/audit_logs_edge.html`
  - **영향도 및 의존성:** 기존 `audit_logs.html`을 `_edge`와 `_standard`로 분할 관리. `user_settings` 스키마 확장.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 독립적으로 개발된 감사 로그 화면이 데스크탑 표준 UI와 달랐으나, 해당 레이아웃이 모바일 환경에서 매우 우수하다는 점에 착안하여 버리지 않고 "스킨" 형태로 제공하기로 결정함.
  - **상세 내용:**
    - Standard 스킨: 데스크탑 통일성 유지 (플로팅 헤더, 둥근 모서리 컨테이너).
    - Edge 스킨: 모바일 공간 활용 극대화 (Edge-to-Edge 내비게이션).
    - UI 상단에 [스킨 전환] 버튼을 두어 원클릭으로 변경 및 상태 영속화.

---

## [제안-020] Context-aware 스마트 동적 뒤로가기 네비게이션 UX 개선
  - **제안 일시:** 2026-08-04 14:47:00
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.6 Flash (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 메뉴간 이동 및 서브 유틸리티(마이페이지 등) 진입 시 직전 작업 메뉴로 복귀하지 못하고 무조건 포털로 튕겨 나가는 부자연스러운 UX 이동 동선을 개선하여 사용자 경험 및 작업 연속성을 보장함.
  - **구현 우선순위:** [상] 레이아웃 프레임의 완성도 및 UX 일관성과 직결됩니다.
  - **구현 가능성:** [매우 높음] `miniserver_frame.html` 및 `session_timer.js` 단에서 `document.referrer`와 `window.history.back()`을 연동한 스마트 뒤로가기 컴포넌트 추가만으로 신속하게 구현 가능.
  - **관련 파일:** `templates/miniserver_frame.html`
  - **영향도 및 의존성:** `miniserver_frame.html` 상단 네비게이션 바의 뒤로가기 버튼 영역 (`#smart-back-btn`) 변경. 기존 포털 직접 진입 케이스는 그대로 `/portal` 유지.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 포털 ➡️ 대시보드 ➡️ 마이페이지 순서로 이동한 뒤 뒤로가기를 누르면 대시보드로 돌아가는 것이 자연스러우나, 현재는 무조건 포털로 이동하는 UX 결함이 확인됨.
  - **상세 내용:**
    - 직전 페이지가 동일 도메인 내 서브 메뉴인 경우: "← 이전으로" 표출 및 `history.back()` 실행
    - 직전 페이지가 없거나 포털인 경우: "← 포털로" 표출 및 `/portal` 이동

---

## [제안-021] 관리자 전용 사용자 계정 관리 기능 (완전 파기 및 비활성화/활성화 토글)
  - **제안 일시:** 2026-08-04 15:10:00 (2026-08-11 기능 확장)
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.6 Flash (High)
  - **상태:** [개발 완료 (기능 확장 중)]
  - **결정 사유:** 관리자가 유예기간 없이 특정/선택 계정을 즉시 영구 삭제하는 기능에 더하여, 관리자 임의로 계정을 무기한 비활성화(정지) 또는 재활성화할 수 있는 토글 및 체크박스 일괄 제어 기능 확장.
  - **구현 우선순위:** [상] 사용자 관리 화면의 완전성 및 통제권 확보 차원.
  - **구현 가능성:** [매우 높음] `/api/users/delete_selected`, `/api/users/<id>/toggle_deactivation`, `/api/users/deactivate_selected` API 연동.
  - **관련 파일:** `app.py`, `templates/users_management.html`
  - **영향도 및 의존성:** `users_management.html`에 일괄 삭제/일괄 비활성화/일괄 활성화 버튼 및 상태 뱃지, 행별 개별 관리 액션 배치.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 일반 자진 탈퇴(Soft Delete 및 30일/1년 라이프사이클)와는 구별되는 관리자 전용 계정 정지(DeactivatedAt = NULL) 및 계정 즉시 파기 기능.
  - **상세 내용:**
    - 사용자 관리 화면에서 선택 유저 일괄 비활성화/활성화/즉시 삭제.
    - 테이블 각 행에 상태 뱃지 및 PW 초기화/비활성화 토글/계정 삭제 액션 배치.
    - 계정 비활성화/삭제 시 관련 사용자 세션 자동 강제 만료.

---

## [제안-022] 장비 상태(Status) 및 이력 관리
  - **제안 일시:** 2026-08-03 10:00:00 (초기 기본 로드맵 이관)
  - **제안자:** 초기 프로젝트 기본 설계
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 단순한 등록/조회를 넘어 자산의 실제 라이프사이클(정상, 대여중, 수리중, 폐기 등)을 관리하는 것은 자산 관리 시스템의 핵심이므로 가장 먼저 구현되어야 합니다.
  - **구현 우선순위:** [최상]
  - **구현 가능성:** [높음] `equipment` 테이블에 상태 컬럼을 추가하고, 상태 변경 시 로그를 기록하는 `equipment_history` 테이블을 신설하여 구현 가능합니다.
  - **관련 파일:** `app.py`, `templates/index.html`
  - **영향도 및 의존성:** `equipment` DB 스키마에 `Status` 컬럼 추가 필요, `equipment_history` 이력 테이블 신설 필요.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 자산 관리 시스템의 핵심 가치는 장비의 현재 상태 및 생애주기 변경 이력을 투명하게 추적하는 것에 있음.
  - **상세 내용:** 장비의 현재 상태(정상, 대여중, 수리중, 폐기 등) 추적 및 상태 변경 이력 개별 로그 기록 기능.

---

## [제안-023] 상세 검색 및 필터링 기능 (API 단)
  - **제안 일시:** 2026-08-03 10:00:00 (초기 기본 로드맵 이관)
  - **제안자:** 초기 프로젝트 기본 설계
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 장비 데이터가 누적됨에 따라 원하는 장비를 빠르게 찾기 위한 필수 기능이므로, 상태 관리 도입 이후 최우선적으로 구현합니다.
  - **구현 우선순위:** [상] 데이터 활용도를 위해 필수적입니다.
  - **구현 가능성:** [높음] 백엔드 API에서 쿼리 파라미터를 받아 동적 쿼리를 생성하고, 프론트엔드 UI(체크박스/셀렉트박스 등)와 연동하여 무난히 구현 가능합니다.
  - **관련 파일:** `app.py`, `templates/index.html`
  - **영향도 및 의존성:** 백엔드 장비 목록 API(`/api/equipment`) 쿼리 파라미터 확장 필요.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 대량 자산 조회의 검색 편의성을 극대화하기 위함.
  - **상세 내용:** 카테고리, 제조사, 상태, 구입일자 등 다양한 조건으로 장비를 다중 검색하고 필터링하는 기능.

---

## [제안-024] 마이페이지(회원정보 수정) 및 로그인 화면 비밀번호 찾기 기능
  - **제안 일시:** 2026-08-03 10:00:00 (초기 기본 로드맵 이관)
  - **제안자:** 초기 프로젝트 기본 설계
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 시스템 보안 및 기본 사용자 편의성을 위해 필수이나, 현재는 사용자 규모가 작아 관리자가 수동 지원할 수 있으므로 '중'으로 책정합니다.
  - **구현 우선순위:** [중] 계정 시스템 고도화 단계에서 필요.
  - **구현 가능성:** [중~높음] 마이페이지 프로필/비밀번호 변경 및 계정 관리 API 구현 완료.
  - **관련 파일:** `app.py`, `templates/mypage.html`
  - **영향도 및 의존성:** 사용자 정보 수정 API (`/api/users/update`), 마이페이지 화면 연동 완료.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 사용자 개인 정보 관리 및 비밀번호 변경 UI 제공.
  - **상세 내용:** 로그인한 사용자가 자신의 프로필을 조회/수정하고 비밀번호를 변경할 수 있는 기능.

---

## [제안-025] 회원탈퇴 및 철회(유예 기간) 기능 (Soft Delete)
  - **제안 일시:** 2026-08-03 10:00:00 (초기 기본 로드맵 이관, 2026-08-11 상세 기획 반영)
  - **제안자:** 초기 프로젝트 기본 설계
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 사용자의 실수 방지와 개인정보 보호 측면에서 훌륭한 기능이며, 4단계 라이프사이클(30일 비활성화 유예 -> 30일 경과 로그인 불가 -> 1년 1일 영구 파기 -> 재가입 시 보안 검증 복구) 및 지연 평가(Lazy Evaluation) 기반으로 시스템 부하 없이 완벽 구현합니다.
  - **구현 우선순위:** [중]
  - **구현 가능성:** [높음] `users` 스키마 확장(`IsDeactivated`, `DeactivatedAt`, `IsDeleted`, `DeletedAt`), 세션 파기 연동, 전용 안내 페이지 `/deactivated_notice` 및 재가입 시 소유권 확인(Name/PW 검증) 복구 로직.
  - **관련 파일:** `app.py`, `templates/mypage.html`, `templates/deactivated_notice.html` (신규), `templates/register.html`, `templates/users_management.html`
  - **영향도 및 의존성:** `users` 스키마 확장, `@login_required` 비활성화 사용자 샌드박싱, 재가입 API 복구 분기.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 사용자 계정 오삭제 방지 및 유예기간 제공.
  - **상세 내용:**
    1. Phase 1 (탈퇴 신청 ~ 30일 유예): `IsDeactivated='Y'`, `DeactivatedAt=현재`, 타 기기 세션 즉시 파기. `/deactivated_notice` 랜딩 (잔여 D-Day 및 철회 버튼).
    2. Phase 2 (30일 경과 ~ 1년 미만): `IsDeleted='Y'`, `DeletedAt=현재`. 로그인 시 일반 실패 처리. 지연 평가 방식으로 전이.
    3. Phase 3 (1년 1일 경과): DB 영구 삭제 (Hard Delete) 및 소유 장비 `UserId = NULL, IsPublic = 1` 이관 (`[제안-021]` 연동).
    4. Phase 4 (재가입 복구): 탈퇴 완료 계정 ID로 신규 가입 시 본인 확인(기존 이름/비번 검증) 후 새 비번으로 계정 부활.

---

## [제안-026] 엑셀(CSV) 데이터 일괄 등록 및 다운로드
  - **제안 일시:** 2026-08-03 10:00:00 (초기 기본 로드맵 이관)
  - **제안자:** 초기 프로젝트 기본 설계
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 대규모 장비 관리에 유용하지만, 개인 목적의 소규모 운영 환경에서는 당장 실사용 빈도가 높지 않으므로 도입을 잠시 미룹니다.
  - **구현 우선순위:** [하]
  - **구현 가능성:** [중] Python 내장 `csv` 모듈 또는 라이브러리를 통한 파일 I/O 파싱 및 DB 벌크 인서트 트랜잭션.
  - **관련 파일:** `app.py`, `templates/index.html`
  - **영향도 및 의존성:** CSV 파일 업로드/다운로드 백엔드 API 신설.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 대량 장비 데이터 일괄 등록 및 엑셀 내보내기 지원.
  - **상세 내용:** 엑셀/CSV 파일을 통해 다수의 장비를 한 번에 업로드(Import)하거나 현재 등록된 장비 목록을 파일로 다운로드(Export)하는 기능.

---

## [제안-027] 마스터 데이터 추가 전자결재(승인) 시스템 (Category/Manufacturer Request Workflow)
  - **제안 일시:** 2026-08-10 22:00:00
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** [제안-011]로 분리된 마스터 데이터에 사용자가 신규 카테고리나 제조사를 임의로 추가하지 못하도록 막으면서도 유연성을 제공하기 위해, 전자결재(Work-flow) 형태의 승인 프로세스를 도입합니다. 이는 향후 장비 폐기/수리 결재 등 타 업무로 확장 가능한 강력한 인프라가 됩니다.
  - **구현 우선순위:** [상] [제안-011] 구현 직후 곧바로 이어져야 할 필수 후속 기능입니다.
  - **구현 가능성:** [중~높음] `approval_requests` 테이블을 신설하여 전자문서 형태(요청자, 요청 내용, 결재선, 상태)를 정의하고 관리자 승인/반려 대시보드를 구축합니다.
  - **관련 파일:** `app.py`, `templates/approval_requests.html` (신설)
  - **영향도 및 의존성:** `[제안-011]`에 강력하게 의존함. 장비 등록 로직 수정(임시 상태 등록 허용 로직).
  - **추정 난이도:** 상 (High) - 상태 변화(대기->승인/반려)에 따른 알림 연동 및 DB 트리거 제어.
  - **제안 배경 (개요):** 단순 드롭다운 적용만으로는 사용자가 원하는 카테고리가 없을 때 발생하는 병목 현상을 해결할 수 없어, 자유도를 보장하면서도 관리자 통제를 유지하는 결재 시스템이 요구됨.
  - **상세 내용:**
    1. 셀렉트 박스에서 '기타' 선택 시 수동 입력란 활성화.
    2. 수동 입력된 데이터는 즉시 반영되지 않고 전자문서로 결재 요청됨 (장비는 임시 가등록).
    3. 관리자가 결재 대시보드에서 내용(요청자, 결재선) 확인 후 승인 또는 반려(사유 포함).
    4. 승인 시 `categories` 정식 등록 및 장비 동기화.
    5. **(추가 확장) 결재 상신, 승인, 반려 발생 시 관련자(관리자, 요청자)에게 즉각 이메일 알림 발송 기능 포함.**

---

## [제안-028] 장비 임시저장(Draft) 기능
  - **제안 일시:** 2026-08-11 09:49:00
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 신규 카테고리/제조사 결재 승인 대기로 인한 등록 지연이나, 당장 시리얼 번호 등의 정보가 부족할 때 장비를 임시로 저장해 둘 수 있는 강력한 유저 편의 기능입니다. 장비 등록 시 UX 파편화를 막아줍니다.
  - **구현 우선순위:** [상] [제안-027]과 시너지를 내는 핵심 기능이므로 묶어서 진행되어야 합니다.
  - **구현 가능성:** [매우 높음] 기존 `equipment` 테이블에 `IsDraft` 컬럼(기본값 0)을 추가하여 스키마 중복을 막고, 프론트엔드의 모달 및 리스트에서 분기 처리를 통해 100% 깔끔하게 구현 가능합니다.
  - **관련 파일:** `app.py`, `templates/my_equipment.html`, `static/js/equipment.js`
  - **영향도 및 의존성:** 기존 장비 목록 API에 전역으로 `IsDraft=0` 필터링이 추가됨. `[제안-027]`의 전자문서 결재와 연계됨.
  - **추정 난이도:** 하~중 (Low~Medium) - 컬럼 추가 및 UI 상태 토글 로직.
  - **제안 배경 (개요):** 사용자가 장비 등록을 완료할 수 없는 상황(정보 부족, 결재 대기 등)에서 등록 흐름을 끊지 않기 위해 보관함 기능 도입.
  - **상세 내용:**
    1. 등록 화면 모달에 한하여 '임시저장' 버튼 제공.
    2. 임시저장 시 '공개(IsPublic)' 여부가 무시되고 개인 임시저장함에만 노출.
    3. 나의 장비 화면에서 '📁 임시저장함' 버튼을 통해 진입.
    4. 임시저장함 화면에서는 '신규 등록'이 불가능하며, 오직 임시 리스트 보기, 수정, 삭제만 가능.
    5. 정식 장비 수정 시에는 임시저장 불가(수정완료만 가능), 임시 장비 수정 시에는 임시저장 유지 및 정식 등록 가능.

---

## [제안-029] 전역 로딩 오버레이 UI/UX 도입 (모든 비동기 데이터 변경 시)
  - **제안 일시:** 2026-08-11 10:46:00 (수정)
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 비동기(fetch) 처리 시간이 소요될 때 화면 피드백이 없으면 사용자가 버튼을 중복 클릭하거나 페이지를 이탈하여 예기치 않은 오류가 발생할 수 있습니다. 삭제뿐만 아니라 장비 등록/수정, 마스터 통폐합, 전자결재 승인 등 모든 데이터 변경 작업(Mutation)에 로딩 오버레이를 적용하여 시각적 피드백을 제공하고 추가 상호작용을 차단하여 UX를 크게 향상시킵니다.
  - **구현 우선순위:** [중] 시스템 전반의 안정성과 UX 품질을 높이는 중요한 개선 사항입니다.
  - **구현 가능성:** [매우 높음] 모든 템플릿(또는 공통 JS 파일)에 재사용 가능한 전체 화면 딤(Dim) 처리 및 스피너(Spinner) UI를 추가하고, fetch API 호출 전후로 상태를 토글(`showLoading(msg)`, `hideLoading()`)하기만 하면 됩니다.
  - **관련 파일:** `static/css/index.css` (또는 Tailwind 클래스), `static/js/common.js` (신설 또는 기존 JS), 각 HTML 템플릿
  - **영향도 및 의존성:** 데이터가 변경되는 모든 비동기 로직(장비 저장/수정/삭제, 마스터 통폐합, 사용자 계정 제어 등)에 전역적으로 적용됩니다.
  - **추정 난이도:** 하 (Low) - UI 마크업 및 JS 상태 제어
  - **제안 배경 (개요):** 데이터의 저장, 수정, 삭제 등의 비동기 동작 완료 시점까지 발생할 수 있는 사용자의 불필요한 추가 조작을 차단하고 직관적인 진행 상태를 알리기 위함.
  - **상세 내용:**
    1. 검은색 반투명(bg-black/50) 배경에 로딩 스피너와 메시지를 표출하는 전역 HTML/CSS 요소 생성.
    2. z-index를 최상단(z-50)으로 설정하여 클릭 등 일체의 상호작용 원천 차단.
    3. 자바스크립트에서 공통 함수(`showGlobalLoading(message)`, `hideGlobalLoading()`)를 제공하여 확장성을 보장하되, 기본값으로는 `UI_MESSAGES.LOADING_DEFAULT`와 같은 **상수 딕셔너리(Message Dictionary)**를 활용하여 유지보수성 확보 (추후 DB 테이블화 등 다국어 지원에 대비).

---

## [제안-030] Exchange Online 기반 메일링 서비스 연동 (Graph API)
  - **제안 일시:** 2026-08-11 11:17:00
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 / 완료]
  - **결정 사유:** Microsoft 365 테넌트 환경을 고려하여, 보안과 확장성이 뛰어난 Microsoft Graph API(OAuth 2.0)를 통해 시스템의 백그라운드 메일 발송 인프라를 구축하기로 최종 승인됨.
  - **구현 우선순위:** [중] 결재 승인, 비밀번호 발급 등 다양한 알림 기능의 근간이 되는 필수 인프라.
  - **구현 가능성:** [높음] 사용자(관리자)의 Azure AD 앱 등록 선행 작업 후 `requests` 모듈로 간단히 구현 가능.
  - **관련 파일:** `.env`, `utils/mailer.py` (신설), `app.py`
  - **영향도 및 의존성:** 기존 시스템에 영향을 주지 않는 신규 발송 모듈 추가.
  - **추정 난이도:** 중 (Medium) - Azure AD 설정 및 OAuth Token 취득 로직 구성.
  - **제안 배경 (개요):** 단순 알림 기능을 넘어, 승인/반려 내역, 계정 제어 등 시스템 상호작용 피드백을 사용자에게 확실하게 전달하기 위함.
  - **상세 내용 및 구현 계획 (Implementation Plan):**
    1. **Phase 1: 사용자 사전 준비 작업 (Azure AD 앱 등록)**
    - Microsoft Entra 관리 센터(entra.microsoft.com) 접속 및 신규 '앱 등록'.
    - 인증 정보 확보: `클라이언트 ID`, `테넌트 ID`.
    - 클라이언트 암호(Secret) 생성 및 값 복사.
    - `Mail.Send` API 권한 (애플리케이션 권한) 부여 및 **관리자 동의 부여** 완료.
    2. **Phase 2: 백엔드 모듈 개발**
    - `.env`에 `MS_TENANT_ID`, `MS_CLIENT_ID`, `MS_CLIENT_SECRET`, `MAIL_SENDER_ADDRESS` 구조화.
    - `utils/mailer.py`를 신설하여 `login.microsoftonline.com`에서 토큰을 받고 `graph.microsoft.com`으로 발송하는 유틸리티 `send_email()` 구현.
    - 셋팅 검증을 위한 관리자용 임시 테스트 API(`/api/test_mail`) 생성.

---

## [제안-031] 카테고리/제조사 마스터 관리 고도화 (신규 추가 및 선택 일괄 삭제)
  - **제안 일시:** 2026-08-11 14:56:00
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.6 Flash (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 기존 마스터 관리 화면에서는 이미 존재하는 항목의 수정 및 단일 삭제만 제공되어 항목 추가 시 셀렉트 박스에서 '기타'를 통해 전자결재 상신 과정을 거쳐야만 했습니다. 카테고리 및 제조사를 직접 신규 추가할 수 있는 버튼/모달 UI를 신설하고, 다수 마스터 데이터 정리 편의성을 위한 체크박스 기반 선택 일괄 삭제 기능을 추가하여 관리 효율성과 UX를 대폭 향상시킵니다.
  - **구현 우선순위:** [상] 마스터 데이터 관리 효율성 향상에 필수적인 기능입니다.
  - **구현 가능성:** [매우 높음] `app.py`에 POST API 신설 및 `master_management.html` UI/UX 개선을 통해 완전하게 구현 완료.
  - **관련 파일:** `app.py`, `templates/master_management.html`, `Plans/2026-08-11_Master_Management_Enhancement_Plan.md`
  - **영향도 및 의존성:** `categories`, `manufacturers`, `equipment` 테이블 및 마스터 관리 화면.
  - **추정 난이도:** 중 (Medium) - 백엔드 API 추가 및 프론트엔드 체크박스/모달 처리.
  - **상세 내용:**
    1. 추가 화면을 기준 뼈대로 통합한 범용 마스터 모달 UI(`itemModal`) 신설.
    2. 탭(카테고리/제조사) 선택 상태에 따른 "➕ 카테고리/제조사 추가" 동적 UI 및 `POST /api/master/manage/<target_type>` 연동.
    3. 전체/개별 선택 체크박스 및 `POST /api/master/manage/<target_type>/delete_selected` 일괄 삭제 엔드포인트 연동 (삭제 시 관련 장비 컬럼 `NULL` 초기화).
    4. 모든 비동기 변동 작업 시 전역 로딩 오버레이 연동.

---

## [제안-031] 대시보드 통계 기능 확장 (제조사 분포 및 복합 조건 검색 제공)
  - **제안 일시:** 2026-08-11 23:42:00
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 자산 관리의 핵심 축인 '제조사' 현황을 대시보드에 시각화하고, 카테고리/제조사 간의 다차원(복합) 분포를 조회할 수 있게 함으로써 통계 대시보드의 효용성을 대폭 증대시킵니다.
  - **구현 우선순위:** [중] 통계 정보의 고도화 측면에서 매우 훌륭한 제안입니다.
  - **구현 가능성:** [매우 높음] 기존 카테고리 통계 렌더링 로직(`api_dashboard_stats`)을 확장하여 제조사 그룹핑 및 복합 그룹핑(WHERE 조건) 쿼리를 구성하면 원활히 구현 가능합니다.
  - **관련 파일:** `app.py`, `templates/portal.html`
  - **영향도 및 의존성:** `api_dashboard_stats` API 응답 규격 확장. 포털(대시보드) 화면의 차트 렌더링 스크립트 수정.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 현재 대시보드는 카테고리별 장비 분포 단일 지표만 제공하여 입체적인 분석이 불가능함.
  - **상세 내용:**
    1. 카테고리별 장비 분포 유지.
    2. 동일한 UI 형태(바/파이 차트 및 리스트)로 제조사별 장비 분포 신규 추가.
    3. 하단에 Select Box(카테고리 선택 / 제조사 선택)를 배치하여, 두 조건이 교차 적용된 상태에서의 세부 분포 통계 조회 UI 신설.

---

*이 문서는 프로젝트 검토 과정에서 공식적으로 제시한 제안들을 추적·관리하기 위한 목적으로 작성되었습니다. 각 제안의 채택/기각 여부는 사용자의 최종 판단에 따릅니다.*



---

## [제안-033] 장비 상태(정상/수리중/폐기) 관리 기능 추가
  - **제안 일시:** 2026-08-12 00:38:22
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini
  - **상태:** [채택 완료 - 상세 기획 대기중]
  - **결정 사유:** 자산 관리의 핵심인 라이프사이클(정상, 수리중, 폐기 등)을 시스템적으로 추적하기 위해 필요합니다.
  - **구현 우선순위:** [상]
  - **구현 가능성:** [높음] equipment 테이블에 Status 컬럼을 추가하고 관련 UI를 수정하여 구현 가능합니다.
  - **관련 파일:** app.py, 모든 장비 관련 html 파일
  - **영향도 및 의존성:** DB 스키마 변경, 통계 및 조회 로직 전반
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 현재 통계 대시보드(api_dashboard_stats)에서 e.Status 컬럼 부재로 500 에러가 발생하여 '정상'으로 하드코딩 임시 조치함.
  - **상세 내용:**
    1. 장비 상태 필드(Status) 추가 및 관련 이력 추적 기능 구현.
    2. **주의:** 본 기능 구현 시, app.py의 api_dashboard_stats() 내부에 임시 하드코딩된 '정상' as Status 쿼리문(제안-031 임시조치건)을 반드시 실제 Status 컬럼 참조 로직으로 원복/수정할 것.

---

## [제안-034] 내 정보 사용자 프로필(아이디, 이름, 닉네임) 본인 인증 기반 수정 기능
  - **제안 일시:** 2026-08-12 15:43:00
  - **제안자:** 사용자 (User)
  - **검토 모델:** Gemini 3.6 Flash (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 기존 내 정보(마이페이지)에서는 이메일과 비밀번호 변경만 가능하고 아이디, 이름, 닉네임 수정 기능이 부재하여 계정 정보 변경 필요 시 관리자가 직접 DB를 조작해야 하는 불편함이 있었습니다. 본인 소유권 검증(현재 비밀번호 대조)을 거쳐 사용자가 안전하게 자신의 기본 프로필(LoginId, Name, NickName)을 직접 수정할 수 있도록 제공합니다.
  - **구현 우선순위:** [상] 사용자 계정 관리의 편의성과 보안성 확보를 위한 필수 기능입니다.
  - **구현 가능성:** [매우 높음] `app.py`에 `/api/users/update_profile` API를 신설하고 `check_password_hash`로 비밀번호를 검증한 후 `users` 테이블 및 세션(`session['user']`)을 업데이트하면 원활히 구현 가능합니다.
  - **관련 파일:** `app.py`, `templates/mypage.html`
  - **영향도 및 의존성:** `users` 테이블, 세션 시스템, `mypage.html` 프로필 수정 모달 UI.
  - **추정 난이도:** 중 (Medium) - 아이디 중복 체크, 현재 비밀번호 검증, 세션 동기화 및 감사 로그(`UPDATE_USER_PROFILE`) 처리.
  - **제안 배경 (개요):** 로그인한 사용자가 자신의 기본 정보(아이디, 이름, 닉네임)를 안전하게 변경할 수 있도록 현재 비밀번호 검증 기반의 수정 기능을 제공함.
  - **상세 내용:**
    1. 마이페이지(`mypage.html`) 프로필 영역에 [프로필 수정] 버튼 및 전용 모달 UI 구현.
    2. 변경할 아이디(`LoginId`), 이름(`Name`), 닉네임(`NickName`)과 본인 확인용 현재 비밀번호(`current_password`) 입력 폼 제공.
    3. 서버단(`/api/users/update_profile`):
       - CSRF 토큰 및 세션 로그인 검증.
       - 현재 비밀번호 검증 (`check_password_hash(user['Password'], current_password)`).
       - 아이디(`LoginId`) 변경 시 타 계정 중복 체크 및 `sqlite3.IntegrityError` 예외 처리.
       - DB 및 세션(`session['user']`) 정보 업데이트.
       - 보안 감사 로그(`UPDATE_USER_PROFILE`) 기록.

---

## [제안-035] 관리자 전용 메뉴 허브(Admin Center) 분리 구축
  - **제안 일시:** 2026-08-13 10:48:00
  - **제안자:** 사용자 (User) & Gemini 3.1 Pro (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 향후 시스템이 확장됨에 따라 관리자 전용 메뉴가 계속해서 늘어날 것을 대비하여, 포털 화면의 혼잡도를 줄이고 일반 사용자와 관리자의 동선을 논리적으로 분리하는 것이 아키텍처 관점에서 올바른 방향입니다.
  - **구현 우선순위:** [상] 시스템 뼈대 및 메뉴 라우팅 체계를 개편하는 구조적 작업이므로 타 기능 확장에 앞서 우선 진행해야 합니다.
  - **구현 가능성:** [매우 높음] `menus` 테이블에 `MenuGroup` 컬럼을 추가하여 포털과 관리자 전용 메뉴를 쿼리 단에서 분리하고, 프론트엔드의 렌더링 로직을 `admin_center.html`로 이관하면 안전하게 구현 가능합니다.
  - **관련 파일:** `app.py`, `templates/portal.html`, `templates/admin_center.html` (신규)
  - **영향도 및 의존성:** DB `menus` 테이블 스키마 변경(`MenuGroup` 추가 마이그레이션), `app.py` 내 `/api/portal/menus` 라우트 조건 변경, 동적 메뉴 렌더링 스크립트 수정.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 기존에는 `/portal` 화면에서 일반 메뉴(나의 장비 등)와 관리자 메뉴(사용자 관리 등)가 섞여서 동적으로 렌더링됨. 관리자 메뉴 항목 증가 시 발생할 복잡성을 대비하여 접근 동선을 분리함. 기존 접속 링크(예: `/permissions`) 체계는 유지.
  - **상세 내용:**
    1. DB 테이블 `menus`에 `ParentMenuCode`, `SortOrder` 추가.
    2. 관리자 메뉴(사용자 관리, 권한 제어 등)의 `ParentMenuCode`를 `'admin_center'`으로 업데이트.
    3. 포털 화면(`/portal`)에서는 최상위 메뉴만 렌더링하되, 관리자일 경우 관리자 센터가 가장 우측에 렌더링.
    4. 신규 관리자 센터 화면(`/admin_center`)에서 하위 자식 메뉴 렌더링.
    5. N-Depth 권한 유효성 검증 적용.

---

## [제안-036] 실시간 웹 접근 로그(HTTP Access Log) 자동 수집 및 모니터링 시스템 구축
  - **제안 일시:** 2026-08-15 00:00:00
  - **제안자:** 사용자 (User) & Gemini 3.7 Flash (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 터미널에 실시간으로 출력되는 표준 웹 서버 접근 로그(HTTP Access Logs: IP, Method, Path, Status Code, Duration 등)를 DB에 체계적으로 적재하고, 비즈니스 감사 로그(`audit_logs`)와는 분리된 관리자 전용 관제 화면(`access_logs.html`)을 통해 외부 봇넷/스캐너 공격 탐지 및 웹 트래픽을 실시간 모니터링할 수 있는 필수 인프라 기능입니다.
  - **구현 우선순위:** [상] 실시간 보안 관제 및 인프라 모니터링 확보 차원.
  - **구현 가능성:** [매우 높음] `@app.after_request` 인터셉터와 인메모리 큐(`queue.Queue`) 기반 백그라운드 벌크 워커를 도입하여 미니서버의 SD카드 디스크 I/O를 완벽히 보호하면서 고성능으로 구현 가능합니다.
  - **관련 파일:** `app.py`, `templates/access_logs.html` (신규)
  - **영향도 및 의존성:** `access_logs` 테이블 신규 생성, `@app.after_request` 인터셉터 추가, `menus` 테이블에 `access_logs` 메뉴 등록 (`ParentMenuCode='admin_center'`), `role_menu_permissions` 관리자 권한 연동.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 비즈니스 감사 로그(`audit_logs`) 외에 로그인 여부와 무관한 모든 인바운드 HTTP 요청(정상 접근, 404/405/415 에러, 정적 리소스 로딩)을 수집하고 실시간으로 관제하기 위함.
  - **상세 내용:**
    1. DB `access_logs` 테이블 및 인덱스 4종 생성 (WAL 모드 활성화).
    2. 5초 주기 세션 폴링(`/api/check_session`)은 제외, 정적 리소스(`/static/*`)는 `IsStatic=1` 플래그로 분리 수집.
    3. 인메모리 큐 + 백그라운드 벌크 인서트(`executemany`)로 미니서버 디스크 I/O 부하 95% 절감.
    4. 관리자 전용 웹 접근 로그 모니터링 화면(`access_logs.html`) 구축 (요약 위젯, 3단 퀵 필터, 실시간 자동 새로고침, 관리자 수동 클렌징 도구).

---

## [제안-037] WebMCP(웹 모델 컨텍스트 프로토콜) 기반 AI 에이전트 연동 표준화 및 안전망 구축
  - **제안 일시:** 2026-08-15 12:20:00
  - **제안 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 지능형 에이전트(AI)가 웹사이트를 도구처럼 사용하여 자동화된 업무(장비 등록, 결재 등)를 수행할 수 있게 하는 차세대 표준(Chrome 149+)입니다. 
  - **구현 우선순위:** [중] (향후 다른 제안 구현 시 설계의 기반 패러다임으로 삼기 위해 채택되었으나, 즉시 적용보다는 점진적 적용이 권장됨)
  - **구현 가능성:** [높음] 기존 Flask 템플릿의 Form에 Declarative API를, 비동기 호출에 Imperative JS API를 추가하는 방식으로 백엔드 구조 변경 없이 프론트엔드 레벨에서 연동 가능합니다.
  - **관련 파일:** `app.py`, `templates/*.html` (모든 화면 템플릿)
  - **영향도 및 의존성:** 기존 폼 UI 요소들에 `data-mcp-*` 속성이 일괄 주입되며, 인증 쿠키/세션 컨텍스트를 그대로 공유합니다.
  - **추정 난이도:** 상 (High) - 코딩 자체의 난이도보다는 **보안 설계(안전망)**의 난이도가 극도로 높습니다.
  - **제안 배경 (개요):** 지능이 낮은 AI의 환각(Hallucination)에 의한 오작동, 혹은 지능이 매우 높은 AI의 논리적 오류로 인한 시스템 삭제/데이터 파괴 등의 치명적인 끔찍한 사고를 완벽히 예방하면서도 "AI-Ready" 웹사이트로 진화시키기 위함.
  - **상세 내용 및 필수 안전망(Guardrails):**
    1. **이중 API 아키텍처:**
       - **선언형(Declarative) API:** 장비 검색, 단순 조회 등 파괴적이지 않은(Read-Only) 폼 제출과 단순 기안 작성 폼.
       - **명령형(Imperative) API:** 장비 상태 변경, 마스터 데이터 수정, 결재 승인 등 시스템 상태를 변화시키는 행위.
    2. **[필수 안전망 1] 결정론적 제약(Deterministic Guardrails) & CSRF 연동:**
       - 모든 명령형(Imperative) 도구 호출 시, **서버 측에서 발급한 일회성 CSRF 토큰 검증**을 강제합니다. AI가 자체적으로 임의의 파라미터를 조작해 우회하는 것을 원천 차단합니다.
    3. **[필수 안전망 2] 파괴적 액션 시 Human-in-the-Loop (확인 프롬프트 강제):**
       - DB를 수정/삭제하는 액션(DELETE 쿼리, `equipment.db` 조작 등)이 요청될 경우, 브라우저 단에서 **사용자의 명시적 승인(버튼 클릭 또는 2FA)**을 받지 않으면 AI가 절대 단독으로 실행할 수 없도록 WebMCP 명세(Schema)에 `requires_human_confirmation: true` 로직을 하드코딩합니다.
    4. **[필수 안전망 3] AI 샌드박싱 및 롤백 체계 (Auditing & Soft-Delete):**
       - 에이전트가 실행한 모든 내역은 `audit_logs` 테이블에 `UserAgent`를 통해 "AI Agent"로 명확히 마킹되어 기록되어야 하며, 데이터 삭제 시 물리적 삭제(DROP, DELETE)를 전면 금지하고 Soft-Delete(`IsDeleted=1` 플래그)만을 허용하여 AI 폭주 시 즉각적인 롤백이 가능하도록 아키텍처를 방어합니다.

---

## [제안-038] 메타데이터 자동 라우팅 및 보안 텍스트(security.txt) 도입
  - **제안 일시:** 2026-08-16 11:30:00
  - **제안 모델:** Gemini 3.7 Flash (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 로봇(크롤러) 및 AI 스캐너 봇들의 무작위 스캐닝으로부터 미니서버의 불필요한 트래픽 및 404 에러 로그 생성을 막고, 프로젝트 소유권을 명확히 하는 표준 메타데이터 제공.
  - **구현 우선순위:** [중]
  - **구현 가능성:** [매우 높음] `Resources/metadata/` 디렉토리에 정적 파일을 위치시키고, `app.py` 구동 시 `os.walk`를 이용해 재귀적으로 라우터를 자동 생성하면 구현 완료됩니다.
  - **관련 파일:** `app.py`, `Resources/metadata/*`
  - **영향도 및 의존성:** `app.after_request` 인터셉터 로직에 메타데이터 라우팅 추가 (O(1) frozenset 검사). 동적 라우팅 중복 생성(AssertionError) 방지 코드 적용.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 외부 크롤러의 `robots.txt`, `security.txt` 등의 고정된 메타데이터 파일 탐색 시, Flask 서버가 일일이 대응하여 정적 리소스로 라우팅해주지 않으면 404 에러 로그가 양산됨.
  - **상세 내용:**
    1. `Resources/metadata/` 디렉토리 하위에 `.well-known/security.txt`, `security.txt`, `robots.txt`, `llms.txt` 작성.
    2. `security.txt` 내 연락처 이메일은 반드시 사용자 지정 하드코딩(`nekohost@nekohost.org`) 적용.
    4. HTTP 접근 로그(`access_logs`) 수집 시 `IsStatic=1`로 분류되도록 O(1) 정적 검사 풀(`frozenset`) 도입.

---

## [제안-039] 에러 유발 고유 IP 분석(Error IP Analysis) 대시보드 추가 (Staging-First 원칙 준수 필수)
  - **제안 일시:** 2026-08-18 12:20:00
  - **제안 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 기존 `access_logs.html`은 로그가 단건 나열식이라 공격/스캐너 IP를 특정하기 어려워, 4xx/5xx 에러를 발생시킨 고유 IP만 추출하는 심층 분석 화면이 필수적입니다.
  - **구현 우선순위:** [중]
  - **구현 가능성:** [매우 높음] 완전히 새로운 테이블이나 백그라운드 엔진의 추가 없이, 기존 `access_logs` 테이블에 순수 SQL 쿼리(`GROUP BY`)만 호출하여 즉각적인 동적 렌더링이 가능합니다.
  - **관련 파일:** `app.py`, `templates/access_logs_error_ips.html` (신규), `templates/access_logs.html`
  - **영향도 및 의존성:** 기존 `access_logs.html` 헤더에 진입 버튼이 추가되며, 신규 템플릿과 API 함수(`/api/access_logs/error_ips`)가 추가됩니다. 권한은 `access_logs`의 메뉴 권한을 그대로 상속받습니다.
  - **추정 난이도:** 하 (Low) - 쿼리 작성과 프론트엔드 연동 수준.
  - **제안 배경 (개요):** 에러 발생 빈도가 높은 IP를 추적하여 직관적으로 모니터링하고 차단 조치를 취하기 위한 기반 화면 제공.
  - **상세 내용 및 🤖 AI 개발자 필수 준수 가이드라인 (절대 준수!):**
    > **주의:** 비교적 추론 지능이 낮은 AI가 이 문서를 읽고 작업을 진행하더라도 파국적 실수를 저지르지 않도록, `Rule.md`와 매핑된 매우 촘촘한 가이드를 제공합니다.
    1. **[기능 구현 내용 (No-Logic 원칙)]**:
       - `templates/access_logs_error_ips.html` 신규 작성. 화면 상단에 상위 IP 요약 차트 배치.
       - 데이터 테이블에는 IP 주소, 총 에러 건수, Client(4xx)/Server(5xx) 건수, 마지막 에러 발생 시간을 표출.
       - IP 주소 클릭 시, 기존 `access_logs.html` 화면으로 자동 딥링크 연동되어 해당 IP가 필터링/검색된 상태로 전환되도록 프론트 연동.
       - `app.py`에 별도 워커/엔진 추가 절대 금지. `WHERE StatusCode >= 400 GROUP BY IpAddress` 쿼리를 사용하는 API `/api/access_logs/error_ips` 1개 추가.
    2. **[Rule.md 제7-3조: 스테이징 격리 필수]**:
       - 절대로 루트 경로의 `app.py`나 `templates/`에 즉시 코드를 작성하지 마십시오.
       - 반드시 `Staging/app.py`를 복사 생성하고, `Staging/templates/access_logs_error_ips.html` 위치에 파일들을 생성하여 **안전한 모의 검증(Safety Net)**을 구축한 상태에서만 코딩을 진행하십시오.
    3. **[Rule.md 제5-1-1조: 윈도우 PC 런타임 구동 금지]**:
       - `Staging/app.py`를 만들었다고 해서 윈도우 터미널(PowerShell)에 `python Staging/app.py`를 실행하려 시도하지 마십시오. 정적 코드 리뷰만으로 검증하고 사용자에게 검토를 맡기십시오.
    4. **[Rule.md 제4-3조: 3대 주석 보존 수칙]**:
       - 추가되는 파이썬 함수 상단에 `[역할]`, `[의존성 관계]`, `[변경 시 영향도]` 3가지 주석 태그를 단 한 글자도 빠짐없이 명시하십시오. 기존 주석을 훼손해서는 안 됩니다.

---

## [제안-040] 웹 접근 로그 상세 기록 확장 (Request & Response Payload 로깅)
  - **제안 일시:** 2026-08-19
  - **제안 모델:** Antigravity (Gemini)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **관련 제안:** [제안-036] 실시간 웹 접근 로그 자동 수집 및 모니터링 시스템 구축
  - **제안 배경 (개요):** 기존 접근 로그가 메타데이터(IP, 상태 코드, URL 등)만 기록하고 있어 상세 에러 내역 및 요청/응답 Body 내용 확인에 제약이 있음. Request Data 및 Response Payload까지 기록하여 상세 트러블슈팅 및 감사 추적 역량을 강화하기 위함.
  - **상세 내용:**
    1. access_logs 테이블 스키마 확장을 통해 request_payload, response_payload 데이터를 저장할 수 있도록 구조 변경.
    2. 관리자 웹 접근 로그 모니터링 화면(access_logs.html)에서 행을 클릭하면 모달 팝업 또는 아코디언 패널로 상세 Request/Response 내용을 조회할 수 있는 UI 구현.
    3. 비밀번호, 인증 토큰 등 민감 정보가 로그 테이블에 평문으로 남지 않도록 로깅 직전 필드 마스킹(Masking) 및 데이터 클렌징 처리 로직 적용.

---

## [제안-041] 웹 접근 로그 요약 통계 기간 필터 확장 (오늘/전체 토글)
  - **제안 일시:** 2026-08-21 09:49:00
  - **제안자:** 사용자 (User)
  - **제안 모델:** Gemini 3.7 Flash (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** 웹 접근 로그 화면 상단 요약 카드가 오늘 하루 데이터로만 제한되어 있어, 전체 누적 트래픽과 장기적 에러율을 즉각 파악하기 어려운 한계를 해소하기 위함.
  - **구현 우선순위:** [상]
  - **구현 가능성:** [매우 높음] DB 스키마 변경 없이 `GET /api/access_logs/stats?period=today|all` 쿼리 조건문 분기 및 프론트엔드 세그먼트 토글 버튼 연동만으로 안전하게 구현 가능합니다.
  - **관련 파일:** `app.py`, `templates/access_logs.html`
  - **영향도 및 의존성:** `GET /api/access_logs/stats` 엔드포인트 파라미터 확장. 상단 요약 카드 레이블 및 UI 갱신.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 관리자 화면 상단 카드에 제공 중인 데이터 기준을 오늘뿐만 아니라 전체 누적으로도 볼 수 있도록 토글 제어 기능을 추가함.
  - **상세 내용:**
    1. 백엔드 통계 API에 `period` 쿼리 파라미터(`today` | `all`, 기본값 `today`) 분기 로직 적용.
    2. 프론트엔드 상단 카드 영역에 세그먼트 컨트롤 UI(`[ 📅 오늘 ]` / `[ 🌐 전체 누적 ]`) 추가 및 동적 레이블/수치 갱신 구현.

---

## [제안-042] Python 3.14 런타임 안정화 및 Reloader 세마포어 크래시 방어
  - **제안 일시:** 2026-08-21 10:34:00
  - **제안자:** 사용자 (User)
  - **제안 모델:** Gemini 3.7 Flash (High)
  - **상태:** [개발 완료 (FEATURES.md 이관)]
  - **결정 사유:** Python 3.14 환경에서 Werkzeug Auto-Reloader 구동 시 자식 프로세스 IPC 세마포어 감시 충돌로 서비스가 시작 직후 무음 셧다운되는 장애를 원천 차단하고 단일 프로세스 구동 안정성을 확보하기 위함.
  - **구현 우선순위:** [상]
  - **구현 가능성:** [매우 높음] `app.run()`에 `use_reloader=False`를 명시하고 `try...except` 래핑을 적용하여 즉시 해결 가능합니다.
  - **관련 파일:** `app.py`
  - **영향도 및 의존성:** `app.py` 서버 진입점(`if __name__ == '__main__':`) 런타임 옵션 수정.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** Python 3.14 런타임 환경에서 서브프로세스 세마포어 누수 경고 및 서비스 강제 종료 현상을 방어하기 위한 런타임 진입점 안정화.
  - **상세 내용:**
    1. `app.run(host='0.0.0.0', port=5000, debug=is_debug, use_reloader=False)` 적용으로 Werkzeug 서브프로세스 분기 차단.
    2. `try...except` 및 `traceback.print_exc()` 적용으로 포트 충돌 및 소켓 바인딩 실패 시 원인 로그 완전 표출.
    1. ccess_logs 테이블 스키마 확장을 통해 equest_payload, esponse_payload 데이터를 저장할 수 있도록 구조 변경.
    2. 관리자 웹 접근 로그 모니터링 화면(ccess_logs.html)에서 행을 클릭하면 모달 팝업 또는 아코디언 패널로 상세 Request/Response 내용을 조회할 수 있는 UI 구현.
    3. 비밀번호, 인증 토큰 등 민감 정보가 로그 테이블에 평문으로 남지 않도록 로깅 직전 필드 마스킹(Masking) 및 데이터 클렌징 처리 로직 적용.
