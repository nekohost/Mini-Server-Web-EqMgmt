# 미구현 제안(Unimplemented Proposals) 관리 문서

이 문서는 `PROPOSALS.md`에서 현재까지 개발이 완료되지 않은 제안 항목(대기중, 채택 완료 - 개발 예정, 재기획 후 승인 대기, 보류, 보류 권장 등)만을 별도로 분리하여 실시간 작업 상태를 추적 및 관리합니다.
개발이 완료된 항목은 `Rule.md` 제7조에 의거하여 이 문서에서 제거되고 `PROPOSALS.md` 및 `FEATURES.md`에서 영구 관리됩니다.

---

## [제안-047] 장비등록 라인업 노드 추가 및 관리자 노드 관리

- **등록 일시:** 2026-09-10 18:28:00.506 (KST)
- **제안자 / 검토자:** 사용자 / OpenAI Codex
- **상태:** [구현·Linux 검증·백업 서버 적용 완료 / 주 서버 적용·추가 실사용 확인 잔여]
- **작업 ID:** `WORK-20260910-LINEUP-NODE-MANAGEMENT`
- **우선순위:** [최상] 장비등록 과정의 노드 추가 불가 문제 우선 해결.
- **성격:** 기존 3-Tier 구조의 누락 보완과 관리 기능 확장. 011-고도화 분류 마스터, 027 승인 절차, 031 카테고리/제조사 관리에 의존.
- **선행 기획:** [가변 깊이 모델 트리 기획안](Plans/2026/08/19/001_Proposal036_Equipment_Hierarchy_Plan.md). 가칭 036은 정식 웹 접근 로그 제안 번호와 충돌하므로 이번 작업은 047로 추적.
- **결정 사유:** 등록 동선과 관리자 트리 관리의 독립 완료 기준이 필요하다. 기존 완료 제안에 개선완료로 붙이면 잔여 통합 작업이 숨겨진다.
- **범위:** 루트·하위 노드 추가, 관리자 즉시 승인/일반 사용자 승인 대기, 관리자 상태·사용량 조회와 생성·이름 변경·이동·삭제, 부모 조합·중복·순환·최대 깊이·삭제 참조 검증.
- **현재:** [운영 안내](docs/proposal047-operation.md). 등록 동선·메뉴·권한·API 소스 통합 완료, 메모리 DB 13건 및 등록 선택기 상태 전이 3건 통과.
- **잔여:** 주 서버는 SSH 인증 불가로 적용 잔여. 013은 백업 실패 개선·실제 DB 호환성·자동 복구 회귀를 완료했으며 별도 실사용 피드백은 수용한다.
- **승인 범위:** 사용자 승인으로 운영 소스 반영 완료. Linux 서비스 적용은 미수행.
- **비범위:** 스키마 재설계, 013 시험·변경, 옵션 승인·임시저장 fallback·마스터 삭제 전체 개편.
- **근거:** [계획](Plans/2026/09/10/003_Lineup_Node_Registration_and_Admin_Management_Plan.md), [귀속 검토](Reports/2026/09/10/010_Lineup_Node_Proposal_Classification_Report.md).

---

## [제안-006] 알림 및 공지사항 시스템
  - **제안 일시:** 2026-08-03 20:19:30
  - **제안 모델:** Claude Opus 4.6 (Thinking)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [보류 권장]
  - **결정 사유:** 미니서버(라즈베리파이 등) 기반의 개인/소규모 팀 자산 관리 용도에서는 시스템 내장 공지사항보다 직접적인 커뮤니케이션(메신저 등)이 더 빠르고 효율적일 수 있습니다. 시스템이 불필요하게 무거워질 우려가 있습니다.
  - **관련 파일:** `app.py`, `templates/portal.html`, `templates/` (신규 `notices.html` 생성 필요)
  - **영향도 및 의존성:** DB에 `notices` 테이블 신규 생성 필요 (`init_db()` 수정). 공지사항 CRUD API 추가. 포털 화면 상단에 공지 배너 영역 추가. `menus` 테이블에 공지사항 관리 메뉴 등록.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 관리자가 사용자들에게 시스템 점검 안내, 업데이트 내역, 중요 공지 등을 전달할 수 있는 수단이 전혀 없음. 서비스 운영의 기본적인 커뮤니케이션 채널로서, 간단한 공지사항 CRUD와 포털 상단 배너 노출 정도만으로도 운영 편의성이 크게 올라감.
  - **상세 내용:**
    - `notices` 테이블: NoticeId, Title, Content, AuthorId, IsActive, CreatedAt, UpdatedAt
    - 관리자: 공지사항 작성/수정/삭제 (CRUD)
    - 사용자: 포털 진입 시 활성화된 공지사항 배너 자동 표시
    - 공지 확인(읽음) 처리 여부는 선택적으로 구현

---

## [제안-009] 장비 QR코드 / 바코드 생성 및 모바일 스캔 기능
  - **제안 일시:** 2026-08-03 22:21:30
  - **제안 모델:** Gemini 3.6 Flash (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [보류 권장]
  - **결정 사유:** 실물 관리 측면에서 매우 매력적이나, 브라우저에서 카메라를 제어하여 바코드를 스캔하려면 모바일 UI 최적화와 HTTPS 보안 설정(카메라 접근 필수)이 강제되며 라벨 프린터와의 연동 등 외부 허들이 많아 현 단계에서는 오버스펙으로 판단됩니다.
  - **관련 파일:** `app.py`, `templates/index.html`
  - **영향도 및 의존성:** QR코드 생성 라이브러리(jsqrcode 또는 qrcode.js CDN) 도입 필요. 모바일 카메라 스캔용 라이브러리(html5-qrcode CDN) 연동 필요.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 장비 검색 시 시리얼 넘버나 이름을 직접 입력하는 검색 단계를 단축하고, 실물 장비에 부착된 스티커 스캔으로 빠른 상세 조회를 지원하기 위함.
  - **상세 내용:**
    - 장비별 unique URL 또는 ID가 포함된 QR코드 이미지 생성 및 모달 출력 (인쇄 기능 지원)
    - 스마트폰 카메라를 이용해 QR코드를 스캔하면 해당 장비의 상세 페이지로 즉시 이동

---

## [제안-012] 다중 사용자 지정 태그 (Custom Tagging) 시스템
  - **제안 일시:** 2026-08-03 22:21:30
  - **제안 모델:** Gemini 3.6 Flash (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [보류 권장]
  - **결정 사유:** 다중 태그 시스템은 다대다(N:M) 매핑 테이블이 필요하며, 검색 로직과 UI(태그 추가/삭제 칩) 구현이 복잡해집니다. [제안-011]의 카현 가능성:** [매우 높음] SQLite DB가 단일 파일(`equipment.db`)이라는 점을 십분 활용하여, 관리자가 해당 파일을 다운로드(send_file)하거나 새 파일로 덮어쓰기(업로드)하는 매우 단순한 API만으로 백업/복원 기능을 완성할 수 있습니다.
  - **제안 배경 (개요):** 미니서버의 SD카드/디스크 장애나 실수로 인한 데이터 손실에 대비하여, 관리자가 클릭 한 번으로 DB 백업본을 수신받고 복원할 수 있는 재난 복구(DR) 수단을 제공하기 위함.
  - **상세 내용:**
    - 관리자 전용 DB 덤프/다운로드 기능 (ZIP/DB 파일 다운로드)
    - 백업 파일 업로드를 통한 DB 롤백/복원 기능 (복원 전 자동 백업 생성 안전장치 포함)

---



## [제안-037] WebMCP(웹 모델 컨텍스트 프로토콜) 기반 AI 에이전트 연동 표준화 및 안전망 구축
  - **제안 일시:** 2026-08-15 12:20:00
  - **제안 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 지능형 에이전트(AI)가 웹사이트를 도구처럼 사용하여 자동화된 업무(장비 등록, 결재 등)를 수행할 수 있게 하는 차세대 표준(Chrome 149+)입니다. 
  - **구현 우선순위:** [중] (향후 다른 제안 구현 시 설계의 기반 패러다임으로 삼기 위해 채택되었으나, 즉시 적용보다는 점진적 적용이 권장됨)
  - **구현 가능성:** [높음] 기존 Flask 템플릿의 Form에 Declarative API를, 비동기 호출에 Imperative JS API를 추가하는 방식으로 백엔드 구조 변경 없이 프론트엔드 레벨에서 연동 가능합니다.
  - **관련 파일:** `app.py`, `templates/*.html` (모든 화면 템플릿)
  - **영향도 및 의존성:** 기존 폼 UI 요소들에 `data-mcp-*` 속성이 일괄 주입되며, 인증 쿠키/세션 컨텍스트를 그대로 공유합니다.
  - **추정 난이도:** 상 (High) - 코딩 자체의 난이도보다는 **보안 설계(안전망)**의 난이도가 극도로 높습니다.
  - **제안 배경 (개요):** 지능이 낮은 AI의 환각(Hallucination)에 의한 오작동, 혹은 지능이 매우 높은 AI의 논리적 오류로 인한 시스템 삭제/데이터 파괴 등의 치명적인 끔찍한 사고를 완벽히 예방하면서도 "AI-Ready" 웹사이트로 진화시키기 위함.
  - **상세 내용 및 필수 안전망(Guardrails):**
    1. **이중 API 아키텍처:**
       - **선언형(Declarative) API:** 장비 검색, 단순 조회 등 파괴적이지 않은(Read-Only) 폼 제출과 단순 기안 작성 폼.
       - **명령형(Imperative) API:** 장비 상태 변경, 마스터 데이터 수정, 결재 승인 등 시스템 상태를 변화시키는 행위.
    2. **[필수 안전망 1] 결정론적 제약(Deterministic Guardrails) & CSRF 연동:**
       - 모든 명령형(Imperative) 도구 호출 시, **서버 측에서 발급한 일회성 CSRF 토큰 검증**을 강제합니다. AI가 자체적으로 임의의 파라미터를 조작해 우회하는 것을 원천 차단합니다.
    3. **[필수 안전망 2] 파괴적 액션 시 Human-in-the-Loop (확인 프롬프트 강제):**
       - DB를 수정/삭제하는 액션(DELETE 쿼리, `equipment.db` 조작 등)이 요청될 경우, 브라우저 단에서 **사용자의 명시적 승인(버튼 클릭 또는 2FA)**을 받지 않으면 AI가 절대 단독으로 실행할 수 없도록 WebMCP 명세(Schema)에 `requires_human_confirmation: true` 로직을 하드코딩합니다.
    4. **[필수 안전망 3] AI 샌드박싱 및 롤백 체계 (Auditing & Soft-Delete):**
       - 에이전트가 실행한 모든 내역은 `audit_logs` 테이블에 `UserAgent`를 통해 "AI Agent"로 명확히 마킹되어 기록되어야 하며, 데이터 삭제 시 물리적 삭제(DROP, DELETE)를 전면 금지하고 Soft-Delete(`IsDeleted=1` 플래그)만을 허용하여 AI 폭주 시 즉각적인 롤백이 가능하도록 아키텍처를 방어합니다.

---

## [제안-038] 메타데이터 자동 라우팅 및 보안 텍스트(security.txt) 도입
  - **제안 일시:** 2026-08-16 11:30:00
  - **제안 모델:** Gemini 3.7 Flash (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 로봇(크롤러) 및 AI 스캐너 봇들의 무작위 스캐닝으로부터 미니서버의 불필요한 트래픽 및 404 에러 로그 생성을 막고, 프로젝트 소유권을 명확히 하는 표준 메타데이터 제공.
  - **구현 우선순위:** [중]
  - **구현 가능성:** [매우 높음] `Resources/metadata/` 디렉토리에 정적 파일을 위치시키고, `app.py` 구동 시 `os.walk`를 이용해 재귀적으로 라우터를 자동 생성하면 구현 완료됩니다.
  - **관련 파일:** `app.py`, `Resources/metadata/*`
  - **영향도 및 의존성:** `app.after_request` 인터셉터 로직에 메타데이터 라우팅 추가 (O(1) frozenset 검사). 동적 라우팅 중복 생성(AssertionError) 방지 코드 적용.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 외부 크롤러의 `robots.txt`, `security.txt` 등의 고정된 메타데이터 파일 탐색 시, Flask 서버가 일일이 대응하여 정적 리소스로 라우팅해주지 않으면 404 에러 로그가 양산됨.
  - **상세 내용:**
    1. `Resources/metadata/` 디렉토리 하위에 `.well-known/security.txt`, `security.txt`, `robots.txt`, `llms.txt` 작성.
    2. `security.txt` 내 연락처 이메일은 반드시 사용자 지정 하드코딩(`nekohost@nekohost.org`) 적용.
    3. `app.py` 기동 시, 해당 디렉토리를 탐색하여 모든 파일을 `send_from_directory`로 서비스하는 동적 라우팅 생성.
    4. HTTP 접근 로그(`access_logs`) 수집 시 `IsStatic=1`로 분류되도록 O(1) 정적 검사 풀(`frozenset`) 도입.

---

## [제안-039] 에러 유발 고유 IP 분석(Error IP Analysis) 대시보드 추가 (Staging-First 원칙 준수 필수)
  - **제안 일시:** 2026-08-18 12:20:00
  - **제안 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 기존 `access_logs.html`은 로그가 단건 나열식이라 공격/스캐너 IP를 특정하기 어려워, 4xx/5xx 에러를 발생시킨 고유 IP만 추출하는 심층 분석 화면이 필수적입니다.
  - **구현 우선순위:** [중]
  - **구현 가능성:** [매우 높음] 완전히 새로운 테이블이나 백그라운드 엔진의 추가 없이, 기존 `access_logs` 테이블에 순수 SQL 쿼리(`GROUP BY`)만 호출하여 즉각적인 동적 렌더링이 가능합니다.
  - **관련 파일:** `app.py`, `templates/access_logs_error_ips.html` (신규), `templates/access_logs.html`
  - **영향도 및 의존성:** 기존 `access_logs.html` 헤더에 진입 버튼이 추가되며, 신규 템플릿과 API 함수(`/api/access_logs/error_ips`)가 추가됩니다. 권한은 `access_logs`의 메뉴 권한을 그대로 상속받습니다.
  - **추정 난이도:** 하 (Low) - 쿼리 작성과 프론트엔드 연동 수준.
  - **제안 배경 (개요):** 에러 발생 빈도가 높은 IP를 추적하여 직관적으로 모니터링하고 차단 조치를 취하기 위한 기반 화면 제공.
  - **상세 내용 및 🤖 AI 개발자 필수 준수 가이드라인 (절대 준수!):**
    > **주의:** 비교적 추론 지능이 낮은 AI가 이 문서를 읽고 작업을 진행하더라도 파국적 실수를 저지르지 않도록, `Rule.md`와 매핑된 매우 촘촘한 가이드를 제공합니다.
    1. **[기능 구현 내용 (No-Logic 원칙)]**:
       - `templates/access_logs_error_ips.html` 신규 작성. 화면 상단에 상위 IP 요약 차트 배치.
       - 데이터 테이블에는 IP 주소, 총 에러 건수, Client(4xx)/Server(5xx) 건수, 마지막 에러 발생 시간을 표출.
       - IP 주소 클릭 시, 기존 `access_logs.html` 화면으로 자동 딥링크 연동되어 해당 IP가 필터링/검색된 상태로 전환되도록 프론트 연동.
       - `app.py`에 별도 워커/엔진 추가 절대 금지. `WHERE StatusCode >= 400 GROUP BY IpAddress` 쿼리를 사용하는 API `/api/access_logs/error_ips` 1개 추가.
  - **결정 사유:** 실물 관리 측면에서 매우 매력적이나, 브라우저에서 카메라를 제어하여 바코드를 스캔하려면 모바일 UI 최적화와 HTTPS 보안 설정(카메라 접근 필수)이 강제되며 라벨 프린터와의 연동 등 외부 허들이 많아 현 단계에서는 오버스펙으로 판단됩니다.
  - **관련 파일:** `app.py`, `templates/index.html`
  - **영향도 및 의존성:** QR코드 생성 라이브러리(jsqrcode 또는 qrcode.js CDN) 도입 필요. 모바일 카메라 스캔용 라이브러리(html5-qrcode CDN) 연동 필요.
  - **추정 난이도:** 중 (Medium)
  - **제안 배경 (개요):** 장비 검색 시 시리얼 넘버나 이름을 직접 입력하는 검색 단계를 단축하고, 실물 장비에 부착된 스티커 스캔으로 빠른 상세 조회를 지원하기 위함.
  - **상세 내용:**
    - 장비별 unique URL 또는 ID가 포함된 QR코드 이미지 생성 및 모달 출력 (인쇄 기능 지원)
    - 스마트폰 카메라를 이용해 QR코드를 스캔하면 해당 장비의 상세 페이지로 즉시 이동

---

## [제안-012] 다중 사용자 지정 태그 (Custom Tagging) 시스템
  - **제안 일시:** 2026-08-03 22:21:30
  - **제안 모델:** Gemini 3.6 Flash (High)
  - **검토 모델:** Gemini 3.1 Pro (High)
  - **상태:** [보류 권장]
  - **결정 사유:** 다중 태그 시스템은 다대다(N:M) 매핑 테이블이 필요하며, 검색 로직과 UI(태그 추가/삭제 칩) 구현이 복잡해집니다. [제안-011]의 카현 가능성:** [매우 높음] SQLite DB가 단일 파일(`equipment.db`)이라는 점을 십분 활용하여, 관리자가 해당 파일을 다운로드(send_file)하거나 새 파일로 덮어쓰기(업로드)하는 매우 단순한 API만으로 백업/복원 기능을 완성할 수 있습니다.
  - **제안 배경 (개요):** 미니서버의 SD카드/디스크 장애나 실수로 인한 데이터 손실에 대비하여, 관리자가 클릭 한 번으로 DB 백업본을 수신받고 복원할 수 있는 재난 복구(DR) 수단을 제공하기 위함.
  - **상세 내용:**
    - 관리자 전용 DB 덤프/다운로드 기능 (ZIP/DB 파일 다운로드)
    - 백업 파일 업로드를 통한 DB 롤백/복원 기능 (복원 전 자동 백업 생성 안전장치 포함)

---



## [제안-037] WebMCP(웹 모델 컨텍스트 프로토콜) 기반 AI 에이전트 연동 표준화 및 안전망 구축
  - **제안 일시:** 2026-08-15 12:20:00
  - **제안 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 지능형 에이전트(AI)가 웹사이트를 도구처럼 사용하여 자동화된 업무(장비 등록, 결재 등)를 수행할 수 있게 하는 차세대 표준(Chrome 149+)입니다. 
  - **구현 우선순위:** [중] (향후 다른 제안 구현 시 설계의 기반 패러다임으로 삼기 위해 채택되었으나, 즉시 적용보다는 점진적 적용이 권장됨)
  - **구현 가능성:** [높음] 기존 Flask 템플릿의 Form에 Declarative API를, 비동기 호출에 Imperative JS API를 추가하는 방식으로 백엔드 구조 변경 없이 프론트엔드 레벨에서 연동 가능합니다.
  - **관련 파일:** `app.py`, `templates/*.html` (모든 화면 템플릿)
  - **영향도 및 의존성:** 기존 폼 UI 요소들에 `data-mcp-*` 속성이 일괄 주입되며, 인증 쿠키/세션 컨텍스트를 그대로 공유합니다.
  - **추정 난이도:** 상 (High) - 코딩 자체의 난이도보다는 **보안 설계(안전망)**의 난이도가 극도로 높습니다.
  - **제안 배경 (개요):** 지능이 낮은 AI의 환각(Hallucination)에 의한 오작동, 혹은 지능이 매우 높은 AI의 논리적 오류로 인한 시스템 삭제/데이터 파괴 등의 치명적인 끔찍한 사고를 완벽히 예방하면서도 "AI-Ready" 웹사이트로 진화시키기 위함.
  - **상세 내용 및 필수 안전망(Guardrails):**
    1. **이중 API 아키텍처:**
       - **선언형(Declarative) API:** 장비 검색, 단순 조회 등 파괴적이지 않은(Read-Only) 폼 제출과 단순 기안 작성 폼.
       - **명령형(Imperative) API:** 장비 상태 변경, 마스터 데이터 수정, 결재 승인 등 시스템 상태를 변화시키는 행위.
    2. **[필수 안전망 1] 결정론적 제약(Deterministic Guardrails) & CSRF 연동:**
       - 모든 명령형(Imperative) 도구 호출 시, **서버 측에서 발급한 일회성 CSRF 토큰 검증**을 강제합니다. AI가 자체적으로 임의의 파라미터를 조작해 우회하는 것을 원천 차단합니다.
    3. **[필수 안전망 2] 파괴적 액션 시 Human-in-the-Loop (확인 프롬프트 강제):**
       - DB를 수정/삭제하는 액션(DELETE 쿼리, `equipment.db` 조작 등)이 요청될 경우, 브라우저 단에서 **사용자의 명시적 승인(버튼 클릭 또는 2FA)**을 받지 않으면 AI가 절대 단독으로 실행할 수 없도록 WebMCP 명세(Schema)에 `requires_human_confirmation: true` 로직을 하드코딩합니다.
    4. **[필수 안전망 3] AI 샌드박싱 및 롤백 체계 (Auditing & Soft-Delete):**
       - 에이전트가 실행한 모든 내역은 `audit_logs` 테이블에 `UserAgent`를 통해 "AI Agent"로 명확히 마킹되어 기록되어야 하며, 데이터 삭제 시 물리적 삭제(DROP, DELETE)를 전면 금지하고 Soft-Delete(`IsDeleted=1` 플래그)만을 허용하여 AI 폭주 시 즉각적인 롤백이 가능하도록 아키텍처를 방어합니다.

---

## [제안-038] 메타데이터 자동 라우팅 및 보안 텍스트(security.txt) 도입
  - **제안 일시:** 2026-08-16 11:30:00
  - **제안 모델:** Gemini 3.7 Flash (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 로봇(크롤러) 및 AI 스캐너 봇들의 무작위 스캐닝으로부터 미니서버의 불필요한 트래픽 및 404 에러 로그 생성을 막고, 프로젝트 소유권을 명확히 하는 표준 메타데이터 제공.
  - **구현 우선순위:** [중]
  - **구현 가능성:** [매우 높음] `Resources/metadata/` 디렉토리에 정적 파일을 위치시키고, `app.py` 구동 시 `os.walk`를 이용해 재귀적으로 라터를 자동 생성하면 구현 완료됩니다.
  - **관련 파일:** `app.py`, `Resources/metadata/*`
  - **영향도 및 의존성:** `app.after_request` 인터셉터 로직에 메타데이터 라우팅 추가 (O(1) frozenset 검사). 동적 라우팅 중복 생성(AssertionError) 방지 코드 적용.
  - **추정 난이도:** 하 (Low)
  - **제안 배경 (개요):** 외부 크롤러의 `robots.txt`, `security.txt` 등의 고정된 메타데이터 파일 탐색 시, Flask 서버가 일일이 대응하여 정적 리소스로 라우팅해주지 않으면 404 에러 로그가 양산됨.
  - **상세 내용:**
    1. `Resources/metadata/` 디렉토리 하위에 `.well-known/security.txt`, `security.txt`, `robots.txt`, `llms.txt` 작성.
    2. `security.txt` 내 연락처 이메일은 반드시 사용자 지정 하드코딩(`nekohost@nekohost.org`) 적용.
    3. `app.py` 기동 시, 해당 디렉토리를 탐색하여 모든 파일을 `send_from_directory`로 서비스하는 동적 라우팅 생성.
    4. HTTP 접근 로그(`access_logs`) 수집 시 `IsStatic=1`로 분류되도록 O(1) 정적 검사 풀(`frozenset`) 도입.

---

## [제안-039] 에러 유발 고유 IP 분석(Error IP Analysis) 대시보드 추가 (Staging-First 원칙 준수 필수)
  - **제안 일시:** 2026-08-18 12:20:00
  - **제안 모델:** Gemini 3.1 Pro (High)
  - **상태:** [채택 / 대기중]
  - **결정 사유:** 기존 `access_logs.html`은 로그가 단건 나열식이라 공격/스캐너 IP를 특정하기 어려워, 4xx/5xx 에러를 발생시킨 고유 IP만 추출하는 심층 분석 화면이 필수적입니다.
  - **구현 우선순위:** [중]
  - **구현 가능성:** [매우 높음] 완전히 새로운 테이블이나 백그라운드 엔진의 추가 없이, 기존 `access_logs` 테이블에 순수 SQL 쿼리(`GROUP BY`)만 호출하여 즉각적인 동적 렌더링이 가능합니다.
  - **관련 파일:** `app.py`, `templates/access_logs_error_ips.html` (신규), `templates/access_logs.html`
  - **영향도 및 의존성:** 기존 `access_logs.html` 헤더에 진입 버튼이 추가되며, 신규 템플릿과 API 함수(`/api/access_logs/error_ips`)가 추가됩니다. 권한은 `access_logs`의 메뉴 권한을 그대로 상속받습니다.
  - **추정 난이도:** 하 (Low) - 쿼리 작성과 프론트엔드 연동 수준.
  - **제안 배경 (개요):** 에러 발생 빈도가 높은 IP를 추적하여 직관적으로 모니터링하고 차단 조치를 취하기 위한 기반 화면 제공.
  - **상세 내용 및 🤖 AI 개발자 필수 준수 가이드라인 (절대 준수!):**
    > **주의:** 비교적 추론 지능이 낮은 AI가 이 문서를 읽고 작업을 진행하더라도 파국적 실수를 저지르지 않도록, `Rule.md`와 매핑된 매우 촘촘한 가이드를 제공합니다.
    1. **[기능 구현 내용 (No-Logic 원칙)]**:
       - `templates/access_logs_error_ips.html` 신규 작성. 화면 상단에 상위 IP 요약 차트 배치.
       - 데이터 테이블에는 IP 주소, 총 에러 건수, Client(4xx)/Server(5xx) 건수, 마지막 에러 발생 시간을 표출.
       - IP 주소 클릭 시, 기존 `access_logs.html` 화면으로 자동 딥링크 연동되어 해당 IP가 필터링/검색된 상태로 전환되도록 프론트 연동.
       - `app.py`에 별도 워커/엔진 추가 절대 금지. `WHERE StatusCode >= 400 GROUP BY IpAddress` 쿼리를 사용하는 API `/api/access_logs/error_ips` 1개 추가.
    2. **[Rule.md 제7-3조: 스테이징 격리 필수]**:
       - 절대로 루트 경로의 `app.py`나 `templates/`에 즉시 코드를 작성하지 마십시오.
       - 반드시 `Staging/app.py`를 복사 생성하고, `Staging/templates/access_logs_error_ips.html` 위치에 파일들을 생성하여 **안전한 모의 검증(Safety Net)**을 구축한 상태에서만 코딩을 진행하십시오.
    3. **[Rule.md 제5-1-1조: 윈도우 PC 런타임 구동 금지]**:
       - `Staging/app.py`를 만들었다고 해서 윈도우 터미널(PowerShell)에 `python Staging/app.py`를 실행하려 시도하지 마십시오. 정적 코드 리뷰만으로 검증하고 사용자에게 검토를 맡기십시오.
    4. **[Rule.md 제4-3조: 3대 주석 보존 수칙]**:
       - 추가되는 파이썬 함수 상단에 `[역할]`, `[의존성 관계]`, `[변경 시 영향도]` 3가지 주석 태그를 단 한 글자도 빠짐없이 명시하십시오. 기존 주석을 훼손해서는 안 됩니다.






---



2026-09-12 현행 구현 상태와 잔여 범위는 [15개 로드맵 묶음 상세 계획](Plans/2026/09/11/004_All_Unimplemented_Roadmap_Plan.md)을 따른다. 017·038·039는 관련 코드가 존재하므로 전체 신규 구현 대상으로 계산하지 않는다. 037은 제한적 조회 도구·수동 제출 폼이 구현되어 있으며 변경형 도구 확장은 별도 범위다. [DB 계약 및 기존 릴리스 적용 결과](Reports/2026/09/12/003_Database_Contract_Production_Release_Report.md)에 서버별 상태를 구분했다.

2026-09-13 제안 002·007·008·010·019·022/033·023·026의 승인된 구현을 백업 서버에 적용했다. 해당 대기 항목(중복 포함)은 미구현 목록에서 제거하고 원안·현재 명세는 PROPOSALS.md/ROADMAP.md 및 FEATURES.md에 보존했다. [검증·배포 결과](Reports/2026/09/13/003_Roadmap_Batch_Release_Report.md).
