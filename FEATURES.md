# 앞으로 구현할 기능 목록 (Will Implement)

이 문서는 장비 및 자산 통합 관리 시스템(`Mini-Server-Web-EqMgmt`)에 향후 추가할 기능들과 그 구현 우선순위를 기록합니다.

## 📌 기능별 구현 우선순위 및 내용

### [최상] 장비 상태(Status) 및 이력 관리
* **내용**: 장비의 현재 상태(예: 정상, 대여중, 수리중, 폐기 등)를 추적할 수 있는 상태 필드 추가 및 변경 이력 추적 기능.
* **이유**: 단순한 등록/조회를 넘어 자산의 실제 라이프사이클을 관리하는 것은 자산 관리 시스템의 핵심이므로 가장 먼저 구현되어야 합니다.

### [상] 상세 검색 및 필터링 기능 (API 단)
* **내용**: 카테고리, 제조사, 상태, 구입일자 등 다양한 조건으로 장비를 검색하고 필터링할 수 있는 기능. (백엔드 API 및 프론트엔드 UI 연동)
* **이유**: 장비 데이터가 누적됨에 따라 원하는 장비를 빠르게 찾기 위한 필수 기능이므로, 상태 관리 도입 이후 최우선적으로 구현합니다.

### [중] 마이페이지(회원정보 수정) 및 로그인 화면 비밀번호 찾기 기능
* **내용**: 
  1. **마이페이지**: 로그인한 사용자가 자신의 프로필을 조회하고 수정(비밀번호 변경 포함)할 수 있는 기능.
  2. **비밀번호 찾기**: 로그인 화면에서 비밀번호를 잊었을 때 본인 인증(이메일 등)을 거쳐 임시 비밀번호를 발급받거나 재설정(초기화)할 수 있는 기능.
* **이유**: 시스템 보안 및 기본 사용자 편의성을 위해 반드시 필요하지만, 현재는 사용자 규모가 작아 관리자가 수동 지원할 수 있으므로 계정 고도화 단계('중')로 책정합니다.

### [중] 회원탈퇴 및 철회(유예 기간) 기능 (Soft Delete)
* **내용**: 사용자가 회원탈퇴를 요청하면 즉시 DB에서 삭제하지 않고(Soft Delete) 일정 기간(예: 7일) 계정을 비활성화 상태로 유지. 기간 내 로그인 시 복구(철회)를 지원하며, 기간 경과 시 영구 삭제(Hard Delete) 처리. 영구 삭제 시 장비의 `OriginalOwnerName` 컬럼에 최종 탈퇴자의 이름을 기록하여 소유 이력을 보존함.
* **설계 시 주의사항**: 마이페이지에서 회원이 자신의 이름/닉네임을 변경할 경우, 장비 테이블에 기록/참조되는 소유자명 정보(`OriginalOwnerName` 등) 역시 동기화하여 일괄 변경되도록 로직을 검토하고 구현해야 함.
* **이유**: 사용자의 실수 방지와 개인정보 보호 측면에서 훌륭한 기능이나, 소규모 운영이라는 특성상 우선순위를 '중'으로 설정합니다.

### [하] 엑셀(CSV) 데이터 일괄 등록 및 다운로드
* **내용**: 엑셀 파일을 통해 다수의 장비를 한 번에 업로드(Import)하거나 현재 등록된 장비 목록을 파일로 다운로드(Export)하는 기능.
* **이유**: 대규모 장비 관리에 유용하지만, 개인 목적의 소규모 운영 환경에서는 당장 실사용 빈도가 높지 않으므로 도입을 잠시 미룹니다.

---
*참고: 위 기능들은 프로젝트 학습 목적(REST API 기본기)과 미니서버 구동 환경 최적화(경량화)를 고려하여 순차적으로 구현할 예정입니다.*
