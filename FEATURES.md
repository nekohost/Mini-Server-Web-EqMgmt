# 구현 기능 명세서 (Implemented Features)

이 문서는 장비 및 자산 통합 관리 시스템(`Mini-Server-Web-EqMgmt`)에 **현재 구현이 완료되어 실제 동작 중인** 기능들의 목록과 상세 명세입니다.

**현황 기준 (2026-09-13):** 현재 구동 대상은 백업 서버이며, 그곳에서 가동 중인 기능은 완료로 판정한다. 미니서버 미배포를 미완료 사유로 계산하지 않는다. 날짜가 붙은 절은 당시 변경 이력을 나타내며 최신 구현 계약은 뒤의 갱신 절을 따른다.

---

## 1. 인증 및 보안 (Authentication & Security)
- **세션 기반 로그인/로그아웃**: 
  - 기본 세션 만료 시간(30분) 설정.
  - 로그인 시 안전한 패스워드 해시(`werkzeug.security`) 검증.
- **CSRF (교차 사이트 요청 위조) 전역 방어 아키텍처**:
  - `@app.context_processor`를 통한 모든 템플릿 메타 태그(`X-CSRFToken`) 전역 주입.
  - 시스템 내 상태를 변경하는 모든 비동기 CUD(생성/수정/삭제) `fetch` 요청 시 토큰 필수 검증.
  - `@csrf_required` 데코레이터를 모든 CUD 전용 API에 적용하여 API 호출의 보안 무결성 확보.
- **다중 로그인 방어 (동시 로그인 제어)**: 
  - 동일 계정으로 다른 기기/브라우저에서 접속 시 기존 세션을 강제 만료 처리 (단일 로그인 강제).
- **백그라운드 세션 유효성 검사**: 
  - 프론트엔드에서 주기적(5초)으로 서버에 `/api/check_session`을 호출하여 세션 만료 혹은 타 브라우저 로그인 감지 시 즉각적인 자동 로그아웃 및 리다이렉트 처리.
- **보안 감사 로그 고도화 (Audit Logs, `/audit_logs`)**: 
  - 로그인 성공/실패, 로그아웃 및 주요 테이블(장비 등)의 CUD(생성/수정/삭제) 액션 발생 시 행위자, 대상, IP 주소, 변경 전/후 값 등 상세 정보 자동 기록.
  - RESTful 비동기 API (`/api/audit_logs`) 기반 전역 페이징(Pagination) 및 검색 시스템 구축.
  - 컬럼별 조건 검색 (`ActorLoginId`, `ActorName`, `Action`, `IpAddress`, `TargetId`, `Details`, `OldValue`, `NewValue`) 및 Match Type (Exact / LIKE) 지정 가능.
  - 페이지 당 보기 개수 설정 (50, 100, 150, 200, 300, 500 및 기타/직접 입력 max 10,000) 지원 및 `user_settings` DB JSON 영속화 자동 복원.
- **역할 기반 접근 제어 (RBAC)**: 
  - `user`(일반 사용자)와 `admin`(최고 관리자) 역할 구분.
  - `check_menu_permission` 데코레이터를 통한 메뉴 접근 권한 철저 검증.
- **이메일 인증 및 보안 메일링 시스템 (Mailing Service)**:
  - Microsoft Graph API (OAuth 2.0) 기반의 백그라운드 메일 발송 파이프라인 연동.
  - 신규 회원가입 및 마이페이지 이메일 변경 시 6자리 PIN 코드 발송 및 해시 검증을 통한 이메일 소유권 확인.
  - 비밀번호 분실 시 1회용 재설정 토큰(UUID 해시) 발급 및 보안 링크 전송 처리.

## 2. 사용자 및 권한 관리 (User & Role Management)
- **사용자 프로필 (마이페이지, `/mypage`)**: 
  - 로그인한 계정의 기본 정보 열람.
  - **프로필 정보(아이디 `LoginId`, 이름 `Name`, 닉네임 `NickName`) 수정 (`/api/users/update_profile`) [제안-034]**: 보안 유효성 검증을 위해 **현재 비밀번호(`current_password`) 검증**을 필수로 적용하고, 타 계정과의 아이디 중복 체크(`IntegrityError` 예외 방어) 및 세션 동기화(`session.modified = True`), 보안 감사 로그(`UPDATE_USER_PROFILE`) 기록 포함.
  - 사용자 본인의 **비밀번호 변경** 기능 제공.
- **어드민 전용 사용자 관리 (`/users_management`)**:
  - 시스템에 가입된 전체 유저 목록 및 가입일 조회.
  - 사용자의 권한(Role)을 `admin` 또는 `user`로 즉시 변경 가능.
  - 특정 유저의 **비밀번호 강제 초기화 (임시 비밀번호 직접 발급)** 기능 제공.
  - **계정 즉시 영구 삭제 (`/api/users/delete_selected`)**: 선택한 사용자 계정을 유예기간 없이 즉시 파기. 삭제된 계정의 설정(`user_settings`) 자동 정리 및 등록된 장비 소유권 안전 해제(공개 장비 전환). 보안 감사 로그(`DELETE_USER`) 자동 기록.
- **동적 메뉴 권한 관리 (`/permissions`)**:
  - 역할(Role)별로 포털 및 네비게이션에서 접근 가능한 메뉴를 런타임에 제어.
  - 체크박스 토글을 통한 즉각적인 메뉴 접근 권한 부여/회수.

## 3. UI 및 사용자 설정 영속화 (UI & Preferences)
- **개별 사용자 설정 영속화 (`user_settings` 테이블)**:
  - 브라우저나 디바이스가 달라지더라도 계정 단위로 유지되는 DB 기반 사용자 설정(Preferences) 저장 기능.
- **다크 모드 (Dark Mode) 완벽 지원**: 
  - 시스템 전체 화면(포털, 목록, 대시보드, 감사 로그 등)에 Tailwind CSS 기반 다크모드 대응 완료.
  - 상단 내비게이션의 테마 토글 버튼(🌗)을 통한 모드 전환 및 사용자 설정(DB) 동기화 지원.
  - 로컬 스토리지 캐싱을 통한 빠른 초기 로딩 깜빡임(FOUC) 방지.
- **목록 최상단 보기 유지 설정**:
  - 장비 목록의 '내 장비도 목록에 포함하여 최상단에 보기' 체크박스 상태를 사용자 설정으로 자동 저장 및 페이지 재진입 시 복원.
- **메뉴 이동 네비게이션 UX (`miniserver_frame.html` & `navigation.js`)**:
  - `document.referrer` 및 브라우저 백스택(`window.history.back()`)을 연동하여 서브 메뉴에서 상단 뒤로가기 클릭 시 직전 작업 메뉴로 복귀하는 스마트 네비게이션 구현.
  - 메인메뉴·이전으로·상위 메뉴를 한 그룹에 배치한다. 이전 이력이 없는 직접/외부/로그인 진입에서는 이전으로를 비활성화한다. 메인 포털에는 상위 메뉴 링크 자체를 렌더링하지 않는다. 관리자 하위 화면은 관리자 센터로, 에러 IP 분석은 접근 로그로 이동한다.

## 4. 장비 관리 핵심 기능 (Equipment Management)
- **공개 장비 및 나의 장비**: 
  - 전체 사용자에게 공개된 장비 목록과 본인이 등록한 장비 목록의 분리 열람 지원.
- **신규 장비 등록**: 
  - 모달 팝업 형태의 폼(Form)을 기반으로 간편하게 신규 장비를 추가하는 기능.
- **장비 목록 리스트**: 
  - `fetch` API 기반 비동기 데이터 통신 및 Vanilla JS 기반의 빠르고 동적인 화면 렌더링.

## 5. 포털 및 통계 대시보드 (Portal & Dashboard)
- **메인 포털 (Portal, `/portal`)**: 
  - 로그인 직후 진입하는 화면으로, 로그인 사용자의 권한이 허용된 메뉴 카드만 동적으로 불러와 표시하는 중앙 라우팅 허브.
  - 포털 최상단에 **나의 장비 수 / 총 관리 장비 수**를 직관적으로 보여주는 요약 위젯 제공.
- **상세 통계 대시보드 (`/dashboard`)**:
  - 시스템에 등록된 전체 장비 및 나의 장비 수량 통계 제공.
  - 카테고리별 장비 수량 분포를 **Chart.js 도넛 차트(Doughnut Chart)**와 데이터 테이블 포맷으로 시각화하여 동시 제공 (다크모드 테마 실시간 대응 완료).
  - 제조사별 장비 수량 분포 및 통계 제공.
  - 카테고리 및 제조사를 지정하여 해당 조건에 부합하는 장비 목록을 조회하는 복합 조건 검색 기능 제공 (임시 등록 장비 제외).

## 6. 마스터 데이터 및 결재 시스템 (Master Data & Workflow)
- **마스터 데이터 (카테고리/제조사) 동적 관리 및 고도화**:
  - 장비 등록 시 사용되는 카테고리 및 제조사를 DB(`categories`, `manufacturers`) 기반으로 동적 렌더링.
  - **신규 마스터 항목 추가 모달**: 관리자가 카테고리/제조사를 직접 신규 등록할 수 있는 전용 범용 모달(`itemModal`) 지원 (`POST /api/master/manage/<target_type>`).
  - **체크박스 기반 일괄 삭제 기능**: 전체 선택/개별 선택 체크박스를 통해 여러 마스터 항목을 한 번에 안전하게 삭제 처리 (`POST /api/master/manage/<target_type>/delete_selected`). 삭제 시 관련 장비 외래키(`CategoryId`, `ManufacturerId`) 및 레거시 텍스트는 `NULL`로 무결하게 초기화 및 감사 로그(`DELETE_MASTER_SELECTED`) 기록.
  - **마스터 데이터 통폐합 (Merge)**: 기존 개별 마스터 항목들을 기준 항목으로 통합 흡수 및 연결 장비 분류 ID 일괄 갱신.
- **마스터 데이터 추가 전자결재(승인) 시스템**:
  - 사용자가 '기타'를 선택하여 신규 마스터 데이터를 입력할 경우 즉시 반영되지 않고 '결재 요청(`approval_requests`)' 상태로 전환됨.
  - 관리자는 결재 대시보드에서 승인/반려(사유 포함) 처리 가능하며, 승인 시 정식 마스터 데이터로 등록됨.
- **장비 임시저장(Draft) 기능**:
  - 마스터 데이터 결재 승인 대기 중이거나 정보가 부족한 장비를 '임시저장' 상태로 보관 가능 (`IsDraft`).
  - 임시저장함에서 별도 관리하며 정식 장비로 언제든 전환(수정) 가능.

## 7. 시스템 전역 상태 제어
- **전역 로딩 오버레이 (Global Loading UI)**:
  - 장비 저장/수정/삭제, 사용자 설정, 마스터 데이터 통폐합, 결재 승인 등 시스템의 모든 비동기 작업(fetch) 시 발생하는 딜레이 동안 화면 전체를 덮는 딤(Dim) 처리 및 로딩 스피너 표출.
  - 작업 진행 중 사용자의 중복 클릭 및 예기치 않은 이탈을 원천 차단하여 시스템 안정성 및 UX 극대화.

## 8. 사용자 라이프사이클 및 계정 제어 (User Lifecycle)
- **4단계 회원탈퇴 및 철회 라이프사이클 (Soft Delete & Lazy Evaluation)**:
  - **Phase 1 (30일 유예)**: 탈퇴 신청 시 즉시 계정이 비활성화되며 모든 기기에서 자동 로그아웃됨. 30일 이내 로그인 시 비활성화 철회(계정 복구) 전용 안내 페이지(`/deactivated_notice`)만 접근 가능.
  - **Phase 2 (30일 경과)**: 탈퇴 신청 30일 경과 시 `IsDeleted='Y'` 상태로 전환되어 로그인 전면 차단.
  - **Phase 3 (1년 파기)**: 탈퇴 신청 1년(365일)+1일 경과 시 DB에서 계정 정보 완전 파기(Hard Delete) 및 소유 장비 공개 전환.
  - **Phase 4 (재가입 복구)**: 탈퇴 완료 계정의 ID로 신규 가입 시도 시, 보안 소유권 증명(기존 이름/비밀번호 검증)을 거쳐 새 비밀번호로 계정을 복구하는 기능 제공.
  - **상태 지연 평가(Lazy Evaluation)**: 배치 데몬 없이 사용자가 로그인하거나 관리자가 목록을 조회할 때만 일수를 계산하여 실시간으로 DB 상태를 갱신(상태 전이)하는 서버 부하 제로(Zero) 아키텍처.
- **관리자 전용 계정 무기한 정지 (Deactivation Toggle)**:
  - 사용자 관리 화면에서 특정 사용자의 계정을 선택하여 무기한 비활성화(정지) 또는 재활성화 가능. 카운트다운(유예기간)이 적용된 자진 탈퇴와 명확히 분리되며, 단일 및 다중 선택 일괄 제어 지원.
- **[제안-035] 관리자 전용 메뉴 허브(Admin Center) 분리 및 N-Depth 권한 시스템**:
  - 관리자용 특수 메뉴들을 포털 메인 라우팅에서 분리하여 새로 신설되는 `/admin_center`에만 노출. 포털에는 기존과 동일한 모양의 "관리자 센터" 진입점 버튼만 렌더링.
  - DB `menus` 테이블에 `ParentMenuCode`, `SortOrder`를 추가하여 무한 깊이(N-Depth)의 메뉴 계층을 표현.
  - 프론트엔드 및 백엔드에 재귀 함수를 내장하여, 하위 메뉴 접근 시 상위 메뉴의 권한 활성화를 자동 강제하는 완벽한 무결성 검증 추가 적용.

- **[제안-036] 가변 깊이 모델 트리 & 3-Tier 장비 계층 아키텍처 (3-Tier Equipment Hierarchy)**:
  - **1차 마스터 & 가변 깊이 라인업 트리 & 옵션 스펙 계층 분리**:
    - **1차 마스터**: `categories`(카테고리), `manufacturers`(제조사)
    - **1~N차 노드 (`lineup_nodes`)**: 최대 50계층(MAX_DEPTH=50)까지 지원하는 가변 자기참조 트리 구조. 루트 노드(`depth=1, parent_id=NULL`)부터 서브 라인업까지 유연한 제품군 계층 모델링 및 순환 참조(Circular Reference) 방어.
    - **N+1차 옵션 스펙 (`equipment_options`)**: 최종 라인업 노드에 종속되는 사양/스펙 조합(`option_name`, `specs_json` Key-Value 속성).
    - **자산 본체 (`equipments`)**: 실제 시리얼 넘버, 구입일, 상태, 소유자 정보를 보유하는 실물 인스턴스 테이블로 완전 분리 정규화.
  - **카탈로그 트리 전역 단일 API (`GET /api/lineup_tree_all`)**:
    - 모든 카테고리, 제조사, 라인업 노드, 옵션 목록을 단일 JSON으로 통합 수신하여 프론트엔드 세션 스토리지(`nodeCache_v2`)에 캐싱함으로써 네트워크 트래픽 및 렌더링 지연 최소화.
  - **프론트엔드 가변 셀렉트박스 & 제출 잠금 UI (`index.html`)**:
    - 카테고리/제조사 선택에 따라 하위 N차 라인업 노드 및 N+1차 옵션 스펙 셀렉트박스가 동적으로 연쇄 생성/제거되는 `dynamicTreeContainer` 및 `dynamicOptionContainer` 렌더링.
    - 최종 옵션까지 올바르게 선택되지 않은 경우 정식 등록 버튼을 비활성화하는 안전 제출 잠금(Submit Lock) 시스템 적용.
  - **전자결재함 3-Tier 연동 (`approvals.html`)**:
    - 신규 라인업 노드(`ADD_LINEUP_NODE`) 및 옵션(`ADD_EQUIPMENT_OPTION`) 추가 요청에 대한 결재 문서 구분 뱃지 표출 및 관리자 승인/반려 모달 워크플로우 연동.
  - **장비 감사 로그 (`equipments_audit_log`)**:
    - 장비 데이터 생성, 변경, 마이그레이션 이력을 추적 보존.
  - **DB 안전 마이그레이션 및 롤백 스크립트 제공**:
    - `db_migration.py`: 기존 단일 테이블 선행 영구 백업, 3-Tier 스키마 DDL 생성, 외래키 B-Tree 인덱스 3종 생성, 3-Tier 정방향 이관.
    - `down_migration.py`: 비상 시 3-Tier 데이터를 1-Tier 단일 테이블 형태로 평탄화 복원하는 역방향 롤백(Memo 내 옵션 스펙 압축 보존).

## 9. 실시간 인프라 및 트래픽 관제 (Infrastructure & Traffic Monitoring)
- **[제안-036] 실시간 웹 접근 로그 모니터링 시스템 (HTTP Access Logs, `/access_logs`)**:
  - **비동기 큐 & 단일 워커 아키텍처 (SD카드 I/O 95% 절감)**: 매 요청마다 DB에 직접 쓰지 않고 `queue.Queue(maxsize=10000)` 및 `put_nowait()`로 인메모리 수집하여 웹 응답 지연 0ms(Fail-Open) 보장. 백그라운드 단일 워커가 0.5초 또는 최대 50건 단위로 `executemany` 벌크 커밋 수행.
  - **완전 무결점 Graceful Shutdown**: `threading.Event` 종료 신호와 `get(0.5s)` / `join(3.0s)` 타이밍 조율을 통해 서버 재시작/종료 시 잔여 큐 데이터를 100% DB에 일괄 커밋 후 안전하게 스레드 종료.
  - **인터셉터 Fail-Safe 완전 격리**: `@app.after_request` 인터셉터 내부의 모든 로깅 데이터 파싱 과정을 최상위 `try...except`로 감싸, 헤더 변조나 예외 발생 시에도 웹 본래 응답(200 OK 등)을 100% 정상 반환하여 가용성 확보.
  - **트래픽 및 리소스 분리 수집**: 내부 세션 폴링(`/api/check_session`)은 로깅에서 제외하며, `/static/*` 및 `/favicon.ico` 등 정적 리소스 요청은 `IsStatic=1` 플래그로 분리 수집하여 API/웹 요청과의 분류 관제 지원.
  - **SQLite WAL 모드 및 동시성 락 방어**: `PRAGMA journal_mode = WAL;`, `PRAGMA busy_timeout = 5000;`을 적용하여 다중 요청 및 워커 커밋 간 DB Lock 경합 방어.
  - **관리자 전용 관제 대시보드 UI (`templates/access_logs.html`)**:
    - **4종 트래픽 요약 카드**: 오늘 총 요청 건수, 웹/API 요청 건수, 정적 리소스 건수, 에러 응답률(4xx/5xx) 실시간 집계.
    - **3단 퀵 필터 칩**: `[🌐 전체]` / `[⚡ 일반 웹/API만]` / `[📁 정적 리소스만]` 원클릭 뷰 전환.
    - **상세 조건 검색**: IP 주소, HTTP 메서드(GET, POST, PUT, DELETE, HEAD), 상태 코드 그룹(200, 302, 404, 415, 500, 4xx, 5xx), 요청 경로(Path) 복합 필터링.
    - **실시간 자동 새로고침(Auto-Refresh)**: 5초 / 10초 / 30초 / OFF 카운트다운 타이머 지원 (조용한 백그라운드 갱신).
    - **안전한 수동 클렌징 도구**: 30일 이전 로그 정리, 정적 리소스 로그 비우기, 전체 로그 완전 초기화 3종 메뉴 제공. 
    - **[제안-044] 대용량 로그 청크 단위 삭제**: 대량 삭제 시 발생하는 SQLite Lock Timeout 방지를 위해 250건 단위로 `DELETE` 청크를 분할하여 처리. 서브쿼리에 `ORDER BY CreatedAt ASC`를 명시하여 무조건 오래된 항목부터 확실하게 삭제되도록 논리적 무결성을 보장함. 프론트엔드 비동기 루프로 삭제 퍼센티지(%) 진행률을 실시간 표출하여 응답성을 유지함.
- **[제안-040] 웹 접근 로그 상세 기록 확장 (Request & Response Payload 로깅)**:
  - **무제한/무마스킹 Body 원본 데이터 수집**: `@app.after_request` 인터셉터에서 Request Body 및 Response Body를 용량 제한이나 마스킹 없이 원본 그대로 추출하여 `access_logs` 테이블의 `RequestPayload`, `ResponsePayload` 컬럼에 적재.
  - **프론트엔드 클립 뱃지 & 행 클릭 상세 모달 (`access_logs.html`)**:
    - 로그 테이블 행에서 Payload가 존재하는 경우 종이클립(`fa-paperclip`) 아이콘 뱃지 표출 및 행 강조 스타일 적용.
    - 행 클릭 시 `openPayloadModal()`을 통해 Request Payload와 Response Payload를 JSON 포맷팅(pretty-print)하여 독립 모달에서 한눈에 분석할 수 있는 심층 트러블슈팅 UI 제공.
- **[제안-041] 웹 접근 로그 요약 통계 기간 필터 확장 (오늘/전체 누적 토글)**:
  - **백엔드 통계 API 기간 파라미터 분기 (`GET /api/access_logs/stats?period=today|all`)**:
    - `period=today` (기본값): 당일 00:00:00 이후 데이터에 대한 집계 쿼리 실행.
    - `period=all`: 전체 `access_logs` 테이블에 대한 집계 쿼리 실행.
  - **상단 요약 카드 세그먼트 컨트롤 UI (`access_logs.html`)**:
    - 요약 카드 섹션 헤더에 `[ 📅 오늘 ]` / `[ 🌐 전체 누적 ]` 세그먼트 버튼 배치.
    - 버튼 클릭 시 비동기(`fetchStats()`)로 통계 데이터를 즉시 재조회하고, 카드 레이블(`오늘 총 요청` ↔ `전체 총 요청`) 및 섹션 타이틀을 동적으로 동기화.
- **[제안-042] Python 3.14 런타임 안정화 및 Reloader 세마포어 크래시 방어**:
  - **서브프로세스 IPC 세마포어 누수 원천 차단 (`app.py`)**:
    - `app.run()` 구동 시 `use_reloader=False`를 명시하여 Werkzeug Auto-Reloader에 의한 자식 프로세스 생성을 억제하고, Python 3.14의 `multiprocessing.resource_tracker` 세마포어 감시 충돌로 인한 서비스 무음 크래시를 완벽히 해결.
  - **서버 런타임 시작 로그 및 예외 가시성 확보 (`try...except`)**:
    - 서버 시작 시 `[Server Startup] Starting Flask on http://0.0.0.0:5000 (debug=..., reloader=False)...` 안내 문구를 콘솔에 출력.
    - 포트 충돌(`OSError: [Errno 98] Address already in use`)이나 소켓 바인딩 에러 발생 시 `[Server Fatal Error]` 및 `traceback.print_exc()`로 스택 트레이스를 즉시 터미널에 출력하여 신속한 장애 파악 지원.
- **[제안-043] 접근 로그 API 자기 참조 ResponsePayload 재귀 루프 차단**:
  - **인터셉터 자기 참조 차단 조건 적용 (`app.py`)**:
    - `@app.after_request` 인터셉터에서 `not is_static and not request.path.startswith('/api/access_logs')` 조건식을 적용하여, `/api/access_logs` (목록 조회), `/api/access_logs/stats` (통계), `/api/access_logs/cleanup` (삭제), `/api/access_logs/error_ips` (에러 IP 분석) 등 접근 로그 자체의 응답 Body가 `ResponsePayload`에 적재되지 않도록 원천 차단.
  - **DB 리소스 및 서비스 가용성 보존**:
    - 5초 자동 새로고침 시 거대한 목록 JSON이 중첩 적재되는 자기 참조 재귀 루프를 차단함으로써 `equipment.db`의 비대화 및 쿼리 지연을 방어하고, 로그 삭제 시 SQLite 배타적 파일 락 교착(Deadlock)과 서비스 크래시를 방지.
- **[제안-045] 웹 접근 로그 페이로드 지연 로딩(Lazy Loading) 최적화**:
  - **초기 목록 조회 대역폭 및 DB 부하 경량화 (`app.py`)**:
    - `/api/access_logs` 엔드포인트에서 50건의 로그 목록 조회 시 거대한 `RequestPayload` 및 `ResponsePayload` 본문 컬럼을 `SELECT`에서 제외하고, `CASE WHEN RequestPayload IS NOT NULL AND RequestPayload != '' THEN 1 ELSE 0 END AS HasRequestPayload` 및 `HasResponsePayload` 경량 플래그(0/1)만 조회하도록 쿼리 튜닝.
    - 초기 목록 로딩 시 네트워크 페이로드 크기 및 메모리 사용량을 90% 이상 절감하여 저사양 미니서버의 응답 지연을 방어.
  - **단건 페이로드 온디맨드 API 신설 (`GET /api/access_logs/<int:log_id>/payload`)**:
    - 관리자가 특정 로그 레코드의 상세 페이로드를 확인하려 할 때만 해당 `LogId`의 1건 데이터만 DB에서 비동기 조회하여 반환하는 전용 엔드포인트 구축.
    - `@login_required` 및 `check_menu_permission('access_logs')` 접근 통제와 SQL 바인딩 파라미터 적용으로 보안 무결성 확보.
  - **프론트엔드 비동기 모달 뷰어 및 로딩 UX 구현 (`access_logs.html`)**:
    - 테이블 목록에서 `log.HasRequestPayload` 및 `log.HasResponsePayload` 플래그를 통해 종이클립 아이콘 표출 및 행 강조 스타일을 완벽하게 유지.
    - 행 클릭 시 `openPayloadModal()`이 즉각 모달을 열고 "불러오는 중..." 스피너를 노출한 뒤 비동기 `fetch`로 단건 페이로드를 수신하여 JSON 포맷팅(`pretty-print`) 렌더링 수행.






## 10. DB 계약 및 무결성 (2026-09-12)

- 정상 DB 연결마다 외래 키를 강제하며 마스터 삭제·병합과 감사를 같은 트랜잭션에서 처리한다. 사용 중인 마스터의 삭제는 참조 건수와 함께 차단한다.
- 스키마 버전 1과 의미 기반 백업 호환성 비교를 제공한다. 컬럼 물리 순서 차이는 허용하고 실제 제약·인덱스 차이는 거부한다. 현재 3-Tier 필수 테이블을 사용하며 레거시는 1차 전환 동안 보존한다.
- 실제 조회에 맞는 인덱스 4개와 사전 백업·행 보존 확인·down/up 검증 도구를 제공한다.
- 제안 046 점검 모드는 사용자 정상 작동 확인을 받은 개발 완료 기능이다. 제안 013 백업/복원과 047 노드/옵션 기능은 Linux 회귀와 백업 서버 적용까지 확인했다.
- 적용 상태: 백업 서버 실행·점검 NORMAL. 현재 구동 대상에 적용되었으므로 완료다. 미니서버는 현재 배포 완료 조건에 포함하지 않는다.
- [운영 안내](docs/database-contract-operation.md), [검증 보고서](Reports/2026/09/12/003_Database_Contract_Production_Release_Report.md).

## 11. AI 거버넌스 Context 입력·진단 (2026-09-12)

- 운영 거버넌스 1.6.0: migration/UI 필수 규칙 선택과 경로 검증을 분리하여 Staging 후보만 선언해도 필요한 보호 노드를 포함한다.
- 실제 작업 대상 `--path`와 읽기 전용 참고·영향 대상 `--reference-path`를 구분한다. 경로 선언과 context 성공은 수정 승인이 아니다.
- 미등록 intent, 등록된 intent의 경로 불일치, 미분류 경로, 외부 scope 누락, section·정책 오류를 구조화 진단으로 구분한다.
- 실패 시 일반 구현은 계속 차단한다. 기존 승인 범위의 읽기 전용 진단과 근거 있는 입력 정정 후 validate·새 context·전체 pack 읽기를 통과해야 재개한다.
- Rule 9-1·9-3, 네 플랫폼 진입점, 노드·추적성·도구·안내 문서를 함께 동기화한다. 기존 안전 조건이나 컨텍스트 예산을 완화하지 않는다.
- [검증 보고서](Reports/2026/09/12/005_Governance_Context_Clarity_Report.md). 장비 공식 모델명 기능과 운영 DB 변경은 이번 반영에 포함하지 않는다.

## 12. 제안 일괄 구현 및 백업 서버 배포 (2026-09-13)

운영 코드 a08f52b 및 예약 구성 bee319e를 백업 서버에 적용했다. 현재 DB 계약은 v3이다. 아래는 2026-09 승인된 실제 구현 범위이며 과거 제안의 예시 테이블/방식보다 우선한다.

| 제안 | 사용 위치와 구현 계약 |
|---|---|
| 002 | 가입·프로필·비밀번호·장비·마스터/노드/옵션의 중앙 입력 검증. 실제 날짜/길이/타입/JSON 크기 검증, 신규 비밀번호 8~256자 영문·숫자·특수문자. 기존 로그인 유지. |
| 007 | SQLite 공유 인증 버킷, 계정+IP 및 IP 한도, 429/Retry-After. 로그인 10/60회·15분이며 API별 분리 정책·자동 만료. 신뢰된 proxy peer만 전달 IP 사용. |
| 022·033 | 상태/첨부 패널: ACTIVE/LOANED/REPAIR/DISPOSED 전이, 사유·현재 revision 필수, 동일 transaction 감사, 소유자·관리자 전용 이력. 대시보드 실제 상태 집계. |
| 023 | 내/공개 범위와 복합 필터 AND 결합, 서버 페이지/정렬, URL 복원. 공식 모델명·전체 노드 경로·제조사 별칭 검색. 기존 배열 API는 유지. |
| 010 | 상태/첨부 패널에서 PNG/JPEG/PDF 업로드·권한 다운로드·논리 삭제. static 외부 0700/0600·UUID 키·10MiB/파일·50개/장비·보관본 포함512MiB/업로더. 전체 ZIP은 DB·보관 첨부·manifest·해시 포함(2GiB). |
| 008 | 등록/수정/목록/상세의 보증 종료일·교체 예정일과 KST 배지. 마이페이지에서 검증된 본인 이메일의 수신 동의. 30/7/1일 전·당일만 발송, 일별 원장/단일 lock/UNKNOWN 자동 재발송 금지. 백업 서버 매일09:00 예약 가동. |
| 019 | 마이페이지 Standard/Edge 선택과 PreferencesJSON 영속화. 공통 CSS/동일 기능·테마 유지. |
| 026 | 목록 상단 CSV 양식·검색 결과 export·파일 미리보기·명시 확정. UTF-8/BOM, 500행/1MiB import, 승인된 OptionId와 본인 소유, 원자적 적용·동일 토큰 재시도, 수식 접두사 방어. export 최대10,000건. |

- 기존 제조사+공식 모델명, 수정 시 카탈로그 선택 복원, 옵션 관리, 점검/DB-only 백업·복원을 회귀 검증했다.
- schema v3는 기존 업무 테이블/컬럼/값을 보존하는 확장이다. 업무 데이터가 생긴 v3를 구버전 DB로 덮어쓰거나 자동 down하지 않는다. private snapshot과 현재 첨부를 보존하고 v3 호환 수정 전진으로 복구한다.
- 전체 ZIP 복구 준비는 tools/prepare_equipment_full_restore.py로 새 격리 폴더에서 검증한다. 기존 파일 충돌을 덮어쓰지 않으며 실제 DB 복원은 기존 관리자 인증·후보 검증 절차를 따른다.
- 첨부 서명 검사는 백신/완전한 PDF 파서가 아니다. inline preview 대신 attachment/nosniff로 내려주며 물리 purge는 별도 보존 정책 없이 실행하지 않는다. xlsx 바이너리, WAF/fail2ban, 변경형 WebMCP, systemd 서비스화는 이 릴리스 범위가 아니다.
- 검증: Windows 정적/JS31건, Linux 격리86건 통과. 실제 DB 사본 및 서비스 전환 후 기존16개 테이블 원래 행 보존, integrity/FK 정상. 도메인 정적 파일 해시 일치·인증 필요 API401·점검 NORMAL 확인. 알림 dry-run 대상0/실제 발송0.
- 사용자 계정으로 실제 브라우저/모바일 조작이나 실제 이메일 수신은 대신 확인하지 않았다. 주 서버는 배포 대상이 아니며 백업 서버 적용과 구별한다.
- [운영 계약/한도](Reports/2026/09/13/003_Roadmap_Batch_Release_Contract.md), [검증·복구·배포 보고서](Reports/2026/09/13/003_Roadmap_Batch_Release_Report.md).

### 12-1. 제안 019 Edge 후속 개선 (2026-09-14, 백업 서버 배포·테스트 계정 확인 완료)

- 카드의 기본 크기·여백·글자 크기를 유지하고 가용 폭으로 열 수를 계산한다. 포털은 1440/1920/2560px에서 4/5/7열로 검증했다. 장비·로그·관리 표와 노드 목록은 넓게, 필터·차트·입력 폼은 적정 최대 폭으로 제한한다.
- 마이페이지는 1200px 이상 Edge에서 좌우 2열, 좁은 화면과 Standard에서는 1열이다. 테마·레이아웃·기한 알림을 환경 설정 패널로 통합했다. 테마 즉시 저장/레이아웃·알림 버튼 저장, 기존 프로필·인증·비밀번호·탈퇴 기능은 유지한다.
- 같은 날 가운데 배치 누락을 보정했다. 폭 제한 패널·필터·분포 영역은 좌우 자동 여백, 메뉴/요약 카드는 빈 열을 접은 고정 폭 그리드의 가운데 배치를 사용한다. 카드 크기·글자 정렬·넓은 표는 유지한다. CSS만 무재시작 배포하고 기존 테스트 계정의 31개 폭/상태에서 중심·넘침·기존 기능을 재확인했다.
- 모바일 내비게이션 줄바꿈·긴 닉네임·세션 표시를 보완했다. 로그인·점검·복원 폼과 편집 모달의 기존 폭/안전 절차는 유지한다. Standard의 기존 컨테이너/카드 배치도 보존하되 설정 패널 통합은 공통 적용한다.
- 검증: 합성 데이터로 실제 템플릿 16개 화면의 Edge 8개 폭 및 Standard 기본 폭, 저장 모의 응답/기존 프런트엔드 회귀 포함 Node 40건 통과. 이후 UI 13개 파일을 백업 서버에 배포했고 실제 nekohost.org에 기존 일반 사용자 테스트 계정으로 로그인해 31개 폭/상태, 저장·새로고침 영속화·다크/라이트·Chart.js를 확인했다. 업무 데이터와 계정 권한/비밀번호는 보존됐고 테스트 설정 복원·로그아웃 완료. 관리자 전용 화면의 실접속은 제외한다. Git commit/push와 주 서버 배포는 수행하지 않았다. [구현·배포·실제 검증 화면](Reports/2026/09/14/003_Edge_Layout_Improvement_Report.md).

### 12-2. 메뉴 이동·검색·카드 및 세로 화면 후속 개선 (2026-09-14)

- 기존 제안 019·020·023의 개선으로 기록한다. Standard/Edge 모두 상단 이동 버튼을 인접 배치하고, 최상위 포털의 상위 메뉴는 생성하지 않는다.
- 나의/공개 장비 검색은 검색어 중심 일반검색이 기본이다. 조건검색/일반검색 버튼으로 상세 조건을 전환한다. 일반검색에서는 숨겨진 조건을 조회·CSV·URL에서 제외하고, 같은 화면에서 다시 펼치면 입력값을 복원한다. 기존 상세 조건 URL은 조건검색으로 복원한다.
- 포털/관리자 메뉴 카드 설명은 3줄 높이(60px)로 통일하고 넘치는 문장은 말줄임한다. 마우스 hover 시 native title로 전체 설명을 제공한다. 설명은 HTML이 아닌 텍스트로 렌더링한다.
- 스마트폰 폭에서는 검색창 한 줄 + 검색/모드 버튼 한 줄, 헤더 높이는 내용에 따라 확장한다. 고정 높이·중복 여백으로 생긴 본문 겹침을 수정했고 장비 작업 버튼·프로필 제목/버튼도 단어 중간 대신 요소 단위로 줄바꿈한다.
- 백업 서버 배포 완료. 기존 일반 테스트 계정으로 PC 중심 37개 상태, 터치 모바일 에뮬레이션의 Standard/Edge 5화면 × 세로 4크기 및 상세검색 16상태(총56)를 검증했다. 320×568·360×800·390×844·412×915에서 헤더/본문 겹침·가로 넘침 없음, 최상위 링크 부재와 터치 이동/검색 확인. 설정 복원·로그아웃 완료. 실기기 및 Safari 검증은 아니다. [구현·보정·검증 보고서](Reports/2026/09/14/004_Navigation_Search_Card_UX_Report.md).

### 12-3. 상단 영역 너비·3영역 정렬·CSV 접기 (2026-09-14)

- Edge에서도 상단바 내부는 Standard의 최대1,280px와 반응형 좌우 여백을 사용한다. 페이지 제목/관련 작업 버튼은 해당 화면의 Standard 컨테이너 폭에 맞춰 가운데 배치한다. 장비·관리·로그 표의 Edge 넓은 영역과 기존 카드 배치는 유지한다.
- 이동 버튼은 왼쪽, 현재 메뉴명은 정확한 중앙, 테마·세션·계정·로그아웃은 오른쪽이다. 최초의 1,024px 미만 세로 3영역 배치는 같은 날 12-4절의 좌우 그룹 내부 2줄 구조로 대체했다. 최상위 포털의 상위 메뉴 생략은 유지한다.
- 장비 화면은 CSV 작업을 기본으로 숨긴다. 검색/조건검색 옆의 CSV 기능 버튼으로 검색 결과 CSV·CSV 양식·CSV 가져오기를 펼치고 CSV 닫기로 접는다. 검색 모드와 독립적이며 펼침 자체로 조회·다운로드·업로드를 수행하지 않는다. 가져오기 모달과 명시적 미리보기/확정 절차는 유지한다.
- 백업 서버 배포 완료. 44개 정적/브라우저 회귀, 실제 테스트 계정의 PC 중심37개 상태 및 터치 세로72개 상태를 통과했다. 320×568·360×800·390×844·412×915에서 제목 중앙·좌우 영역·CSV 접기·본문 겹침/넘침을 확인했다. 실기기/Safari 검증은 아니다. [구현·배포 보고서](Reports/2026/09/14/005_Header_Boundary_and_CSV_Disclosure_Report.md).

### 12-4. 상단바 좌우 그룹 내부 2줄 배치 (2026-09-14)

아래는 당시 구현 이력이다. 600px 미만의 제목 상단 분리·오른쪽 배치는 같은 날 12-6절의 중앙 제목/좌우 3줄 구조로 대체했다. 600px 이상의 2줄 그룹 구조는 유지한다.

- 메인메뉴 버튼은 왼쪽 그룹의 세로 중앙에, 바로 오른쪽에는 이전으로/상위 메뉴를 위아래로 배치한다. 최상위 포털은 상위 메뉴 요소가 없으므로 이전으로만 메인메뉴 바로 옆의 세로 중앙에 놓인다. 기존 이력/부모 경로와 비활성 조건은 유지한다.
- 오른쪽은 테마·세션과 계정·로그아웃을 두 줄로 나누어 오른쪽 정렬한다. 600px 이상은 좌/중앙/우를 같은 행에 유지하고 동일 폭 좌우 트랙으로 제목을 정확한 중앙에 둔다. 600px 미만에서만 제목을 위로 보내며 좌우 그룹은 다음 행에 나란히 유지한다. 상단 전체의 세로 3영역 강제 배치는 제거했다.
- Standard의 상단 최대1,280px/여백, Edge의 넓은 표·카드, 페이지 제목 경계, 검색/CSV 접기는 유지한다. 360px 미만은 장식용 이동 아이콘만 숨기고 버튼 문구/기능은 유지한다. 긴 닉네임·역할은 영역 안에서 말줄임하고 계정 링크 title에 전체 값을 남긴다. 만료 임박 세션 안내는 그룹 안에서 줄바꿈한다.
- 백업 서버에 UI 2개 파일 반영 완료. 정적/브라우저 회귀44건, 실제 테스트 계정 PC 중심49상태, 터치 세로72상태 통과. 16개 실제 템플릿 × 9폭 × 두 스킨 검사와 599/600px 경계·긴 계정명/세션 경고 검사를 포함한다. 업무 데이터·계정 권한 보존 및 테스트 설정 복원/로그아웃 완료. 모바일은 에뮬레이션이며 실기기/Safari 검증은 아니다. [구현·배포·검증 보고서](Reports/2026/09/14/006_Compact_Navigation_Report.md).

### 12-5. 표 제목·버튼 문구 가독성 (2026-09-14)

- 메뉴 표와 CSV 미리보기의 컬럼 제목은 한 줄을 유지한다. 제목 자체의 필요한 폭을 컬럼 최소 폭 계산에 반영하고, 좁은 화면은 기존 표 내부 가로 스크롤로 확인한다. 제목을 글자 단위로 세로 분할하거나 말줄임하지 않는다. 데이터 셀의 기존 긴 문장/URL 줄바꿈과 컬럼 숨김 기능은 유지한다.
- 버튼과 버튼형 링크는 단어 중간 분리를 피하고 여러 줄이 필요하면 줄 길이를 균형 있게 조정한다. 기존 명시적 한 줄 버튼은 유지하며, 글자 수를 강제로 맞추기보다 단어 보존을 우선한다. 지원하지 않는 브라우저에서도 단어 단위 줄바꿈은 유지하고 균형 배분만 일반 줄바꿈으로 대체된다. 단어 자체가 영역보다 긴 극단적 경우에는 넘침 방지용 분할을 허용한다.
- 320px의 장비 등록·마스터 편집 팝업은 버튼 요소를 다음 행으로 옮겨 문구가 압축되거나 잘리지 않게 한다. 이메일 수정 입력칸은 줄어들 수 있도록 해 발송/확인 버튼 공간을 보존한다. 버튼 이벤트·저장·권한 로직은 변경하지 않는다.
- 백업 서버에 CSS/마스터 관리 템플릿 2개 파일 반영 완료. 정적/브라우저 회귀47건 및 마지막 버튼형 링크 선택자 변경의 대상 검사 통과. 실제 일반 테스트 계정 PC 중심49상태, 터치 세로72상태와 팝업12상태 통과. 데이터·권한 보존과 설정 복원/로그아웃 완료. 모바일은 에뮬레이션이며 실기기/Safari 검증은 아니다. [구현·배포·검증 보고서](Reports/2026/09/14/007_Table_and_Button_Readability_Report.md).

### 12-6. 좁은 화면 중앙 제목·우측 작업 버튼·관리자 UI (2026-09-14)

이 절은 당시 구현 이력이다. 닉네임 대신 내 정보 표기와 관리자 그룹별 새 행 시작은 아래12-8절에서 대체했다. 중앙 제목·선택 버튼 동작은 유지한다.

- 두 스킨 모두 600px 미만에서도 상단바는 좌/중앙/우를 유지한다. 왼쪽은 메인메뉴·이전으로·상위 메뉴를 세로로, 오른쪽은 로그아웃·내 정보·테마/세션을 세로로 놓는다. 포털에는 상위 메뉴를 만들지 않는다. 기존 Standard 너비 경계와 PC의 좌우 그룹 2줄 배치는 보존한다.
- 좁은 화면의 메뉴명은 글자(grapheme)1·공백0.5 단위로 계산하고, 10단위와 실제 가용 픽셀 폭을 함께 고려해 온전한 단어 사이에서 균형 있게 줄바꿈한다. 메뉴별 문구 하드코딩 없이 제목·폭·폰트 변경에 재계산하며 줄 수를 잘라 숨기지 않는다. 한 단어 자체가 영역보다 긴 극단적 경우만 넘침 방지용 분할을 허용한다. 세션은 좁은 화면에 간결한 남은 시간을 표시하되 접근성 이름으로 전체 의미를 제공하고 클릭/Enter로 연장한다.
- **후속 요청으로 롤백한 이력:** 제목 왼쪽/작업 버튼 오른쪽 고정과 좁은 화면의 우측 세로 쌓기는 철회했다. 장비·사용자·마스터·노드·권한·접근 로그·에러 IP·내 정보의 제목/작업 영역은 이 개선 직전의 화면별 반응형 배치로 복원했다. 당시 결과는 아래12-7절, 이후 재개선의 현재 동작은12-8절을 따른다.
- 사용자 관리의 선택 세션 파기·비활성화·활성화·영구 삭제는 항상 표시한다. 선택0건이면 disabled이며 선택/전체 선택/해제/목록 재조회에 따라 상태와 선택 건수를 동기화한다. 기존 확인창·권한·실행 API는 유지한다.
- 관리자 카드는 사용자/권한 → 마스터/라인업/결재 → 감사/접근 로그 → 점검/백업 순으로 묶고, 각 묶음은 새 그리드 행에서 시작한다. 허용된 API 결과의 표시만 정렬하며 DB 메뉴 순서·권한·포털 순서는 바꾸지 않는다. 미등록 미래 메뉴도 누락하지 않고 원래 상대 순서로 뒤에 표시한다.
- 백업 서버 UI14파일 배포 완료. 정적35건/실제 템플릿 브라우저19건, 실제 일반 테스트 계정 PC 중심49상태·세로72상태·팝업12상태 통과. 관리자 전용 작업은 네트워크 차단/합성 데이터 검증이며 실제 관리 작업은 실행하지 않았다. 데이터/권한 보존, 설정 복원·로그아웃 완료. 실기기/Safari 검증은 아니다. 기존 제안018·019·020·035의 개선으로 기록한다. [구현·배포·검증 보고서](Reports/2026/09/14/008_Responsive_Shell_and_Admin_UX_Report.md).

### 12-7. 페이지 제목/작업 버튼 배치만 선택적 롤백 (2026-09-14)

선택적 롤백 당시의 이력이다. 이후 사용자가 요청한 제목/설명 공통 규격과 그 아래 작업·검색 영역은12-8절에서 새로 구현했다. 철회한 제목 왼쪽/작업 오른쪽 고정 구조를 복구한 것은 아니다.

- 사용자 요청의 2번만 철회했다. 나의/공개 장비는 좁은 화면에서 제목 아래 작업 버튼이 놓이는 기존 배치로 돌아갔으며, 마스터 작업 버튼도 이전의 카테고리/제조사 탭 영역으로 복원했다. 각 화면의 기존 줄바꿈 조건과 Standard/Edge 제목 너비 경계를 유지한다.
- 1번(좁은 상단바 중앙 제목/좌우 세로 그룹·동적 제목), 3번(사용자 선택 작업 항상 표시/선택0건 비활성), 4번(관련 관리자 카드 묶음)은 보존했다. 공통 CSS와 사용자 관리 템플릿은 관련 배치 부분만 수정하고, 나머지7개 템플릿은 개선 직전과 줄바꿈 형식 정규화 기준으로 동일하게 복원했다.
- 백업 서버에 UI9파일만 반영했다. DB 복원·전체 릴리스 되돌리기·Git reset은 하지 않았다. [선택적 롤백·검증 보고서](Reports/2026/09/14/009_Page_Heading_Selective_Rollback_Report.md).

### 12-8. 메뉴 제목·작업/검색 영역 통일 및 긴 닉네임 3줄 상단바 (2026-09-14)

아래는 당시 구현 이력이다. 장식 아이콘·테마 아이콘과 내 정보의 보안/탈퇴 패널은12-9절에서 텍스트 테마·프로필 하단 모달 버튼으로 대체했다. 제목/작업 영역과 긴 닉네임3줄 조건은 유지한다.

- 인증 후 메뉴15개 템플릿(나의/공개 장비는 공유)의 본문 제목·설명을 마스터 관리 기준의 공통 스타일로 통일했다. 제목은24px/768px 이상30px, 설명은14px이며 기능 버튼을 제목·설명 아래 별도 영역에 둔다. Edge의 Standard 수준 제목/작업 영역 경계와 전체 폭 표는 유지한다.
- 나의/공개 장비의 검색·작업 영역을 공유한다. 1024px 이상은 기능 버튼 왼쪽/검색 오른쪽, 그 미만은 제목 → 설명 → 검색 → 기능 버튼 → 상태/페이지 → 표 순서다. 임시저장함, 공개 목록의 내 장비 포함, 간편/조건 검색, CSV 접기/펼치기와 기존 이벤트·API는 유지한다.
- 좁은 상단바의 계정 버튼을 원래 닉네임+권한 표시로 복원했다. 닉네임은 한 줄·필요할 때만 말줄임하고 권한은 파란색으로 유지한다. 전체 닉네임/권한은 링크의 title·aria-label 및 마이페이지에서 확인한다. 360px 미만에서는 장식 계정 아이콘만 숨겨 이름 공간을 확보한다. 600px 미만의 우측은 로그아웃 / 계정 / 테마·세션의3줄이며, 긴 닉네임 때문에4줄이 되지 않는다.
- 오른쪽4개 버튼은 카드형 표면·테두리·둥근 모서리·포커스를 공유한다. 빨간 로그아웃/세션 경고, 파란 권한과 테마 아이콘 의미를 보존하며, 타이머 상태 변화는 공통 디자인 클래스를 덮어쓰지 않는다.
- 관리자 카드는 사용자 → 권한 → 마스터 → 라인업 → 결재 → 감사 로그 → 접근 로그 → 점검 → 백업 순으로 연속 배치한다. 관련 순서만 유지하고 그룹마다 강제 개행하지 않는다. 마지막 행의 잔여 칸을 제외한 인위적 빈칸을 제거했다. DB 메뉴 순서·권한·포털 순서·미래 메뉴 보존은 유지한다. 사용자 선택 작업 버튼의 항상 표시/선택0건 비활성도 유지한다.
- 백업 서버 UI20파일 반영 완료. 정적38건+템플릿 브라우저22건, 실제 일반 테스트 계정 PC 중심49상태·터치 세로76상태·팝업12상태 통과. 데이터/권한 보존, 설정 복원·로그아웃 완료. 관리자 화면과 극단적 닉네임은 합성 fixture 검증이며 실기기/Safari 검증은 아니다. 기존 제안019·020·035의 후속 개선으로 기록한다. [구현·배포·검증 보고서](Reports/2026/09/14/010_Unified_Menu_Headers_and_Toolbars_Report.md).

### 12-9. 메뉴 표기 정리·역할/노드 탐색·나의 결재함 (2026-09-14)

- 메뉴 이름의 괄호 부연과 제목 앞 장식 아이콘을 제거했다. 내 정보는 `내 정보`, 상단 홈은 `메인 포털`로 표시한다. 메뉴 카드/권한 목록의 이름은 표시 시 정리하며 DB 원문은 보존한다. 가입·비밀번호 재설정 제목의 잔여 Staging 표기도 제거했다. 입력 항목의 선택 여부·상태·권한 등 기능상 설명은 무조건 삭제하지 않는다.
- 상단바와 내 정보의 장식 아이콘을 제거하고 테마 버튼에 현재 모드인 `라이트`·`다크`·`시스템`을 표시한다. 전환 순서와 저장 방식은 유지하며, 긴 닉네임도600px 미만의 우측 로그아웃/계정/테마·세션3줄을 늘리지 않는다. 빨간 로그아웃·파란 역할은 보존한다.
- 내 정보는 사용자 프로필/환경 설정2개 패널이다. 프로필 하단의 비밀번호 변경·회원 탈퇴 버튼으로 각 native dialog를 연다. Escape/취소·포커스 복귀·닫을 때 입력 초기화, 처리 중 중복 제출/닫기 차단을 제공한다. 기존 비밀번호 인증·탈퇴 확인과30일 유예 정책은 유지한다.
- 메뉴 권한 관리의 최초 화면은 ROLE 목록이다. ROLE 선택 후 해당 메뉴만 표시하며1024px 이상은 오른쪽, 그 미만은 아래에 놓는다. 역할/메뉴 검색, 역할별 변경 보존·개별 저장, 상위 권한 종속 검증과 이탈 경고를 제공한다. 사용자가 없는 기존 권한 역할도 목록에서 빠지지 않는다.
- 라인업은 카테고리·제조사 조합의 최상위 노드부터 시작한다. 선택한 노드의 직속 자식과 해당 노드 상세/옵션만 표시하고 경로 버튼으로 상위로 이동한다. 스마트폰에서는 세로 배치한다. 기존 생성·수정·이동·삭제, 공식 모델명, 미사용 옵션 필터와 사용 중 삭제 금지를 유지한다.
- 메인 포털에 `나의 결재함`(`/my_approvals`)을 추가했다. 장비 등록 외 분류·제조사·노드·옵션 상신을 한곳에서 찾도록 독립 메뉴로 정했다. 본인 상신만 전체/처리 중/승인 완료/반려로 조회하며 페이지 이동·처리일시·반려 사유·요청 상세를 제공한다. 관리자 처리함과 분리하고 일반 사용자에게 승인/반려 기능을 제공하지 않는다.
- 일회성 메뉴 마이그레이션은 현재admin/user에 본인 결재함을 부여하고, 상위 관리자 센터 접근 없이 남아 있던user의 기존approvals 허용만 새 개인 조회 메뉴로 옮긴다. 관리자 센터 권한은 부여하지 않는다. 기존 메뉴/기타 권한·업무 데이터·DB v3 구조는 보존하며 반복 시작으로 명시적 거부를 덮어쓰지 않는다.
- 백업 서버에 기능18파일 및 인증 화면 표기2파일, 총20파일 적용 완료. Linux94건, 정적41건, 브라우저26건 통과. 실도메인 기존 일반 테스트 계정 PC 중심57상태·터치 세로84상태·팝업20상태 확인, 설정 복원·로그아웃 완료. 관리자 UI와 내용 있는 결재 이력은 격리 fixture 검증이고 실기기/Safari 검증은 아니다. [구현·배포·검증 보고서](Reports/2026/09/14/011_Menu_Workflows_and_Personal_Approvals_Report.md).

## 13. 현행 기능 및 완료 상태 동기화 (2026-09-13)

현재 운영 대상인 백업 서버 기준으로 013·017·038·039·047을 개발 완료로 정정했다. 12절의 일괄 구현 002·007·008·010·019·022/033·023·026과 사용자 확인을 받은 046의 완료 상태도 유지한다.

| 제안 | 현재 제공하는 기능 |
|---|---|
| 013 | 관리자 점검 상태에서 DB 백업 다운로드, 후보 업로드·호환성 검증, 온라인 복원, 복원 전 자동 백업·실패 시 자동 원복. DB 계약 호환성 및 독립 감사 로그 보존 개선 포함. 사용자 점검 활성화·백업 다운로드·점검 해제 성공 피드백과 Linux 회귀·배포 근거가 있다. |
| 017 | `/api/audit_logs`의 선택 컬럼·일치 방식 검색과 Action·시작/종료 일시 동시 필터, 페이지·최대 10,000건 조회, 사용자별 열 숨김(`audit_log_hidden_columns`) 및 보기 개수 영속화. |
| 038 | `Resources/metadata/` 자동 라우트 등록, 기존 URL 중복 검사, security.txt·robots.txt·llms.txt 등의 제공. 등록 경로를 frozenset으로 분류해 정적 접근 로그로 구별한다. 연락처는 기존 사용자 지정 값을 유지한다. |
| 039 | `/access_logs/error_ips` 화면과 `/api/access_logs/error_ips` 집계 API. 메뉴 권한 검사, IP별 총 에러·4xx·5xx·마지막 발생 시각, 총 건수/최근 시각 순 정렬, 상위 IP 차트 및 원본 접근 로그 IP 필터 딥링크. |
| 047 | 등록 중 루트·하위 라인업 추가, 일반 사용자 승인 대기·관리자 승인, 관리자 노드 생성·이름 변경·이동·삭제·상태·사용량 관리. 순환·깊이·부모 조합·사용 중 참조 검사. 후속 옵션 관리와 삭제 참조 집계 보완도 배포되어 있다. |
| 037 — 구현된 부분 | HTTPS·지원 브라우저의 `document.modelContext`에 `search_equipment`, `view_equipment_list`를 등록한다. 현재 내/공개 목록 범위와 로그인 권한을 유지하고 결과 수·문자열·입력을 제한한다. 미지원 환경에서는 기존 화면을 유지하며 장비 등록 폼은 사용자의 수동 제출을 요구한다. |

장비 모델 표시의 후속 개선도 가동 중이다. 목록은 제조사와 공식 카탈로그 모델명을 표시하며 공식명 미설정 시 노드 전체 경로를 대체값으로 사용한다. 장비 수정 화면은 기존 카탈로그 선택을 복원한다. 사용자가 제조사 표시와 다른 정보 보존, 옵션 삭제 동작을 확인했다.

037의 변경형 AI 도구·전용 사용자 확인·감사 귀속·삭제/복구 정책 확장은 아직 미구현이다. 원안 전체를 완료로 표시하지 않으며 [미구현 로드맵](UNIMPLEMENTED_ROADMAP.md)에서 추적한다.

검증 경계: 이번 문서 동기화는 기존 배포 보고서와 현재 소스의 정적 대조다. 017·038·039의 추가 복합 회귀, 지원 브라우저·모바일·키보드 실조작, 알림 이메일 실수신 확인을 새로 수행한 것으로 주장하지 않는다. 이 후속 확인은 가동 중 기능의 미구현 판정과 구분한다.

[현황 검토](Reports/2026/09/13/004_Roadmap_Status_Sync_Report.md), [기존 DB 배포](Reports/2026/09/12/003_Database_Contract_Production_Release_Report.md), [최신 일괄 배포](Reports/2026/09/13/003_Roadmap_Batch_Release_Report.md).

## 14. 장비 UI 공통 규격·독립 카탈로그 신청 (2026-09-15)

- 나의 장비·공개 장비·임시저장 목록은 동일 템플릿에서 장비명 아래에 공개 범위, 장비 상태, 보증·교체 기한 배지를 한 영역으로 표시한다. 공개/비공개/임시저장과 기존 상태 배지는 동일한 정보 배지 높이·글자·패딩·모서리 규격을 사용한다.
- 조작 버튼은 정보 배지와 역할을 구분한다. 기존 색상·기능을 유지하면서 장비 등록/관리 및 결재 필터의 클릭 영역은 별도의 공통 버튼 높이·패딩·모서리 규격을 사용한다.
- 나의 결재함과 장비 등록 화면에서 카테고리·제조사·라인업 노드 3종 등록 신청을 같은 native dialog 모듈로 제공한다. 카테고리와 제조사는 독립 신청이며, 노드는 승인된 카테고리·제조사 및 같은 조합의 승인된 상위 노드에만 종속된다.
- 일반 사용자 신청은 기존 `ADD_CATEGORY`, `ADD_MANUFACTURER`, `Lineup_Node` 결재 타입을 사용해 PENDING으로 남고, 관리자는 기존 정책대로 즉시 승인 등록된다. 개인 결재함에는 승인/반려 권한을 추가하지 않는다.
- 신청 API는 로그인·CSRF·메뉴 권한을 확인하고 RequesterId/Role/status를 본문에서 신뢰하지 않는다. 마스터/노드·결재·감사는 같은 `BEGIN IMMEDIATE` transaction으로 처리하며 실패 시 rollback한다.
- 신청 dialog를 닫거나 실패해도 작성 중인 장비 입력과 기존 카탈로그 선택을 보존한다. 즉시 승인 항목은 목록만 갱신하며 기존 항목을 임의로 자동 선택하지 않는다. POST/목록 조회 제한시간과 접수 결과 불확실 안내를 제공해 중복 재상신을 줄인다.
- 메인 포털과 관리자 센터는 기존 `MenuCards` renderer를 공유한 채 같은 최대 컨테이너 폭, 1/2/3/4열 반응형 배열 및 Edge 카드 폭을 사용한다.

### 14-1. 백업서버 운영 적용 완료 (2026-09-16)

14절의 UI 공통 규격과 3종 독립 신청서를 백업서버에 적용했다. Git 기준이 뒤처져 있던 운영본은 코드/DB private 사본과 지정 경로 stash로 보존한 후 fast-forward pull했다. 기존 Python 웹앱을 동일 명령/환경으로 재시작했으며 systemd 전환은 하지 않았다.

Linux 회귀99건, 추가 격리 HTTP19건, 실제 템플릿 브라우저26건 통과. 실제 HTTPS 로그인 화면/신규 정적 자산200 및 비로그인 API401, 운영 DB integrity ok/FK0와 확인 대상10개 업무·메타 테이블 행 보존을 확인했다. 실사용자 신청/승인 쓰기와 실기기 검증은 수행하지 않았다. [배포·보존·검증 보고서](Reports/2026/09/16/001_Catalog_UI_Backup_Release_Report.md).

## 15. 이중언어 주석 동기화·Git 감사 보조 도구 (2026-09-16)

- 거버넌스 1.8.1. 안정 Comment ID, EN/KO revision 및 source hash를 추적하고, 기존 역할·의존성·영향 메타 필드를 보존한다.
- revision 누락·회귀, 번역 대기, 중복/삭제 ID, 감사되지 않은 baseline 변경을 구분한다. 원장 갱신으로 위반을 지우지 않는다.
- Git guard는 감사 직전의 작업트리와 index를 각각 보존·비교한다. 한국어 파일명과 선행 dirty 수정 보존, 실제 KO-only 변경 및 최초 번역 추가를 검증한다.
- Python/JS/HTML의 지원되는 독립 주석을 공유 판독기로 처리하며 실행 문자열을 번역 범위에서 제외한다. commit 검사기는 Conventional Commit type과 한국어 제목/본문을 확인한다.
- 파일럿 `LINEUP.NODE.REQUIRED_INT`는 소스 감사 후 EN/KO rev.2로 동기화하고 최초 baseline에 실제 감사자를 기록했다. 전 저장소 주석 전환이나 의미의 자동 인증 기능은 아니다.
- [규격](docs/COMMENT_BILINGUAL_GOVERNANCE.md), [최종 검토·한계·증거](Reports/2026/09/16/004_Bilingual_Comment_Governance_Final_Review_Report.md). DB와 Flask 실행 코드는 변경하지 않았다.
