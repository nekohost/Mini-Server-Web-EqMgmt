# 다차원 심층 자체 검증 방법론 (N-Phase Validation Methodology)

본 문서는 신규 제안(Proposal)이나 기능 구현 계획서(Implementation Plan)가 수립된 직후, 코드를 실제로 작성하거나 운영(Production) 환경에 병합하기 전에 수행해야 하는 **'자체 검증 표준 절차(SOP)'**를 정의합니다. 향후 프로젝트에 투입되는 모든 AI 및 개발자는 본 검증 방법론을 기계적으로 준수해야 합니다.

## 📌 기본 원칙 (맹목적 추종 방지)
1. **순차적 검증**: 모든 단계를 한 번에 몰아서 검증하지 않습니다. `task.md` 아티팩트를 먼저 구성한 뒤, 1단계부터 차례대로 점검하고 결과를 종합 보고서(Comprehensive Validation Report)에 병합해 나갑니다.
2. **객관적/건조한 판단**: `GEMINI.md` 규정에 따라 긍정적 측면과 부정적 측면을 억지로 만들어내지 않으며, 있는 사실 그대로를 객관적이고 명확한 문구로 판단하여 보고서에 기록합니다.
3. **긍정/부정 다면 피드백 교차 검증**: 어떤 제안이나 아키텍처를 점검할 때는 무조건 장점(긍정적 피드백/측면)과 단점·위험요소(부정적 피드백/측면) 양쪽을 독립적으로 도출하여 저울질해야 합니다.
4. **🚨 자율적 위협 발굴 (Minimum Baseline Rule)**: **본 문서에 기재된 각 단계별 '주요 관찰점'은 검증을 위한 아주 최소한의 베이스라인에 불과합니다.** 문서를 열람하는 AI 작업자는 기재된 항목들만을 맹목적으로 체크하고 넘어가는 행위를 절대 금지합니다. 해당 관찰점들을 힌트로 삼아 시스템 맥락에 맞춰 숨겨진 엣지 케이스(Edge Case)와 취약점을 스스로 확장하여 발굴하고 리포트해야 합니다.

---

## 🔍 다차원 단계별 검증 방법론

### 1단계: Rule.md 거버넌스 준수성 검증
기획된 아키텍처 및 구현 가이드라인이 프로젝트의 최고 규범인 `Rule.md`를 어기지 않는지 심문합니다.
- **주요 관찰점**:
  - 프론트엔드와 백엔드 간의 로직 분리 원칙이 뷰 렌더링 방식에 엄수되었는가.
  - 데이터베이스 스키마 변경 시, 파괴적 행위가 통제되고 기존 데이터 보존 원칙이 명시되었는가.
  - 실제 구현 시 라이브 환경이 아닌 지정된 격리 환경에서의 작업이 강제되고 있는가.
  - 프레임워크가 제공하는 기본 설정에 의존하지 않고, 요구되는 커스텀 규칙이 제대로 수용되고 있는가.
  - 외부 라이브러리 사용 시 인가된 목록에 해당하는지 점검되었는가.

### 2단계: 기획 요건(사용자 의도) 달성도 비교 검증
사용자가 최초에 지시한 기획 의도와 현재 수립된 계획서 간의 간극(Gap)이 없는지 대조합니다.
- **주요 관찰점**:
  - 시스템의 고유한 요구사항이 데이터 모델 스키마와 다이어그램에 왜곡 없이 완벽히 반영되었는가.
  - 기존 시스템이 보유한 거버넌스 워크플로우와의 통합 및 연계 기획이 누락되지 않았는가.
  - 기획 단계에서 사용자가 제시한 제약 조건(Constraints) 중 묵인되거나 간과된 사항은 없는가.
  - 사용자가 기대하는 UI/UX적 동선이 프론트엔드 설계 사상에 반영되어 있는가.
  - 단기적인 요건 해소뿐만 아니라, 향후 N차 확장을 고려한 유연한 모델링인가.

### 3단계: 논리적 구동 가능성 검증 (정적 분석)
로컬에서 윈도우 기반 직접 구동을 금지하는 제약을 우회하기 위해, 코딩 전 설계 논리만으로 발생 가능한 병목을 분석합니다.
- **주요 관찰점**:
  - 데이터 폭증 시 데이터베이스 스캔이나 락(Lock), 비효율적 쿼리로 인한 부하가 발생할 여지가 없는가.
  - 프론트엔드 통신이 비정상적인 반복 호출이나 클라이언트 메모리 누수를 유발하지 않도록 방어 로직이 존재하는가.
  - 트랜잭션 도중 예외가 발생할 경우를 대비하여 데이터 무결성을 지키는 롤백 방어선이 구축되어 있는가.
  - 비동기 로직 흐름에서 Race Condition(경합 조건)이나 교착 상태(Deadlock)가 유발될 지점은 없는가.
  - 캐싱(Caching)을 사용할 경우, 캐시 만료(Invalidation) 또는 정합성 파괴로 인한 데이터 불일치 문제는 없는가.

### 4단계: 운영 병합(Production Merge) 시 사이드이펙트 평가
격리된 결과물이 운영 환경 루트로 병합될 때 발생할 수 있는 충돌을 미리 진단합니다.
- **주요 관찰점**:
  - 신규 라우터 주소가 기존 시스템의 패턴과 충돌하여 비정상적인 우선순위를 점유하지 않는가.
  - 신규 API 및 기능이 기존 권한 제어 모듈 밖으로 노출되어 접근 통제를 우회하지 않는가.
  - 기존 비즈니스 레거시 코드를 침범하지 않고 아키텍처 확장(Open-Closed) 원칙에 따라 설계되었는가.
  - 전역 변수나 글로벌 상태 관리 로직을 무분별하게 변경하여 타 기능에 영향을 미치지 않는가.
  - 기존 테스트 케이스나 동작 방식을 파괴(Breaking Change)하는 무리한 구조 변경이 포함되었는가.

### 5단계: 보안 및 예외 엣지 케이스 (Edge Case & Security) 검증
시스템이 정상적인 범주를 벗어난 악의적 조작이나 극단적 상황에 처했을 때 방어할 수 있는지 검증합니다.
- **주요 관찰점**:
  - SQL Injection, XSS, CSRF 등 널리 알려진 웹 취약점을 방어하기 위한 파라미터 이스케이프/검증이 기획되었는가.
  - Null, Undefined, 빈 문자열, 비정상적 타입(String 대신 Array 등)이 입력되었을 때 백엔드가 크래시(Crash) 나지 않고 안전하게 예외 처리(Exception Handling) 되는가.
  - 민감한 데이터나 통신 시 평문 노출 위험이 없는가.
  - 사용자에게 노출되는 에러 메시지(Alert, 500 오류 화면)에 시스템 내부 구조(Stack trace 등)가 불필요하게 노출되지 않는가.

### 6단계: 롤백 및 역방향 파급 효과 (Downgrade/Rollback) 검증
개발물이 실제 병합된 직후 치명적인 오류가 발견되었을 때, 신속하고 안전하게 이전 상태로 되돌릴 수 있는 구조인지 검증합니다.
- **주요 관찰점**:
  - 데이터베이스 스키마 변경 시 Up 스크립트뿐만 아니라 Down(롤백) 스크립트도 구조적으로 수행 가능한가.
  - 코드를 이전 버전으로 되돌릴 때(Git Revert 등), 새롭게 쌓인 데이터 때문에 시스템 기동이 실패하는 의존성 오염 현상이 없는가.
  - 캐시 스토리지나 세션 등 클라이언트에 남아있는 찌꺼기 데이터로 인해 롤백 후 구동 시 에러가 나지 않는가.
  - 인프라 또는 환경 변수(.env)의 변경 사항이 코드 변경 사항과 결합되어 있어, 부분 롤백 시 충돌할 여지는 없는가.

### 7단계: 엔드 유저 휴먼 에러 및 UX 방어 검증 (Human-in-the-loop)
아키텍처와 로직이 완벽하더라도, 실제 사용자가 폼(Form)을 잘못 입력하거나 오작동했을 때 혼란을 느끼면 시스템 실패로 간주될 수 있으므로, 엔드 유저 관점에서 발생할 수 있는 휴먼 에러 시나리오를 심문합니다.
- **주요 관찰점**:
  - 의도적으로 잘못된 동선으로 조작했을 때 시스템이 '비활성화 사유'나 에러를 명확한 안내 문구(Toast, Alert 등)로 피드백하는가.
  - 사용자가 다음 단계로 넘어가지 못하고 갇히는 데드엔드(Dead-end) UI 영역은 없는가.
  - 파괴적인 액션(삭제, 덮어쓰기 등) 수행 전, 사용자의 명시적인 재확인(Double-check) 절차를 강제하고 있는가.
  - 입력 폼(Form)에서 사용자가 필수값을 누락하거나 범위를 벗어난 값을 입력했을 때, 프론트엔드 단에서 실시간(Validation)으로 1차 방어선이 작동하는가.

### 8단계: AI 작업자 메타(Meta) 거버넌스 검증
본 프로젝트의 주요 개발 주체가 AI인 환경 특성상, 목적 달성을 위해 불필요한 행위를 남발하여 프로젝트 컨텍스트를 오염시키는 행위를 외부 시스템 관점에서 스스로 검열합니다.
- **주요 관찰점**:
  - 해당 태스크 진행 중 AI가 임시 테스트나 분석을 위해 생성한 스크래치(Scratch) 파일들을 스스로 청소(Clean-up) 하였는가.
  - 대화 로그 및 아티팩트 작성 시 환각(Hallucination)에 의한 억지 주장이나 프로젝트 맥락과 맞지 않는 오염된 컨텍스트를 삽입하지 않았는가.
  - 시스템 통제 규정(`Rule.md`, `GEMINI.md`)을 우회하기 위해 교묘하게 표현을 비틀거나 꼼수를 쓰지 않고 투명하게 지시를 이행하였는가.
  - 사용자에게 불필요하게 빠른 승인이나 답변을 재촉하지 않고, 건조하고 객관적인 톤 매너를 유지했는가.
