# 앞으로 구현할 기능 목록 (Will Implement)

이 문서는 장비 및 자산 통합 관리 시스템(`Mini-Server-Web-EqMgmt`)에 향후 추가할 기능들과 그 구현 우선순위를 기록합니다.
제안(`PROPOSALS.md`)에서 채택된 기능과 기존 로드맵의 기능을 통합하여, 우선순위별로 일원화된 포맷으로 관리합니다.

---

## 📌 [최상] 개발 예정 목록 (우선순위: 최상)

1. **[제안-022] & [제안-033] 장비 상태(Status) 및 라이프사이클(이력) 관리 통합 기능**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 자산 관리의 핵심인 라이프사이클(정상, 대여중, 수리중, 폐기 등)을 시스템적으로 추적하기 위해 필수적인 공통 요소입니다.
   - **구현 우선순위:** [최상] 
   - **구현 가능성:** [높음] `equipment` 테이블에 `Status` 컬럼을 확장하고, 상태 변경 이력을 기록하는 `equipment_history` 테이블을 신설하여 구현 가능합니다.
   - **상세 내용:** 장비의 현재 상태 추적 필드 추가 및 상태 이력 추적 구현. **구현 시 대시보드 통계(`app.py` `api_dashboard_stats`)에 하드코딩된 `'정상' as Status` 임시 조치문을 실제 `Status` 컬럼 참조 쿼리로 반드시 복구할 것.**

2. **[제안-001] 세션 만료(자동 로그아웃) 및 동시 로그인 제어 (+보안 감사 로그)**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 인터넷에 노출되는 시스템 특성상 계정 탈취를 방어하기 위한 가장 기초적이고 필수적인 보안 장치이므로 반드시 구현해야 합니다.
   - **구현 우선순위:** [최상] 보안과 직결되므로 다른 부가 기능보다 먼저 구현되어야 합니다.
   - **구현 가능성:** [매우 높음] Flask 기본 `PERMANENT_SESSION_LIFETIME` 설정만으로 자동 로그아웃은 즉시 구현 가능합니다. 동시 로그인 제어의 경우 SQLite에 `session_tokens` 테이블을 만들어 로그인 시마다 발급/검증하면 무리 없이 구현 가능합니다.
   - **상세 내용:** `app.config['PERMANENT_SESSION_LIFETIME']` 설정 및 세션 토큰 DB 기록을 통한 동시 접속 차단.

3. **[제안-013] 웹 기반 DB 데이터 백업 및 복원 (Administrator Backup & Restore)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 물리적인 미니서버(특히 SD카드 기반) 손상 시 모든 자산 데이터가 증발하는 치명적인 사태를 방지하는 가장 훌륭하고 단순한 재난 복구 수단입니다.
   - **구현 우선순위:** [최상] 시스템 데이터의 영속성 및 생존과 직결된 핵심 기능입니다.
   - **구현 가능성:** [매우 높음] SQLite DB가 단일 파일(`equipment.db`)이라는 점을 십분 활용하여, 관리자가 해당 파일을 다운로드(send_file)하거나 새 파일로 덮어쓰기(업로드)하는 단순한 API만으로 백업/복원 기능을 완성할 수 있습니다.
   - **상세 내용:** 관리자 전용 DB 덤프/다운로드(ZIP/DB 파일) 기능 및 백업 파일 업로드를 통한 DB 롤백/복원 기능.

---

## 📌 [상] 개발 예정 목록 (우선순위: 상)

1. **[제안-044] 웹 접근 로그 청크(Chunk) 단위 분할 삭제 및 진행률 UI 적용**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 방대한 로그 삭제 시 발생하는 SQLite Lock Timeout 방지 및 안정적인 영구 파기 보장. 프론트엔드 진행률(%) 표출로 UX 강화.
   - **구현 우선순위:** [상]
   - **구현 가능성:** [매우 높음] `app.py`의 cleanup 로직에 `step` 매개변수를 도입하여 chunk size 250으로 분할 처리.
   - **상세 내용:** `DELETE` 쿼리에 서브쿼리(`LIMIT 250`) 적용, 프론트엔드(`access_logs.html`) 비동기 반복 루프 연동 및 실시간 진행 상태 UI 업데이트.

2. **[제안-045] 웹 접근 로그 페이로드 지연 로딩(Lazy Loading) 최적화**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **연관 제안:** [제안-036], [제안-040], [제안-043], [제안-044]
   - **결정 사유:** 웹 접근 로그 목록 조회 시 대용량 텍스트인 Request/Response Payload를 일괄 조회함에 따라 발생하는 불필요한 네트워크 대역폭 낭비, DB I/O 부하 및 브라우저 메모리 부담을 온디맨드 비동기 조회 방식으로 전환하여 해소.
   - **구현 우선순위:** [상]
   - **구현 가능성:** [매우 높음] 목록 조회 API(`/api/access_logs`)에서 페이로드 유무 플래그만 반환하고, 상세 모달 열람 시 전용 단건 조회 API(`GET /api/access_logs/<int:log_id>/payload`)를 비동기 호출하도록 구현.
   - **상세 내용:** 백엔드 목록 쿼리 최적화, 단건 조회 API 신설, 프론트엔드 모달 비동기 페칭 및 로딩 상태 UX 적용.

3. **[제안-023] 상세 검색 및 필터링 기능 (API 단)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 장비 데이터가 누적됨에 따라 원하는 장비를 빠르게 찾기 위한 필수 기능이므로, 상태 관리 도입 이후 최우선적으로 구현합니다.
   - **구현 우선순위:** [상] 데이터 활용도를 위해 필수적입니다.
   - **구현 가능성:** [높음] 백엔드 API에서 쿼리 파라미터를 받아 동적 쿼리를 생성하고, 프론트엔드 UI(체크박스/셀렉트박스 등)와 연동하여 무난히 구현 가능합니다.
   - **상세 내용:** 카테고리, 제조사, 상태, 구입일자 등 다양한 조건으로 장비를 다중 검색하고 필터링하는 기능.

2. **[제안-002] 입력값 유효성 검증 강화 (프론트엔드 + 백엔드)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 사용자의 악의적, 혹은 실수로 인한 잘못된 데이터 입력을 서버단에서 원천 차단하는 것은 DB 정합성과 시스템 안정성을 위해 필수적입니다.
   - **구현 우선순위:** [상] DB 스키마 무결성 유지와 직결됩니다.
   - **구현 가능성:** [매우 높음] Python 백엔드의 API 엔드포인트(`/register`, `/api/equipment`)에 간단한 정규식(Regex)과 조건문을 추가하는 것만으로 신속히 방어 로직을 구성할 수 있습니다.
   - **상세 내용:** 비밀번호 복잡도(영문/숫자/특수문자 8자 조합), 아이디 제한(특수문자/공백 차단), 장비 등록 시 필수 필드 서버단 검증.

3. **[제안-007] API 요청 속도 제한 (Rate Limiting)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 브루트포스(Brute-force) 공격으로 인한 관리자 계정 탈취를 막기 위해 로그인 API 등에 대한 최소한의 호출 속도 제한은 보안상 필수적입니다.
   - **구현 우선순위:** [상] 보안 강화 차원입니다.
   - **구현 가능성:** [중~높음] 외부 라이브러리(`flask-limiter`)를 도입하거나, 미니서버의 성능을 고려해 Python 내장 딕셔너리와 시간을 이용한 in-memory Rate Limiting을 가볍게 구현할 수 있습니다.
   - **상세 내용:** 동일 계정/IP 기준 N회(예: 5회) 로그인 연속 실패 시 일정 시간(예: 15분) 계정 잠금.

4. **[제안-010] 장비 이미지 및 문서 첨부 기능 (영수증, 사진, 매뉴얼)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 영수증(증빙자료)과 매뉴얼(사용법) 보관은 자산 관리의 필수 요소입니다. 텍스트만으로는 상세 정보나 회로도 등을 보관하기 어렵습니다.
   - **구현 우선순위:** [상] 사용자 편의성과 자산 관리의 완결성을 위해 우선순위가 높습니다.
   - **구현 가능성:** [중] Flask의 파일 업로드 처리(`secure_filename`), 미니서버의 디스크 보호를 위한 용량 및 확장자 제한 로직, 그리고 `equipment_files` 테이블 추가 작업이 필요합니다.
   - **상세 내용:** 장비 등록/수정 모달에 파일 첨부 UI 추가 및 업로드된 영수증/이미지 preview 기능 제공.

5. **[제안-034] 내 정보 사용자 프로필(아이디, 이름, 닉네임) 본인 인증 기반 수정 기능**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 기존 마이페이지에서는 이메일/비밀번호 변경만 가능했던 한계를 개선하여, 본인 비밀번호 인증을 거쳐 안전하게 아이디, 이름, 닉네임을 변경할 수 있도록 함.
   - **구현 우선순위:** [상] 사용자 관리 유연성 확보.
   - **구현 가능성:** [매우 높음] `/api/users/update_profile` API 신설, 비밀번호 대조 검증, 아이디 중복 예외 처리 및 세션 동기화.
   - **상세 내용:** 마이페이지 내 프로필 수정 모달 UI 추가, 현재 비밀번호 인증 후 LoginId, Name, NickName 일괄/선택 변경 기능 제공.

5. **[제안-011] 동적 카테고리 & 제조사 마스터 관리 (태그/분류 표준화)**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 자유 텍스트 입력으로 인한 데이터 파편화("삼성", "SAMSUNG", "samsung")를 방지하고 검색과 필터링의 정확도를 100% 보장할 수 있습니다.
   - **구현 우선순위:** [상] 데이터가 방대해지기 전에 초기에 마스터 데이터 체계를 잡는 것이 가장 바람직합니다.
   - **구현 가능성:** [높음] `categories` 및 `manufacturers` 테이블을 신설하고, 텍스트 입력기를 서버 렌더링된 `<select>` 드롭다운으로 교체하는 직관적인 작업입니다. (수동 추가 기능은 [제안-027]과 연계됨)

6. **[제안-027] 마스터 데이터 추가 전자결재(승인) 시스템**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** [제안-011]로 분리된 마스터 데이터에 신규 항목을 임의로 추가하지 못하도록 막으면서도 유연성을 제공하기 위해, '전자결재' 형태의 승인 프로세스를 도입합니다. 향후 다른 결재 업무로 확장 가능한 강력한 인프라가 됩니다.
   - **구현 우선순위:** [상] [제안-011]과 함께 구현되어야 할 필수 후속 기능입니다.
   - **구현 가능성:** [중~높음] `approval_requests` 테이블(전자문서)을 신설하고 관리자 전용 결재 대시보드를 구축합니다.
   - **상세 내용:** 사용자가 셀렉트 박스에서 '기타' 선택 후 수동 입력 시, 즉각 마스터에 반영되지 않고 결재가 상신됨. 관리자가 승인하면 정식 반영 및 임시 장비 데이터 자동 업데이트. **(추가 확장) 결재 상신, 승인, 반려 발생 시 관련자(관리자, 요청자)에게 즉각 이메일 알림 발송 기능 포함.**

7. **[제안-028] 장비 임시저장(Draft) 기능**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 신규 카테고리/제조사 결재 승인 대기로 인한 등록 지연이나 당장 기입할 정보가 부족할 때, 흐름을 끊지 않고 보관할 수 있는 필수 유저 편의 기능입니다.
   - **구현 우선순위:** [상] [제안-027]과 시너지를 내는 핵심 기능입니다.
   - **구현 가능성:** [매우 높음] `equipment` 테이블에 `IsDraft` 컬럼을 신설하고 프론트엔드 모달/리스트 분기 처리.
   - **상세 내용:** 장비 등록 창에 한정하여 임시저장 기능을 제공하고, '임시저장함'이라는 독립된 뷰에서 상태를 관리할 수 있습니다.
8. **[제안-017] 감사 로그 고도화 (다중 필터링, 컬럼 커스텀, 페이징 및 영속화)**
   - **상태:** [일부 완료 / 고도화 대기중]
   - **결정 사유:** 대용량 감사 로그 탐색 효율성과 보안 추적 성능을 대폭 향상시키는 엔터프라이즈급 모니터링 필수 기능입니다.
   - **구현 우선순위:** [상] 보안 모니터링 고도화 차원입니다.
   - **구현 가능성:** [매우 높음] RESTful 비동기 API(`/api/audit_logs`), 10,000건 대량 조회 및 `user_settings` 연동 기반 완성.
   - **상세 내용:** 
     - [완료] RESTful 비동기 API, 전역 페이징, per_page 설정(`user_settings` 영속화), max 10,000 대량 조회.
     - [대기중] 복합 조건 다중 필터링 (행위자, Action 유형, 시작/종료 일시 동시 적용) 및 표 표시 열 선택(컬럼 커스터마이징) 기능 추가.

9. **[제안-018] 세션 강제 만료 3단계 세분화 (본인 제외/본인 포함/선택 유저)**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 비상 상황(보안 패치, 해킹 의심 등) 발생 시 관리자가 즉각적으로 특정 사용자 또는 전체 사용자의 세션을 강제 종료시킬 수 있는 강력한 통제 수단이 필요함.
   - **구현 우선순위:** [상] 보안 통제권 확보 차원에서 핵심적인 기능입니다.
   - **구현 가능성:** [매우 높음] `/api/system/force_logout` API를 신설하여 `users` 테이블의 `SessionToken`을 새로운 난수로 갱신하는 방식으로 즉시 차단 가능.
   - **상세 내용:** 전체 만료(본인 제외), 전체 만료(본인 포함), 선택 유저 만료 기능을 사용자 관리 화면에 다중 선택 체크박스와 함께 구현.

10. **[제안-020] Context-aware 스마트 동적 뒤로가기 네비게이션 UX 개선**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 메뉴간 이동 및 서브 유틸리티(마이페이지 등) 진입 시 직전 작업 메뉴로 복귀하지 못하고 무조건 포털로 튕겨 나가는 부자연스러운 UX 이동 동선을 개선하여 사용자 경험 및 작업 연속성을 보장함.
   - **구현 우선순위:** [상] UX 완성도 및 이동 동선 일관성 확보 차원입니다.
   - **구현 가능성:** [매우 높음] `miniserver_frame.html`의 네비게이션 헤더에 `document.referrer` 및 `history.back()` 연동 스마트 뒤로가기 컴포넌트를 적용하여 신속 구현 가능.
   - **상세 내용:** 직전 작업 메뉴가 있을 경우 "← 이전으로" (직전 메뉴 복귀), 그 외의 경우 "← 포털로" (포털 복귀)가 동적으로 동작하도록 네비게이션 헤더 개선.

11. **[제안-021] 관리자 전용 사용자 계정 관리 기능 (완전 파기 및 비활성화/활성화 토글)**
   - **상태:** [개발 완료 (기능 확장 중)]
   - **결정 사유:** 계정 즉시 파기 기능에 더하여 관리자 전용 계정 정지(무기한 비활성화) 및 재활성화 토글과 체크박스 다중 일괄 제어 확장.
   - **구현 우선순위:** [상] 사용자 관리 체계의 완전성 및 통제권 확보.
   - **구현 가능성:** [매우 높음] `/api/users/delete_selected`, `/api/users/<id>/toggle_deactivation`, `/api/users/deactivate_selected` 연동.
   - **상세 내용:** 사용자 관리 화면에서 체크박스 다중 선택 및 행별 단일 선택으로 즉시 파기 및 계정 정지/복구 토글 제공.

12. **[제안-035] 관리자 전용 메뉴 허브(Admin Center) 분리 구축**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 향후 시스템이 확장됨에 따라 관리자 전용 메뉴가 계속해서 늘어날 것을 대비하여, 포털 화면의 혼잡도를 줄이고 일반 사용자와 관리자의 동선을 논리적으로 분리하는 것이 아키텍처 관점에서 올바른 방향입니다.
   - **구현 우선순위:** [상] 시스템 뼈대 및 메뉴 라우팅 체계를 개편하는 구조적 작업이므로 타 기능 확장에 앞서 우선 진행해야 합니다.
   - **구현 가능성:** [매우 높음] `menus` 테이블에 `MenuGroup` 컬럼을 추가하고, 포털과 관리자 전용 메뉴 렌더링 로직을 분리(`admin_center.html` 신설)하면 구현 가능합니다.
   - **상세 내용:** 관리자용 메뉴버튼들을 기존 포털 라우팅에서 분리하여 새로 신설되는 `/admin_center`에만 노출. 포털에는 기존과 동일한 모양의 "관리자 센터" 버튼 카드를 무조건 가장 마지막에 렌더링하여 진입점으로 제공.

13. **[제안-036] 실시간 웹 접근 로그(HTTP Access Log) 자동 수집 및 모니터링 시스템 구축**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 터미널에 실시간으로 출력되는 표준 웹 서버 접근 로그(HTTP Access Logs: IP, Method, Path, Status Code, Duration 등)를 DB에 체계적으로 적재하고, 비즈니스 감사 로그(`audit_logs`)와는 분리된 관리자 전용 관제 화면(`access_logs.html`)을 통해 외부 봇넷/스캐너 공격 탐지 및 웹 트래픽을 실시간 모니터링할 수 있는 필수 인프라 기능입니다.
   - **구현 우선순위:** [상] 실시간 보안 관제 및 인프라 모니터링 확보 차원.
   - **구현 가능성:** [매우 높음] `@app.after_request` 인터셉터와 인메모리 큐(`queue.Queue`) 기반 백그라운드 벌크 워커를 도입하여 미니서버의 SD카드 디스크 I/O를 완벽히 보호하면서 고성능으로 구현 완료되었습니다.
   - **상세 내용:** DB `access_logs` 테이블(WAL 모드) 및 인덱스 4종 생성, 세션 폴링 제외 및 정적 리소스 분리 수집(`IsStatic`), 인메모리 큐 기반 벌크 인서트, 관리자 전용 웹 접근 로그 모니터링 화면(`access_logs.html`) 및 수동 클렌징 도구 제공.

14. **[제안-041] 웹 접근 로그 요약 통계 기간 필터 확장 (오늘/전체 토글)**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 웹 접근 로그 화면 상단 요약 카드가 오늘 하루 데이터로만 제한되어 있어, 전체 누적 트래픽과 장기적 에러율을 즉각 파악하기 어려운 한계를 해소하기 위함.
   - **구현 우선순위:** [상]
   - **구현 가능성:** [매우 높음] DB 스키마 변경 없이 `GET /api/access_logs/stats?period=today|all` 쿼리 조건문 분기 및 프론트엔드 세그먼트 토글 버튼 연동만으로 안전하게 구현 가능합니다.
   - **상세 내용:** 백엔드 통계 API에 `period` 쿼리 파라미터(`today` | `all`, 기본값 `today`) 분기 로직 적용 및 프론트엔드 상단 카드 영역에 세그먼트 컨트롤 UI(`[ 📅 오늘 ]` / `[ 🌐 전체 누적 ]`) 추가 및 동적 레이블/수치 갱신 구현.

15. **[제안-042] Python 3.14 런타임 안정화 및 Reloader 세마포어 크래시 방어**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** Python 3.14 환경에서 Werkzeug Auto-Reloader 구동 시 자식 프로세스 IPC 세마포어 감시 충돌로 서비스가 시작 직후 무음 셧다운되는 장애를 원천 차단하고 단일 프로세스 구동 안정성을 확보하기 위함.
   - **구현 우선순위:** [상]
   - **구현 가능성:** [매우 높음] `app.run()`에 `use_reloader=False`를 명시하고 `try...except` 래핑을 적용하여 즉시 해결 가능합니다.
   - **상세 내용:** `app.run(host='0.0.0.0', port=5000, debug=is_debug, use_reloader=False)` 적용으로 Werkzeug 서브프로세스 분기 차단 및 `try...except` 기반 예외 완전 로깅.

16. **[제안-043] 접근 로그 API 자기 참조 ResponsePayload 재귀 루프 차단**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** `/api/access_logs` 호출 시 50건의 로그 목록 JSON 응답이 다시 `ResponsePayload`에 적재되며 발생하는 재귀적 데이터 팽창, DB 비대화, 쿼리 지연 및 삭제 시 SQLite 파일 락 교착 장애를 원천 차단하기 위함.
   - **구현 우선순위:** [상]
   - **구현 가능성:** [매우 높음] `after_request` 인터셉터에서 `request.path.startswith('/api/access_logs')` 예외 조건 분기 추가로 즉시 안전하게 구현 가능합니다.
   - **상세 내용:** `@app.after_request` 인터셉터에서 `not is_static and not request.path.startswith('/api/access_logs')` 조건 적용으로 로그 API 응답의 자기 참조 Body 캡처 방어.

---

## 📌 [중] 개발 예정 목록 (우선순위: 중)


1. **[제안-024] 마이페이지(회원정보 수정) 및 로그인 화면 비밀번호 찾기 기능**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 시스템 보안 및 기본 사용자 편의성을 위해 필수이나, 현재는 사용자 규모가 작아 관리자가 수동 지원할 수 있으므로 '중'으로 책정합니다.
   - **구현 우선순위:** [중] 계정 시스템 고도화 단계에서 필요.
   - **구현 가능성:** [중~높음] 마이페이지는 단순 CRUD이나, 비밀번호 찾기의 경우 외부 이메일(SMTP) 서버 연동 또는 별도 인증 로직 구축이 필요합니다.
   - **상세 내용:** 
     - 로그인한 사용자가 자신의 프로필을 조회하고 수정(비밀번호 변경 포함)할 수 있는 기능.
     - 로그인 화면에서 비밀번호를 잊었을 때 본인 인증을 거쳐 임시 비밀번호를 발급받거나 재설정하는 기능.

2. **[제안-025] 회원탈퇴 및 철회(유예 기간) 기능 (Soft Delete)**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 사용자의 실수 방지와 개인정보 보호 측면에서 훌륭한 기능이며, 4단계 라이프사이클(30일 비활성화 유예 -> 30일 경과 로그인 불가 -> 1년 1일 영구 파기 -> 재가입 시 보안 검증 복구)을 제공합니다.
   - **구현 우선순위:** [중]
   - **구현 가능성:** [높음] `users` 스키마 확장, `/deactivated_notice` 전용 페이지, 지연 평가 상태전이, 세션 연동 및 재가입 복구 검증.
   - **상세 내용:** 30일 비활성화 유예 화면(잔여 D-Day 및 철회 버튼), 30일 경과 시 로그인 불가 처리, 1년 1일 경과 시 영구 파기(`[제안-021]` 연동), 탈퇴 계정 ID로 재가입 시 소유권 확인 후 새 비번으로 계정 부활.

3. **[제안-003] 감사 로그(Audit Log) 조회 화면**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 현재 훌륭하게 적재되고 있는 `audit_logs` 데이터를 관리자가 웹 UI를 통해 쉽게 모니터링하고 추적할 수 있도록 시각화해야 로그의 가치가 발휘됩니다.
   - **구현 우선순위:** [중] 시스템 운영 및 보안 모니터링을 위해 필요합니다.
   - **구현 가능성:** [높음] 이미 데이터가 적재되고 있으므로, 새로운 HTML 템플릿(`audit_logs.html`)과 페이징(Pagination)이 적용된 단순 SELECT 쿼리 API만 작성하면 됩니다.
   - **상세 내용:** 관리자 전용 감사 로그 UI, 행위 유형 및 날짜/사용자 필터링 지원.

4. **[제안-004] 관리자용 사용자 관리 화면 (회원 목록 조회, 역할 변경)**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 관리자가 사용자의 역할을 변경하거나 계정을 관리하는 기능은 다중 사용자 시스템의 가장 기초적인 관리 도구입니다.
   - **구현 우선순위:** [중] 사용자가 늘어나기 전에 관리 도구 확보 차원에서 유용합니다.
   - **구현 가능성:** [높음] `users` 테이블에 대한 간단한 CRUD 및 권한 검증 미들웨어를 조합하여 구현할 수 있어 아키텍처 변경 없이 즉각적인 구현이 가능합니다.
   - **상세 내용:** 전체 사용자 목록 조회 및 역할(admin ↔ user) 변경 기능.

5. **[제안-008] 장비 보증기간(A/S) 및 교체 주기 추적 (D-Day 카운트다운)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 자산 관리의 핵심 중 하나인 "유지보수 및 교체 기간"을 추적할 수 있어 시스템의 실용성을 크게 높여줍니다.
   - **구현 우선순위:** [중] 장비 데이터의 품질과 관리 편의성 향상을 위해 유용합니다.
   - **구현 가능성:** [높음] `equipment` 테이블에 `WarrantyDate` 컬럼을 추가하고, 프론트엔드에서 현재 날짜와 비교하는 JavaScript(D-Day 계산) 로직만 추가하면 됩니다.
   - **상세 내용:** 장비 카드/목록에서 잔여 D-Day 계산 및 뱃지(Badge) 표시. **(추가 확장) 매일 자정, 보증기간 만료 임박(예: 7일) 장비 리스트를 담당자 이메일로 자동 리포팅하는 백그라운드 스케줄러 기능 포함.**

6. **[제안-016] 사용자 맞춤 설정(User Preferences) DB 테이블 분리 및 UI 상태 영속화**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 회원의 핵심 정보(`users`)와 UI 편의성 설정(`user_settings`)을 분리하는 데이터베이스 정규화 측면의 모범 사례(Best Practice)입니다. 사용자 편의성과 시스템 확장성을 동시에 확보할 수 있습니다.
   - **구현 우선순위:** [중] 장비 목록 고도화 기능과 함께 묶어서 개발하기 적합합니다.
   - **구현 가능성:** [높음] `users` 테이블과 1:1 관계를 가지는 `user_settings` 테이블을 신설하고, 설정 데이터를 JSON으로 저장하여 프론트엔드와 통신하도록 설계하면 아키텍처 충돌 없이 구현 가능합니다.
   - **상세 내용:** 
     - "내 장비 최상단 보기" 등 체크박스 상태 변경 시 백엔드 DB 자동 저장.
     - 로그인 직후 이 설정값을 불러와 UI(정렬 상태 등) 자동 복원.
     - 차후 "다크 모드" 등 모든 개인화 옵션을 이 테이블 한 곳에서 통합 관리.

7. **[제안-019] 사용자 맞춤형 레이아웃 스킨(Skin) 시스템 (Edge vs Standard)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 서로 다른 기기(모바일/데스크탑) 환경에서 최적화된 UI를 사용자 입맛에 맞게 선택하여 활용할 수 있어 UX 만족도가 극대화됨.
   - **구현 우선순위:** [중] UI 파편화를 해결하면서도 사용자 편의성을 챙길 수 있습니다.
   - **구현 가능성:** [매우 높음] 이미 구현된 `user_settings` 인프라를 재활용하여 `layout_skin` 값을 저장/불러오고, Python 서버 렌더링 시 조건부로 HTML을 분기하면 됨.
   - **상세 내용:** 데스크탑 통일성 유지용 Standard 스킨과 모바일 공간 활용 극대화용 Edge 스킨을 분리 제작하고, 원클릭 스킨 전환 버튼을 통한 UI 설정 영속화.

8. **[제안-029] 전역 로딩 오버레이 UI/UX 도입 (모든 비동기 데이터 변경 시)**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 비동기 작업 시 화면 피드백이 없으면 중복 클릭이나 페이지 이탈 등 예상치 못한 오류를 유발할 수 있습니다. 로딩 상태 시각화 및 상호작용 차단은 시스템 전반의 사용자 경험(UX)과 안정성을 대폭 향상시키는 표준적이고 필수적인 장치입니다.
   - **구현 우선순위:** [중] 시스템 전반에 적용할 수 있는 매우 효과적인 UX 개선 사항입니다.
   - **구현 가능성:** [매우 높음] 모든 템플릿(또는 공통 적용 가능 파일)에 재사용 가능한 전체 화면 딤(Dim) 처리 및 스피너(Spinner) 마크업을 두고 JS로 제어하는 방식으로 간단하게 적용 가능합니다.
   - **상세 내용:** 삭제, 저장, 통폐합 등 데이터 변경(비동기 요청) 발생 시 전체 화면을 덮는 반투명 검은색 배경과 스피너를 표출하되, 출력되는 메시지는 확장성을 위해 파라미터로 받으며, 기본적으로는 상수 딕셔너리(Message Dictionary)를 활용해 "작업 처리 중입니다..."로 고정하여 표출.

9. **[제안-038] 메타데이터 자동 라우팅 및 보안 텍스트(security.txt) 도입**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 로봇(크롤러) 및 AI 스캐너 봇들의 무작위 스캐닝으로부터 미니서버의 불필요한 트래픽 및 404 에러 로그 생성을 막고, 프로젝트 소유권을 명확히 하는 표준 메타데이터 제공.
   - **구현 우선순위:** [중] 시스템 안정성 향상.
   - **구현 가능성:** [매우 높음] `Resources/metadata/` 디렉토리에 정적 파일을 위치시키고, `app.py` 구동 시 `os.walk`를 이용해 재귀적으로 라우터를 자동 생성하면 구현 완료됩니다.
   - **상세 내용:**
     - `security.txt` 내 연락처 이메일은 반드시 사용자 지정 하드코딩(`nekohost@nekohost.org`) 적용.
     - `app.py` 기동 시 메타데이터 디렉토리를 탐색하여 자동 동적 라우팅 생성 (URL Rule 중복 생성 방어 적용).
     - HTTP 접근 로그(`access_logs`) 수집 시 `IsStatic=1`로 분류되도록 O(1) 정적 검사 풀(`frozenset`) 도입.

10. **[제안-039] 에러 유발 고유 IP 분석(Error IP Analysis) 대시보드 추가 (Staging-First 필수)**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 기존 `access_logs.html`은 단건 로그만 나열되어 특정 공격/봇 IP 추적이 어렵습니다. 에러(4xx, 5xx)를 유발한 IP만 고유하게 리스팅하여 직관적으로 확인해야 합니다.
   - **구현 우선순위:** [중] 시스템 보안 모니터링 고도화.
   - **구현 가능성:** [매우 높음] 신규 테이블/백그라운드 엔진 없이, 기존 테이블에 순수 SQL 쿼리(`GROUP BY`)만으로 동적 렌더링이 가능합니다.
   - **상세 내용 및 🤖 AI 개발자 필수 준수 가이드라인 (절대 준수!):**
     - **[기능 구현 (No-Logic 원칙)]**: 신규 `access_logs_error_ips.html` 화면 구성 (상위 IP 요약 차트, 데이터 테이블). IP 클릭 시 기존 화면 딥링크(필터링) 자동 연동. 백그라운드 엔진 추가 절대 금지 (`WHERE StatusCode >= 400 GROUP BY IpAddress` 쿼리만 사용).
     - **[Rule.md 제7-3조 (스테이징 격리 필수)]**: 절대 루트 `app.py`를 즉시 고치지 마십시오. 무조건 `Staging/app.py` 복사 후 격리된 `Staging/templates/` 폴더에서 작업하여 안전 검증(Safety Net)을 구축하십시오.
     - **[Rule.md 제5-1-1조 (PC 구동 금지)]**: Windows 런타임 구동 테스트를 시도하지 말고, 정적 리뷰와 쿼리 검증만으로 마무리하십시오.
     - **[Rule.md 제4-3조 (주석 3원칙)]**: 새 API 작성 시 `[역할]`, `[의존성 관계]`, `[변경 시 영향도]` 주석을 100% 명시하십시오.

11. **[제안-040] 웹 접근 로그 상세 기록 확장 (Request & Response Payload 로깅)**
    - **상태:** [개발 완료 (FEATURES.md 이관)]
    - **결정 사유:** 기존 메타데이터(IP, 상태 코드 등) 중심의 접근 로그에서 발생하는 에러 원인 추적의 한계를 극복하고, Payload(Body) 수준의 상세 트러블슈팅과 강력한 보안 감사를 제공하기 위한 확장 기능입니다.
    - **구현 우선순위:** [중] 보안 및 디버깅 역량 강화를 위해 필요합니다.
    - **구현 가능성:** [높음] 기존 `access_logs` 테이블 스키마에 Payload 컬럼을 추가하고, `access_logs.html` 모달 UI를 확장하여 구현 가능합니다. 단, 민감 정보 마스킹 로직이 동반되어야 합니다.
    - **상세 내용:** 
      - `access_logs` 테이블에 `request_payload`, `response_payload` 컬럼 추가.
      - 관리자 모니터링 화면 내 모달 팝업/아코디언 형태의 상세 Payload 조회 UI 구현.
      - 비밀번호, 인증 토큰 등 민감 정보(Sensitive Data) 마스킹 처리 로직 적용.

---


## 📌 [하] 개발 예정 목록 (우선순위: 하)

1. **[제안-026] 엑셀(CSV) 데이터 일괄 등록 및 다운로드**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 대규모 장비 관리에 유용하지만, 개인 목적의 소규모 운영 환경에서는 당장 실사용 빈도가 높지 않으므로 도입을 잠시 미룹니다.
   - **구현 우선순위:** [하]
   - **구현 가능성:** [중] `pandas` 또는 Python 내장 `csv` 모듈을 통한 파일 I/O 파싱 로직 및 DB 벌크 인서트 트랜잭션 등 백엔드 작업이 소요됩니다.
   - **상세 내용:** 엑셀 파일을 통해 다수의 장비를 한 번에 업로드(Import)하거나 현재 등록된 장비 목록을 파일로 다운로드(Export)하는 기능.

2. **[제안-005] 대시보드 (통계/현황 요약)**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 시스템 포털 화면에 통계를 표시함으로써 사용자와 관리자 모두에게 직관적이고 유용한 인사이트(현재 관리 중인 자산 규모 등)를 제공합니다.
   - **구현 우선순위:** [하] UI/UX 개선 측면이므로 필수 기능(보안/권한) 이후에 진행해도 무방합니다.
   - **구현 가능성:** [높음] SQLite의 `COUNT`, `GROUP BY` 쿼리를 활용해 집계 API를 만들고 프론트엔드에 간단한 CSS 카드 형태로 렌더링하면 됩니다.
   - **상세 내용:** 전체 등록 장비 수, 나의 장비 수, 카테고리별 장비 분포 요약.

3. **[제안-014] 다크 모드(Dark Mode) 지원 및 사용자 UI 테마 설정**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 최근 웹 환경의 표준 편의 기능이며, 다크 모드를 선호하는 사용자들의 눈 피로도를 낮추고 시스템의 미적 만족도(Premium UX)를 높여줍니다.
   - **구현 우선순위:** [하] 핵심 비즈니스 로직이 아니므로 여유가 있을 때 진행합니다.
   - **구현 가능성:** [높음] Tailwind CSS를 이미 사용 중이므로, HTML 최상단 태그에 `dark` 클래스를 토글하고 브라우저 `localStorage`에 상태를 저장하는 자바스크립트 코드 몇 줄로 손쉽게 구현할 수 있습니다.
   - **상세 내용:** 상단 내비게이션 바에 라이트/다크 모드 토글 스위치 제공 및 설정 유지.

4. **[제안-031] 대시보드 통계 기능 확장 (제조사 분포 및 복합 조건 검색 제공)**
   - **상태:** [개발 완료 (FEATURES.md 이관)]
   - **결정 사유:** 자산 관리의 핵심 축인 '제조사' 현황을 대시보드에 시각화하고, 카테고리/제조사 간의 다차원(복합) 분포를 조회할 수 있게 함으로써 통계 대시보드의 효용성을 대폭 증대시킵니다.
   - **구현 우선순위:** [하] (실제 구현 완료됨)
   - **구현 가능성:** [매우 높음] 기존 카테고리 통계 렌더링 로직(`api_dashboard_stats`)을 확장하여 제조사 그룹핑 및 복합 그룹핑 쿼리를 구성하면 원활히 구현 가능합니다.
   - **상세 내용:** 기존 카테고리별 장비 분포 유지, 제조사별 장비 분포 추가, 두 조건을 Select Box로 교차 적용한 상태의 복합 통계 조회 UI 신설.

5. **[제안-037] WebMCP 기반 AI 에이전트 연동 표준화 및 치명적 오류 방어망 구축**
   - **상태:** [채택 / 대기중]
   - **결정 사유:** 사람뿐만 아니라 지능형 에이전트(AI)가 시스템을 안전하게 사용할 수 있도록 하여 진정한 "AI-Ready" 웹을 구현하기 위함입니다. 단, AI의 오작동 및 시스템 파괴를 방지하기 위해 매우 엄격한 안전망 설계가 동반됩니다.
   - **구현 우선순위:** [상] 향후 다른 제안 구현 시 설계의 기반 패러다임으로 삼기 위해 우선적으로 채택됨.
   - **구현 가능성:** [높음] 기존 템플릿의 Form 구조와 JS API를 재활용하여 선언형(Declarative) / 명령형(Imperative) 분리 설계로 적용 가능.
   - **상세 내용:** 
     - 조회/검색 등 안전한 동작은 Declarative API(HTML 속성)로 구성.
     - 상태 변경/수정 등 파괴적 동작은 Imperative API(JS)로 구성하되, **CSRF 토큰 강제 검증, 파괴적 액션 전 Human-in-the-Loop(사용자 명시적 승인 버튼) 강제, Soft-Delete 전면 도입, AI 전용 감사 로그 마킹** 등 철저한 4중 안전망(Guardrails)을 구축.

---

## 📌 [최하] 개발 예정 목록 (우선순위: 최하)
*(현재 최하 우선순위로 등록된 기능은 없습니다. 추후 보류된 제안 중 일부가 재논의를 거쳐 이곳에 추가될 수 있습니다.)*

---
*참고: 위 기능들은 프로젝트 학습 목적(REST API 기본기)과 미니서버 구동 환경 최적화(경량화)를 고려하여 순차적으로 구현할 예정입니다. 각 항목의 진행 상태는 향후 작업 시 [진행중] 혹은 [완료]로 갱신됩니다.*

14. **[제안-030] Exchange Online 기반 메일링 서비스 연동 (Graph API)**
    - **상태:** [개발 완료 (FEATURES.md 이관)]
    - **결정 사유:** Microsoft 365 테넌트 환경을 고려하여, 보안과 확장성이 뛰어난 Microsoft Graph API(OAuth 2.0)를 통해 시스템의 백그라운드 메일 발송 인프라를 구축하기로 최종 승인됨.
    - **구현 우선순위:** [중] 결재 승인, 비밀번호 발급 등 다양한 알림 기능의 근간이 되는 필수 인프라.
    - **구현 가능성:** [높음] 사용자(관리자)의 Azure AD 앱 등록 선행 작업 후 `requests` 모듈로 간단히 구현 가능.
    - **상세 내용:** Azure AD 연동 및 requests 모듈을 통해 메일 발송 API 구현. 회원가입 및 재설정 등에서 인증 로직 수행.
